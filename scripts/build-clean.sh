#!/usr/bin/env bash
set -euo pipefail

# Скрипт для чистой сборки проекта на Linux
# Решает проблему зависания SwiftPM из-за блокировки .build

echo "🧹 Убиваем зависшие процессы Swift..."
pkill -9 swift 2>/dev/null || true
sleep 1

echo "🗑️  Очищаем .build и кэш пакетов..."
rm -rf .build

echo "📦 Резолвим зависимости..."
swift package resolve

echo "🔨 Собираем проект..."
swift build

echo "✅ Сборка завершена!"
