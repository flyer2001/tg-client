# Правила разработки

## TDD подход

**Тесты первыми** (RED → GREEN → REFACTOR):
- Для каждого нового модуля сначала создаём `Tests/<Module>Tests/<Module>Tests.swift` с базовыми кейсами
- Затем пишем реализацию
- Проект кроссплатформенный - сборка на Linux обязательна

## Работа с ветками

Для каждой новой задачи создаём отдельную feature-ветку:

```bash
# Создание новой ветки для задачи
git checkout -b feature/task-name

# После завершения работы и прохождения тестов
git push -u origin feature/task-name
# Создаём Pull Request через GitHub CLI или веб-интерфейс
```

Работаем в ветке до полного завершения задачи и прохождения всех тестов. Только после этого мержим в `main`. Это позволяет держать `main` всегда в рабочем состоянии и облегчает code review.

## Кросс-платформенная проверка

- Используем `#if os(macOS)` / `#if os(Linux)` только там, где неизбежно
- Регулярно собираем проект на Linux через Docker:
  ```bash
  docker run --rm -v $(pwd):/code swift:6.0 bash -c "cd /code && swift build"
  ```

### Новые зависимости/фреймворки

**ОБЯЗАТЕЛЬНАЯ проверка на Linux ПЕРЕД использованием:**

1. Если настроен GitHub Actions:
   - Попросить пользователя запустить/проверить логи CI
   - Убедиться что сборка проходит на Linux

2. Если нет CI:
   - Попросить manual проверку на VPS/Docker
   - Выполнить `swift build && swift test` на Linux

**Только после успешной проверки** начинать использовать новую зависимость в коде.

## Логирование

**На каждом внешнем вызове** (Telegram, AI, Bot):
- Перед операцией: `logger.info("Fetching unread chats...")`
- После операции: `logger.info("Fetched \(count) chats")` или `logger.error("Failed to fetch: \(error)")`

## Обработка ошибок

- Все сетевые вызовы оборачиваем в `do-catch`
- Используем кастомные enum-ошибки: `enum ChatFetcherError: Error`

## Стиль кода

- **Отступы**: 4 пробела
- **Naming**: camelCase для переменных, PascalCase для типов
- **Асинхронность**: Swift Concurrency (async/await) для сетевых вызовов
- **Комментарии**: на русском языке для бизнес-логики, на английском для технических деталей
- **Обработка ошибок**: используем кастомные enum с associated values для контекста

## JSON Encoding/Decoding

**КРИТИЧНО: Используйте ТОЛЬКО централизованные encoder/decoder во всём проекте.**

### Правило

**ЗАПРЕЩЕНО создавать encoder/decoder напрямую:**

```swift
// ❌ НЕПРАВИЛЬНО - НЕ ДЕЛАЙ ТАК
let encoder = JSONEncoder()
let decoder = JSONDecoder()
let data = try encoder.encode(request)
```

**ОБЯЗАТЕЛЬНО использовать централизованные factory методы:**

```swift
// ✅ ПРАВИЛЬНО
let encoder = JSONEncoder.tdlib()
let decoder = JSONDecoder.tdlib()
let data = try encoder.encode(request)

// ✅ ИЛИ inline
let data = try JSONEncoder.tdlib().encode(request)
```

### Почему это важно

- ✅ Гарантирует консистентность настроек по всему проекту
- ✅ Централизованное управление snake_case маппингом
- ✅ Упрощает тестирование (одна точка конфигурации)
- ✅ Предотвращает bugs из-за разных настроек encoder/decoder

### Исключения

**НЕТ.** Используй `.tdlib()` везде (Sources + Tests).

### Реализация

Централизованные encoder/decoder находятся в:
- `Sources/Shared/JSONCoding.swift`

Настройки:
- `JSONEncoder.tdlib()` - snake_case encoding
- `JSONDecoder.tdlib()` - snake_case decoding

См. также: [TASKS.md — TD-5: Централизованные Encoder/Decoder](TASKS.md)

## Документация

**Принцип:** Тесты — источник правды о внешних API, код — документация внутренних абстракций.

См. [TESTING.md — Документация через тесты](TESTING.md#документация-через-тесты) для деталей.

**В тестах документируем:**
- Примеры реальных JSON ответов от внешних API (TDLib, OpenAI, Bot API)
- Ссылки на официальную документацию
- Описание поведения внешних систем
- Edge cases

**В коде документируем:**
- Назначение внутренних протоколов и абстракций
- Контракты модулей (что принимает, что возвращает)
- Бизнес-логику (почему принято такое решение)

### Принцип минимализма в документации

**Правило:** Документация должна быть максимально лаконичной.

**Минимум:**
1. Одна строка: ЧТО делает (без деталей КАК)
2. Ссылка на официальную доку (если внешний API)
3. Инварианты (что НЕ изменится)

**ЗАПРЕЩЕНО:**
- ❌ "Используется в" (протухает)
- ❌ "Примеры использования" (дублирует тесты)
- ❌ Дублирование официальной документации
- ❌ Список всех параметров/полей (есть в коде)
- ❌ Описание внутренней реализации

**Тест на избыточность:**
Задай вопрос: "Если я удалю этот комментарий, код станет менее понятным?"
- Если НЕТ → удали комментарий
- Если ДА → оставь минимум

См. также: [TESTING.md — Правила документирования](TESTING.md#правила-документирования)

### Swift-DocC документация

Проект использует Swift-DocC для генерации живой документации.

**Структура:**
```
Sources/TgClient/TgClient.docc/
├── TgClient.md           # Главная страница
├── E2E-Scenarios/        # E2E сценарии
└── Component-Tests/      # Компонентная документация
```

**Правила актуализации:**

1. **При добавлении E2E сценария:**
   - Создать `Sources/TgClient/TgClient.docc/E2E-Scenarios/NewScenario.md`
   - Добавить ссылку в `TgClient.md` → секция "E2E Scenarios"
   - Описать flow, используемые компоненты, ссылки на тесты

2. **При добавлении нового модуля:**
   - Создать `Sources/TgClient/TgClient.docc/Component-Tests/ModuleName.md`
   - Описать назначение, API, ссылки на component/unit тесты
   - Добавить ссылку в главную страницу

3. **При добавлении модели/запроса:**
   - Обновить unit-тесты с doc comments (примеры JSON, ссылки на API docs)

4. **При изменении статуса реализации:**
   - Обновить секцию "Статус реализации" в `TgClient.md`

**Локальный preview (macOS):**
```bash
./scripts/preview-docs.sh
# Откроется http://localhost:8000/documentation/tgclient
```

**Публикация:**
- Документация автоматически публикуется на GitHub Pages при push в `main`
- Workflow запускается при изменении `.docc/`, `.swift` файлов с doc comments

## Чеклист перед коммитом

Перед созданием коммита:

1. **Записать сессию в [CHANGELOG.md](CHANGELOG.md)** (в начало файла):
   - Подробное описание что сделали
   - Список коммитов
   - Добавленные/завершенные задачи
   - **Важно:** только писать, не читать весь файл (экономия токенов)

2. **Актуализировать [TASKS.md](TASKS.md)**:
   - Отметить выполненные подзадачи (пока фича не готова)
   - Завершенные фичи → краткое резюме + ссылка на CHANGELOG
   - Добавить новые задачи
   - Обновить приоритеты

3. **Обновить Swift-DocC документацию** (если добавлены модули/сценарии):
   - Новый E2E сценарий → создать `.md` в `E2E-Scenarios/`
   - Новый модуль → создать `.md` в `Component-Tests/`
   - Новая модель/запрос → обновить unit-тесты с doc comments
   - Изменился статус фичи → обновить `TgClient.md`

4. **Проверить критичный контекст** - если в ходе работы обнаружено:
   - Изменились требования деплоя, команды, конфигурация → обновить [DEPLOY.md](DEPLOY.md)
   - Изменились требования установки для macOS → обновить [SETUP.md](SETUP.md)
   - Изменилась архитектура, модули, паттерны → обновить [ARCHITECTURE.md](ARCHITECTURE.md)
   - Нашли и решили новую проблему → обновить [TROUBLESHOOTING.md](TROUBLESHOOTING.md)
   - Изменились API credentials или переменные окружения → обновить [CREDENTIALS.md](CREDENTIALS.md)

5. **Проверить тесты**: `swift test`

6. **Проверить сборку**: `swift build`

## Git коммиты

**⚠️ ВАЖНО:**
- Сообщения коммитов на **русском языке**
- **НИКОГДА НЕ добавлять** строки:
  - ❌ `🤖 Generated with [Claude Code](https://claude.com/claude-code)`
  - ❌ `Co-Authored-By: Claude <noreply@anthropic.com>`
  - ❌ Любые упоминания Claude/AI в коммитах

**Формат коммита:**
```
Краткое описание изменений

- Детали изменения 1
- Детали изменения 2
- Детали изменения 3
```

- Коммитить логически связанные изменения вместе

### ВАЖНО: Разделяй коммиты

- Изменения в коде (`Sources/`, `Tests/`) - отдельный коммит
- Изменения в документации (`CLAUDE.md`, `DEPLOY.md`, `TASKS.md`) - отдельный коммит
- Это упрощает code review и позволяет откатывать изменения независимо

## Создание релизов

### Подготовка к релизу

1. **Проверить статус проекта:**
   - Все тесты проходят: `swift test`
   - Сборка успешна на macOS и Linux
   - Документация актуализирована
   - CHANGELOG.md обновлён с записью о текущих изменениях

2. **Закоммитить все изменения:**
   ```bash
   git add .
   git commit -m "feat: описание основных изменений релиза"
   ```

3. **Обновить TASKS.md:**
   - Отметить завершённые задачи
   - Обновить приоритеты следующей сессии

### Создание релиза через GitHub CLI

**Требования:**
- Установлен GitHub CLI: `brew install gh` (macOS) или см. [cli.github.com](https://cli.github.com)
- Авторизация выполнена: `gh auth login` (выбрать "Login with a web browser")

**Команды для создания релиза:**

```bash
# 1. Создать git tag
git tag -a v0.X.Y-alpha -m "Краткое описание релиза"

# 2. Запушить с тегами
git push origin main --tags

# 3. Создать GitHub Release
gh release create v0.X.Y-alpha \
  --title "v0.X.Y-alpha - Название релиза" \
  --notes "$(cat <<'EOF'
## 📦 Release Notes: v0.X.Y-alpha

**Краткое описание релиза** - что нового в этой версии.

### ✅ Что работает

- Список основных фич
- Что добавлено/исправлено

### 🚀 Следующий релиз

- Планы на следующую версию

### 📖 Документация

https://flyer2001.github.io/tg-client/documentation/tgclient

### ⚠️ Важно

Проект в активной разработке. Не рекомендуется для production использования.
EOF
)"
```

**Пример:** См. релиз [v0.1.0-alpha](https://github.com/flyer2001/tg-client/releases/tag/v0.1.0-alpha)

### Версионирование

Проект использует **Semantic Versioning** с alpha/beta суффиксами:

- **v0.X.Y-alpha** - ранние версии, API может меняться
- **v0.X.Y-beta** - стабилизация API перед релизом
- **v1.0.0** - первый стабильный релиз (после завершения MVP)

**Примеры:**
- `v0.1.0-alpha` - первый alpha релиз (базовая авторизация)
- `v0.1.1-alpha` - второй alpha (добавлены новые фичи)
- `v0.2.0-beta` - beta релиз (API стабилизирован)
- `v1.0.0` - стабильный релиз

### После создания релиза

1. **Обновить TASKS.md:**
   - Отметить задачу релиза как выполненную
   - Добавить ссылку на созданный релиз

2. **Добавить запись в CHANGELOG.md:**
   ```bash
   # Prepend запись о релизе
   cat > /tmp/changelog_entry.md << 'EOF'
   ## 2025-MM-DD: Релиз v0.X.Y-alpha

   **Задачи:** RELEASE-X

   ### Выполнено
   - Создан релиз v0.X.Y-alpha
   - URL: https://github.com/flyer2001/tg-client/releases/tag/v0.X.Y-alpha

   ---

   EOF

   cat /tmp/changelog_entry.md .claude/CHANGELOG.md > /tmp/changelog_new.md
   mv /tmp/changelog_new.md .claude/CHANGELOG.md
   rm /tmp/changelog_entry.md
   ```

3. **Закоммитить изменения:**
   ```bash
   git add .claude/TASKS.md .claude/CHANGELOG.md
   git commit -m "docs: актуализировать TASKS.md и CHANGELOG.md после релиза v0.X.Y-alpha"
   git push origin main
   ```

## Зависимости

- Swift 6
- Зависимости обновлены ≤ 6 мес.
- Минимум сторонних библиотек
- Секреты — только из env/конфига, не в коде

## Архитектура

- Изменения > 2 файлов или смена архитектуры — сначала короткий план/RFC
- Публичные контракты не менять без тестов
- Если правок много — сначала план

См. [ARCHITECTURE.md](ARCHITECTURE.md) для деталей архитектуры.
