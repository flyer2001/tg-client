# Ретроспектива: Анализ процесса разработки

**Дата:** 2025-11-30
**Статус:** В процессе
**Цель:** Выявить проблемы в текущем процессе TDD/архитектуры и выработать улучшения

---

## Структура документа

Каждый блок содержит:
- **Наблюдения** (факты из git истории, метрики)
- **Root Causes** (гипотезы причин)
- **Предлагаемые действия** (черновик, финализируем в конце)

В конце ретроспективы:
- Дедупликация/объединение действий
- Выбор гипотеза → решение → метрика успеха
- Запись в RETRO-RESULT.md

---

## Блок #1: Позднее обнаружение архитектурных проблем

### 📊 Наблюдения (количественная оценка):

**Статистика из git:**
- **76 коммитов** между MockTDLibClient (5379964) и TDLibFFI protocol (2aec395)
- **531 строк изменений** в коммите с исправлением race condition (7a6e20c)
- **2 коммита** с race condition фиксами:
  - 7a6e20c: устранить race condition через unified background loop
  - 256d03b: исправить race condition в updates stream initialization
- **ADR-002** написан ПОСЛЕ решения проблемы (документация post-factum)

**Проблема:**
> `td_json_client_receive()` вызывался из двух мест одновременно (authorization loop + background updates loop) → deadlock при loadChats().

**Стоимость:** ~3-4 дня работы (оценка по коммитам), ручной дебаг через принты, зависания build.

**Контекст от пользователя:**
1. Проблема обнаружилась при реализации клиента (детали API выяснились поздно)
2. Тесты НЕ падали — были странные зависания приложения
3. MockTDLibClient переписывался много раз → в итоге осознали: мокать только FFI boundary
4. Нет процедуры code review (работа человек+Claude)
5. Авторизация = sequential запросы, loadChats = parallel → критическое усложнение

---

### 🧪 Root Causes (гипотезы):

#### 1. ❌ Outside-In TDD без Architecture-First для concurrency компонентов
- **Что произошло:** начали с интерфейса (`TDLibClientProtocol`), реализовали Mock, потом Real
- **Проблема:** пропустили critical design question: "Как работает `td_json_client_receive()`? Thread-safe ли он?"
- **Доказательство:** 76 коммитов до осознания правильной архитектуры

#### 2. ❌ Неопределённая граница мокирования
- **Что произошло:** мокировали весь `TDLibClient` (high-level API) вместо C API boundary
- **Проблема:** Mock дублировал concurrency логику Real → при рефакторинге Real ломался Mock
- **Доказательство:** в коммите 2aec395 ввели `TDLibFFI protocol` — осознание что мокать нужно FFI

#### 3. ❌ Отсутствие Code Review (паузы на рефлексию)
- **Что произошло:** работа в паре человек+Claude, нет второй пары глаз
- **Проблема:** никто не задал вопрос "А может ли `receive()` вызваться параллельно?"
- **Вопрос от PO:** "Нужна ли процедура code review, когда пишем с Claude?"

#### 4. ❌ Нет инструментов для concurrency debugging
- **Что произошло:** дебажили race condition через принты, зависания build
- **Проблема:** Thread Sanitizer, async instrumentation не использовались
- **Метрика:** ручной дебаг занял ~1-2 дня вместо 10 минут с TSan

#### 5. ❌ Недооценка перехода sequential → parallel requests
- **Что произошло:** авторизация работала (sequential), loadChats сломалось (parallel)
- **Проблема:** не задали вопрос "Поддерживает ли TDLib параллельные запросы?"
- **Доказательство:** "Continuation leaked при параллельных запросах одного типа" (отмечено как НЕ критично!)

---

### 💡 Предлагаемые действия (черновик):

#### Действие 1.1: Architecture-First для Concurrency компонентов

**Гипотеза:** Если делать архитектурный анализ ДО реализации компонентов с concurrency/external API, то обнаружим критические проблемы на этапе design.

**Решение:**
- Добавить в CLAUDE.md правило: "ОБЯЗАТЕЛЬНО Architecture-First для concurrency/external API/performance-critical"
- Checklist для critical components (concurrency primitive выбран? race conditions? boundary?)
- Read ARCHITECTURE.md + PROMPTS.md (Senior Architect role) перед реализацией

**Метрика успеха (черновик):**
- Срок: До конца MVP-2 (2 недели)
- Измерение: Следующий concurrency компонент — ADR/design написан ДО первого теста?
- Целевой показатель: 0 рефакторингов "переделать concurrency после 50+ коммитов"

---

#### Действие 1.2: Определить границу мокирования явно

**Гипотеза:** Если мокировать только FFI boundary (C API), то Mock не будет ломаться при рефакторинге Real implementation.

**Решение:**
- Правило для Mock в TESTING.md: "Mock ТОЛЬКО boundary (FFI, network, filesystem), НЕ high-level API"
- Уже реализовано: MockTDLibFFI вместо MockTDLibClient ✅

**Метрика успеха (черновик):**
- Срок: Уже достигнуто
- Измерение: При следующем рефакторинге TDLibClient — 0 изменений в MockTDLibFFI?
- Целевой показатель: Mock стабилен 3+ рефакторинга подряд

---

#### Действие 1.3: Code Review через Overnight Pause

**Гипотеза:** Если делать паузу между написанием и merge критичных компонентов, то обнаружим проблемы через "свежий взгляд".

**Решение:**
- Правило в DEVELOPMENT.md: Overnight Pause для concurrency/external API компонентов
- Альтернатива: отдельная Claude сессия в режиме "Code Reviewer" (роль Senior Architect)

**Метрика успеха (черновик):**
- Срок: Тестируем на следующем критичном компоненте (SummaryGenerator)
- Измерение: Обнаружили ли проблему в overnight review, которую пропустили при написании?
- Целевой показатель: 1+ критичная проблема найдена ДО production

---

#### Действие 1.4: Включить Thread Sanitizer в CI

**Гипотеза:** Если использовать TSan в тестах, то обнаружим race conditions автоматически вместо ручного дебага.

**Решение:**
- `swift test --sanitize=thread` локально для concurrency компонентов
- Добавить в GitHub Actions workflow
- Документировать в SETUP.md

**Метрика успеха (черновик):**
- Срок: Внедрить за 1 день
- Измерение: TSan обнаруживает race condition в существующем коде?
- Целевой показатель: 0 race conditions пропущены TSan в следующих 5 компонентах

---

#### Действие 1.5: Явный анализ "Sequential → Parallel" переходов

**Гипотеза:** Если при проектировании API явно задавать вопрос "Поддерживает ли параллельные запросы?", то не пропустим concurrency проблемы.

**Решение:**
- Добавить в ARCHITECTURE.md checklist для external APIs (thread-safety? request ordering? error handling? state?)
- Документировать в TDLibClient DocC: "⚠️ td_json_client_receive() НЕ thread-safe"

**Метрика успеха (черновик):**
- Срок: Документировать за 1 день
- Измерение: При следующей external API (AI API) — checklist заполнен ДО реализации?
- Целевой показатель: 100% external APIs имеют thread-safety анализ

---

### 📏 Индикаторы успеха (общие для блока #1):

1. **Количество refactors из-за concurrency ошибок:**
   - Сейчас: 1 крупный (76 коммитов)
   - Цель: 0 на следующих 3 компонентах

2. **Время дебага race conditions:**
   - Сейчас: ~1-2 дня (ручной дебаг)
   - Цель: <1 час (TSan автоматически находит)

3. **Mock переписываний:**
   - Сейчас: MockTDLibClient → MockTDLibFFI (полная переделка)
   - Цель: 0 переделок Mock при рефакторинге Real

4. **Architecture документация:**
   - Сейчас: ADR ПОСЛЕ реализации
   - Цель: ADR/design ДО первого теста (100% critical components)

---

## Накопленный список действий (будет дополняться после каждого блока)

### Группа: Architecture & Design
- **1.1** Architecture-First для concurrency/external API компонентов
- **1.5** External API checklist (thread-safety анализ)

### Группа: Testing & Mocking
- **1.2** Mock boundary явно (FFI, network, filesystem - не high-level)
- **1.4** Thread Sanitizer в CI и локально

### Группа: Code Review & Процессы
- **1.3** Overnight Pause для критичных компонентов

---

_Следующий блок: #2 - Недооценка критичности багов_
