# Ретроспектива: Анализ процесса разработки

**Дата:** 2025-11-30
**Статус:** В процессе
**Цель:** Выявить проблемы в текущем процессе TDD/архитектуры и выработать улучшения

---

## Структура документа

Каждый блок содержит:
- **Наблюдения** (факты из git истории, метрики)
- **Root Causes** (гипотезы причин)
- **Предлагаемые действия** (черновик, финализируем в конце)

В конце ретроспективы:
- Дедупликация/объединение действий
- Выбор гипотеза → решение → метрика успеха
- Запись в RETRO-RESULT.md

---

## Блок #1: Позднее обнаружение архитектурных проблем

### 📊 Наблюдения (количественная оценка):

**Статистика из git:**
- **76 коммитов** между MockTDLibClient (5379964) и TDLibFFI protocol (2aec395)
- **531 строк изменений** в коммите с исправлением race condition (7a6e20c)
- **2 коммита** с race condition фиксами:
  - 7a6e20c: устранить race condition через unified background loop
  - 256d03b: исправить race condition в updates stream initialization
- **ADR-002** написан ПОСЛЕ решения проблемы (документация post-factum)

**Проблема:**
> `td_json_client_receive()` вызывался из двух мест одновременно (authorization loop + background updates loop) → deadlock при loadChats().

**Стоимость:** ~3-4 дня работы (оценка по коммитам), ручной дебаг через принты, зависания build.

**Контекст от пользователя:**
1. Проблема обнаружилась при реализации клиента (детали API выяснились поздно)
2. Тесты НЕ падали — были странные зависания приложения
3. MockTDLibClient переписывался много раз → в итоге осознали: мокать только FFI boundary
4. Нет процедуры code review (работа человек+Claude)
5. Авторизация = sequential запросы, loadChats = parallel → критическое усложнение

---

### 🧪 Root Causes (гипотезы):

#### 1. ❌ Outside-In TDD без Architecture-First для concurrency компонентов
- **Что произошло:** начали с интерфейса (`TDLibClientProtocol`), реализовали Mock, потом Real
- **Проблема:** пропустили critical design question: "Как работает `td_json_client_receive()`? Thread-safe ли он?"
- **Доказательство:** 76 коммитов до осознания правильной архитектуры

#### 2. ❌ Неопределённая граница мокирования
- **Что произошло:** мокировали весь `TDLibClient` (high-level API) вместо C API boundary
- **Проблема:** Mock дублировал concurrency логику Real → при рефакторинге Real ломался Mock
- **Доказательство:** в коммите 2aec395 ввели `TDLibFFI protocol` — осознание что мокать нужно FFI

#### 3. ❌ Отсутствие Code Review (паузы на рефлексию)
- **Что произошло:** работа в паре человек+Claude, нет второй пары глаз
- **Проблема:** никто не задал вопрос "А может ли `receive()` вызваться параллельно?"
- **Вопрос от PO:** "Нужна ли процедура code review, когда пишем с Claude?"

#### 4. ❌ Нет инструментов для concurrency debugging
- **Что произошло:** дебажили race condition через принты, зависания build
- **Проблема:** Thread Sanitizer, async instrumentation не использовались
- **Метрика:** ручной дебаг занял ~1-2 дня вместо 10 минут с TSan

#### 5. ❌ Недооценка перехода sequential → parallel requests
- **Что произошло:** авторизация работала (sequential), loadChats сломалось (parallel)
- **Проблема:** не задали вопрос "Поддерживает ли TDLib параллельные запросы?"
- **Доказательство:** "Continuation leaked при параллельных запросах одного типа" (отмечено как НЕ критично!)

---

### 💡 Предлагаемые действия (черновик):

#### Действие 1.1: Architecture-First для Concurrency компонентов

**Гипотеза:** Если делать архитектурный анализ ДО реализации компонентов с concurrency/external API, то обнаружим критические проблемы на этапе design.

**Решение:**
- Добавить в CLAUDE.md правило: "ОБЯЗАТЕЛЬНО Architecture-First для concurrency/external API/performance-critical"
- Checklist для critical components (concurrency primitive выбран? race conditions? boundary?)
- Read ARCHITECTURE.md + PROMPTS.md (Senior Architect role) перед реализацией

**Метрика успеха (черновик):**
- Срок: До конца MVP-2 (2 недели)
- Измерение: Следующий concurrency компонент — ADR/design написан ДО первого теста?
- Целевой показатель: 0 рефакторингов "переделать concurrency после 50+ коммитов"

---

#### Действие 1.2: Определить границу мокирования явно

**Гипотеза:** Если мокировать только FFI boundary (C API), то Mock не будет ломаться при рефакторинге Real implementation.

**Решение:**
- Правило для Mock в TESTING.md: "Mock ТОЛЬКО boundary (FFI, network, filesystem), НЕ high-level API"
- Уже реализовано: MockTDLibFFI вместо MockTDLibClient ✅

**Метрика успеха (черновик):**
- Срок: Уже достигнуто
- Измерение: При следующем рефакторинге TDLibClient — 0 изменений в MockTDLibFFI?
- Целевой показатель: Mock стабилен 3+ рефакторинга подряд

---

#### Действие 1.3: Code Review через Overnight Pause

**Гипотеза:** Если делать паузу между написанием и merge критичных компонентов, то обнаружим проблемы через "свежий взгляд".

**Решение:**
- Правило в DEVELOPMENT.md: Overnight Pause для concurrency/external API компонентов
- Альтернатива: отдельная Claude сессия в режиме "Code Reviewer" (роль Senior Architect)

**Метрика успеха (черновик):**
- Срок: Тестируем на следующем критичном компоненте (SummaryGenerator)
- Измерение: Обнаружили ли проблему в overnight review, которую пропустили при написании?
- Целевой показатель: 1+ критичная проблема найдена ДО production

---

#### Действие 1.4: Включить Thread Sanitizer в CI

**Гипотеза:** Если использовать TSan в тестах, то обнаружим race conditions автоматически вместо ручного дебага.

**Решение:**
- `swift test --sanitize=thread` локально для concurrency компонентов
- Добавить в GitHub Actions workflow
- Документировать в SETUP.md

**Метрика успеха (черновик):**
- Срок: Внедрить за 1 день
- Измерение: TSan обнаруживает race condition в существующем коде?
- Целевой показатель: 0 race conditions пропущены TSan в следующих 5 компонентах

---

#### Действие 1.5: Явный анализ "Sequential → Parallel" переходов

**Гипотеза:** Если при проектировании API явно задавать вопрос "Поддерживает ли параллельные запросы?", то не пропустим concurrency проблемы.

**Решение:**
- Добавить в ARCHITECTURE.md checklist для external APIs (thread-safety? request ordering? error handling? state?)
- Документировать в TDLibClient DocC: "⚠️ td_json_client_receive() НЕ thread-safe"

**Метрика успеха (черновик):**
- Срок: Документировать за 1 день
- Измерение: При следующей external API (AI API) — checklist заполнен ДО реализации?
- Целевой показатель: 100% external APIs имеют thread-safety анализ

---

### 📏 Индикаторы успеха (общие для блока #1):

1. **Количество refactors из-за concurrency ошибок:**
   - Сейчас: 1 крупный (76 коммитов)
   - Цель: 0 на следующих 3 компонентах

2. **Время дебага race conditions:**
   - Сейчас: ~1-2 дня (ручной дебаг)
   - Цель: <1 час (TSan автоматически находит)

3. **Mock переписываний:**
   - Сейчас: MockTDLibClient → MockTDLibFFI (полная переделка)
   - Цель: 0 переделок Mock при рефакторинге Real

4. **Architecture документация:**
   - Сейчас: ADR ПОСЛЕ реализации
   - Цель: ADR/design ДО первого теста (100% critical components)

---

## Накопленный список действий (будет дополняться после каждого блока)

### Группа: Architecture & Design
- **1.1** Architecture-First для concurrency/external API компонентов
- **1.5** External API checklist (thread-safety анализ)

### Группа: Testing & Mocking
- **1.2** Mock boundary явно (FFI, network, filesystem - не high-level)
- **1.4** Thread Sanitizer в CI и локально

### Группа: Code Review & Процессы
- **1.3** Overnight Pause для критичных компонентов

---

## Блок #2: Недооценка критичности багов

### 📊 Наблюдения:

**Что произошло:**
- **Коммит 7a6e20c (2025-11-19):** "Известные ограничения: ⚠️ Continuation leaked при параллельных запросах одного типа"
- **TASKS.md:** задача TD-3 "Улучшение request-response механизма" с приоритетом **Medium**
- **Пометка:** "Known Issues" (ограничение, не баг) + "решение: @extra field - см. TASKS.md"
- **Обнаружение критичности:** только после переспроса пользователя "почему не критично?"
- **Реальный impact:** Continuation leak **блокировал разработку** → муторный дебаг на каждом шаге

**Контекст от пользователя:**
1. Были warning'и от Swift или зависания при разработке
2. Claude пометил как "не критично" и продолжил писать код
3. Пользователь переспросил → только тогда поняли критичность
4. Ожидание: "Ты как профессионал Swift Concurrency должен был САМ выявить критичность на этапе проектирования"
5. Правило: "Не критично" допустимо ТОЛЬКО для супер редких кейсов с явным описанием
6. Эти "баги" с многопоточкой блокировали разработку, принуждая дебажить после каждого шага

---

### 🧪 Root Causes (гипотезы):

#### 1. ❌ Недостаточное понимание Swift Concurrency severity

**Что должен был знать:**
- Continuation leak в Swift = ВСЕГДА критично (не "technical debt")
- Причины: memory leak + undefined behavior + deadlock risk

**Что сделал:**
- Пометил как "известное ограничение" вместо **P0 BLOCKER**

#### 2. ❌ Отсутствие Bug Severity Matrix для concurrency

**Проблема:**
- Нет классификации "что критично, что нет" для concurrency багов
- Нет правил: "Data loss > Deadlock > Memory leak > Performance"

**Результат:**
- Continuation leak (memory leak + deadlock risk) → "Medium priority"
- Должно быть: **P0 BLOCKER**

#### 3. ❌ Давление продвигаться вперед ("есть workaround → идем дальше")

**Что произошло:**
- Подумал: "Можно пофиксить через @extra позже" → идем дальше
- Реальность: Блокирует development на каждом шаге

**Антипаттерн:**
```
"Не критично, идем дальше" ← ДОПУСТИМО ТОЛЬКО для супер редких кейсов
```

#### 4. ❌ Нет проактивной проверки перед пометкой "не критично"

**Что должен был сделать:**
1. Увидел continuation leak → СТОП
2. Задать вопрос: "Может ли это сломать production?"
3. Написать тест: "Два параллельных getChatHistory() одновременно"
4. Тест падает → BLOCKER → Фиксим СЕЙЧАС

**Что сделал:**
1. Увидел leak → пометил "Known Issues" → продолжил

#### 5. ❌ Некорректная classification: "Known Issues" vs "Blocker Bug"

**Путаница терминов:**
- "Known Issues" = edge case, можно отложить
- "Blocker Bug" = ломает функционал СЕЙЧАС, нельзя релизить

**Continuation leak:** BLOCKER, не "Known Issues"

---

### 💡 Предлагаемые действия (черновик):

#### Действие 2.1: Bug Severity Matrix для Swift Concurrency

**Гипотеза:** Если есть явная классификация критичности для concurrency багов, то не пропустим BLOCKER.

**Решение:**
- Добавить в DEVELOPMENT.md или ARCHITECTURE.md Bug Severity Matrix:
  - P0 BLOCKER: data loss, deadlock, memory leak (continuation leaked), crash, race condition
  - P1 HIGH: performance regression >2x, error handling broken
  - P2 MEDIUM: missing timeout (есть retry), suboptimal concurrency
  - P3 LOW: missing feature (пока не нужно), code duplication

**Метрика успеха (черновик):**
- При следующем баге → классификация по Matrix ДО пометки "не критично"
- 0 случаев "BLOCKER помечен как Medium"

---

#### Действие 2.2: Правило "Continuation leak = P0 BLOCKER"

**Гипотеза:** Если явно зафиксировать что continuation leak = BLOCKER, то не будет недооценки.

**Решение:**
- Добавить в DEVELOPMENT.md или ARCHITECTURE.md список "Автоматические BLOCKER'ы":
  - Continuation leaked → ВСЕГДА P0
  - Double resume → ВСЕГДА P0
  - Actor deadlock → ВСЕГДА P0
  - Data race (TSan warning) → ВСЕГДА P0

**Правило:** Если видишь эти паттерны → СТОП → фиксим СЕЙЧАС → продолжаем.

**Метрика успеха (черновик):**
- 0 случаев "continuation leak помечен как не критично"

---

#### Действие 2.3: Проактивная проверка перед "не критично"

**Гипотеза:** Если перед пометкой "не критично" написать тест воспроизводящий проблему, то увидим реальный impact.

**Решение:**
- Добавить checklist в DEVELOPMENT.md перед пометкой "не критично":
  1. Написать тест воспроизводящий проблему (production сценарий)
  2. Проверить: тест падает? → BLOCKER
  3. Проверить: есть workaround БЕЗ overhead? → можно отложить
  4. Задать вопрос: "Может ли это сломать production?"
  5. Если сомневаешься → спроси пользователя (не решай сам!)

**Метрика успеха (черновик):**
- При следующем "не критично" → есть тест доказывающий что не ломает функционал

---

#### Действие 2.4: Code Review Checklist для Concurrency

**Гипотеза:** Если есть checklist для review concurrency кода, то не пропустим критичные баги.

**Решение:**
- Добавить в DEVELOPMENT.md или ARCHITECTURE.md checklist для concurrency code review:
  - Все CheckedContinuation resume'ятся ровно 1 раз?
  - Нет race conditions? (TSan проверен)
  - Нет deadlock? (actor ждёт сам себя)
  - Error handling: continuation resume в catch блоке?
  - Timeout для external API?
  - Cancellation в long loops?

**Метрика успеха (черновик):**
- При overnight review следующего concurrency компонента → checklist заполнен

---

### 📏 Индикаторы успеха (общие для блока #2):

1. **BLOCKER'ов помеченных как "не критично":**
   - Сейчас: 1 (continuation leak)
   - Цель: 0

2. **Время обнаружения критичности:**
   - Сейчас: После переспроса пользователя
   - Цель: Автоматически (TSan, тест, checklist)

3. **"Не критично" с доказательством:**
   - Сейчас: 0% (без теста)
   - Цель: 100% (тест показывает что не ломает функционал)

---

## Накопленный список действий (обновлён после блока #2)

### Группа: Architecture & Design
- **1.1** Architecture-First для concurrency/external API компонентов
- **1.5** External API checklist (thread-safety анализ)
- **2.1** Bug Severity Matrix для Swift Concurrency (P0/P1/P2/P3)
- **2.2** Автоматические BLOCKER'ы (continuation leak, double resume, etc.)

### Группа: Testing & Mocking
- **1.2** Mock boundary явно (FFI, network, filesystem - не high-level)
- **1.4** Thread Sanitizer в CI и локально

### Группа: Code Review & Процессы
- **1.3** Overnight Pause для критичных компонентов
- **2.3** Checklist перед пометкой "не критично" (тест воспроизводящий проблему)
- **2.4** Code Review Checklist для Concurrency кода

---

## Блок #3: Outside-In TDD и cascade changes

### 📊 Наблюдения:

**Масштаб cascade changes:**
- **Период:** Nov 19 → Nov 28 (9 дней)
- **Коммитов:** 20 коммитов рефакторинга
- **Изменений:** **+4909 строк, -2172 строк** (98 файлов!)
- **AuthenticationFlowTests закомментирован:** Nov 23 → Nov 28 (5 дней)

**Timeline:**
1. **Nov 19** (7a6e20c) - unified background loop → начало cascade
2. **Nov 23** (33f318d) - закомментировали AuthenticationFlowTests (сломался!)
3. **Nov 24** (256d03b) - fix race condition в updates stream
4. **Nov 28** (afb6a13) - финальный fix fire-and-forget + @extra

**Что произошло:**
1. Начали с Outside-In TDD: интерфейс TDLibClient → Mock → тесты → реализация
2. Авторизация работала (sequential запросы)
3. При добавлении loadChats (parallel запросы) выяснилось:
   - `receive()` блокирующий, нельзя вызывать из двух мест
   - @extra механизм не использовался → continuation leak
   - MockTDLibClient дублировал всю логику → при рефакторинге Real ломался Mock
4. Cascade changes: 20 коммитов, 9 дней, ~7000 строк изменений
5. AuthenticationFlowTests закомментирован на 5 дней
6. Финальное решение: TDLibFFI protocol + fire-and-forget vs @extra разделение

**Контекст от пользователя:**
1. "Постоянно откатываться назад из-за изменений в TDLibClient"
2. "MockTDLibClient дублировал функционал → переписывали тесты, реализацию, Mock → ад и напряжение"
3. "Мок компонентный тест падал, авторизация сломалась → отключили тест"
4. "Как предусмотреть на этапе проектирования когда не знали как работает TDLib API?"
5. Вопрос: "Может нужен был Inside-Out TDD (от API к тестам) вместо Outside-In?"

---

### 🧪 Root Causes (гипотезы):

#### 1. ❌ Outside-In TDD БЕЗ Spike/Research при работе с незнакомым external API

**Проблема:**
- Начали с интерфейса (`TDLibClientProtocol`) БЕЗ изучения реального TDLib API
- Предположили что API работает как REST (request → response), а реальность:
  - `receive()` блокирующий, единая очередь
  - @extra optional механизм для matching
  - Fire-and-forget vs Request-Response разные паттерны

**Что должны были сделать:**
1. Spike/Research фаза (1-2 часа): minimal example с реальным TDLib
2. Документировать находки: blocking receive, threading model, @extra
3. Architecture First с учетом constraints
4. ПОТОМ Outside-In TDD

**Доказательство:**
- 20 коммитов рефакторинга, 9 дней работы

#### 2. ❌ Отсутствие E2E Regression тестов на критичные пути

**Проблема:**
- Авторизация = ручная проверка (не автоматизирована)
- Component тест падал → закомментировали
- E2E не обнаружил проблему

**Результат:**
- Сломали авторизацию при loadChats
- Обнаружили только когда запустили Component тесты

#### 3. ❌ Mock дублировал всю логику Real implementation

**Проблема:**
- MockTDLibClient дублировал ResponseWaiters, updates loop, все методы
- При рефакторинге Real → Mock тоже ломался
- Двойная работа: переписать Real + переписать Mock

**Связь с блоком #1:** Та же проблема "неопределённая граница мокирования"

#### 4. ❌ Нет защиты от cascade changes (regression suite)

**Антипаттерн:**
```
Тест падает → закомментировать → идем дальше
```

**Правильно:**
```
Тест падает → СТОП → понять почему → fix → green → продолжаем
```

#### 5. ❌ Outside-In TDD не подходит для незнакомых external APIs

**Ключевое наблюдение пользователя:**
> Возможно нужен был Inside-Out TDD (от API к тестам) вместо Outside-In?

**Гипотеза:**
- Outside-In работает когда мы контролируем все слои
- Когда external API unknown → нужен **Spike-First, потом TDD**
- Или **Hybrid:** Spike → Architecture First → Inside-Out (от C API) → Outside-In (от high-level API)

---

### 💡 Предлагаемые действия (черновик):

#### Действие 3.1: Spike/Research фаза для незнакомых external APIs

**Гипотеза:** Если делать Spike ДО TDD при незнакомом API, то не будет cascade changes.

**Решение:**
- Добавить в DEVELOPMENT.md правило: "Spike-First для external APIs"
- Checklist когда нужен Spike:
  - Незнакомая C библиотека
  - API не документирован или документация устарела
  - Не знаем threading model / concurrency constraints
- Spike output: minimal working example (1-2 часа) + documented findings
- ПОТОМ Architecture First → TDD

**Метрика успеха (черновик):**
- При следующем external API (AI API) → Spike ДО TDD?
- 0 cascade changes из-за "не знали как работает API"

---

#### Действие 3.2: Обязательные E2E Regression тесты для критичных путей

**Гипотеза:** Если запускать E2E regression suite после каждого рефакторинга, то обнаружим cascade breakage раньше.

**Решение:**
- Создать E2E Regression Suite:
  - test_auth_phone_code.sh (авторизация)
  - test_load_chats.sh (загрузка чатов)
  - test_get_chat_history.sh (получение сообщений)
- Запускать после каждого рефакторинга TDLibClient
- Если E2E падает → BLOCKER → откат/fix

**Автоматизация:**
- GitHub Actions: E2E suite в отдельном job
- Локально: ./scripts/e2e-regression.sh перед коммитом

**Метрика успеха (черновик):**
- При следующем рефакторинге TDLibClient → E2E suite запущен ДО коммита?

---

#### Действие 3.3: Запрет на комментирование падающих тестов

**Гипотеза:** Если запретить комментировать падающие тесты, то не будет скрытых регрессий.

**Решение:**
- Добавить в DEVELOPMENT.md правило:
  ```
  ❌ ЗАПРЕЩЕНО:
  - Комментировать падающий тест чтобы "пофиксить позже"
  - Skip тесты без TODO + дата

  ✅ ПРАВИЛЬНО:
  - Тест падает → СТОП → понять → fix → green
  - Если fix долгий (>1 час) → revert breaking change
  ```

**Метрика успеха (черновик):**
- 0 закомментированных тестов в main branch

---

#### Действие 3.4: Hybrid TDD для external APIs (Spike → Inside-Out → Outside-In)

**Гипотеза:** При external API нужен hybrid подход вместо чистого Outside-In.

**Решение:**
- Добавить в TESTING.md: "TDD Strategy для External APIs"
  1. Spike (1-2 часа): minimal example, изучить constraints
  2. Architecture First: boundary, threading model
  3. Inside-Out: от C API wrapper к Swift (TDLibFFI → ResponseWaiters)
  4. Outside-In: от high-level API к интеграции (TDLibClient)

**Ключевое:** сначала понимаем constraints, ПОТОМ проектируем interface.

**Метрика успеха (черновик):**
- При следующем external API → использован Hybrid?
- Spike output документирован?

---

### 📏 Индикаторы успеха (общие для блока #3):

1. **Cascade changes из-за незнания API:**
   - Сейчас: 1 крупный (20 коммитов, 9 дней, 7000 строк)
   - Цель: 0

2. **Закомментированные тесты:**
   - Сейчас: 1 (AuthenticationFlowTests на 5 дней)
   - Цель: 0

3. **E2E Regression coverage:**
   - Сейчас: ручная проверка
   - Цель: автоматизированная suite

4. **Spike ДО TDD:**
   - Сейчас: 0%
   - Цель: 100% для external APIs

---

## Накопленный список действий (обновлён после блока #3)

### Группа: Architecture & Design
- **1.1** Architecture-First для concurrency/external API компонентов
- **1.5** External API checklist (thread-safety анализ)
- **2.1** Bug Severity Matrix для Swift Concurrency (P0/P1/P2/P3)
- **2.2** Автоматические BLOCKER'ы (continuation leak, double resume, etc.)
- **3.1** Spike/Research фаза для незнакомых external APIs
- **3.4** Hybrid TDD для external APIs (Spike → Inside-Out → Outside-In)

### Группа: Testing & Mocking
- **1.2** Mock boundary явно (FFI, network, filesystem - не high-level)
- **1.4** Thread Sanitizer в CI и локально
- **3.2** E2E Regression тесты для критичных путей (auth, loadChats, getChatHistory)
- **3.3** Запрет на комментирование падающих тестов

### Группа: Code Review & Процессы
- **1.3** Overnight Pause для критичных компонентов
- **2.3** Checklist перед пометкой "не критично" (тест воспроизводящий проблему)
- **2.4** Code Review Checklist для Concurrency кода

---

## Блок #4: Overhead от MockClient: дублирование логики Real клиента

### 📊 Наблюдения:

**Количественная оценка "монстра":**
- **MockTDLibFFI:** 381 строка
- **Real CTDLibFFI:** 75 строк
- **Соотношение:** Mock в **5x раз больше** Real!

**Real TDLibFFI (3 метода):**
```swift
func create() throws
func send(_ request: String)
func receive(timeout: Double) -> String?
```

**MockTDLibFFI (13 методов/свойств):**
- 7 private свойств: lock, mockedResponses, queuedUpdates, updatesProcessed, responsesByExtra, pendingUpdates, expectedThread
- 6 public методов: mockResponse(), queueUpdate(), mockUpdate() x2, send(), receive(), create()
- 2 private helpers: handleLoadChats(), handleGetChat()

**Контекст от пользователя:**
1. "Даже благодаря MockFFI подходу родили какого-то монстра"
2. "Вот эта 'имитация' реального поведения - не сильно ли дорого нам обходится"
3. "Не придется ли нам такого же монстра рожать для OpenAI API?"
4. Вопрос: "Нам 'повезло' что столкнулись со сложной библиотекой TDLib. С простым API будет проще?"

---

### 🧪 Root Causes (гипотезы):

#### 1. ❌ Имитация сложного асинхронного API

**Проблема:**
- TDLib API: blocking receive() + async updates stream + @extra matching
- Mock имитирует ВСЁ это поведение → 381 строка кода

**Контраст:**
- **OpenAI API:** HTTP request → response (простой Mock = 1 dictionary)
- **TDLib API:** async, updates, @extra → сложный Mock = 381 строка

#### 2. ❌ Не рассмотрели альтернативы Mock (Integration Test)

**Альтернатива:**
- Integration Test с реальным TDLib в sandbox mode
- Плюсы: 0 строк Mock кода, тестируем реальное поведение
- Минусы: требует credentials, медленнее (но насколько?)

**Вопрос:** Почему не использовали Integration Test вместо Mock?

#### 3. ❌ Mock имитирует implementation details, а не interface

**Проблема:**
- Mock знает про `loadChats` логику (handleLoadChats)
- Mock знает про `getChat` логику (handleGetChat)
- Это **не Mock FFI**, это **Fake TDLib**!

**Ожидание от FFI Mock:**
```swift
mockFFI.addResponse(json: """{"@type": "ok"}""")
mockFFI.receive() // → возвращает что добавили
```

**Реальность:**
```swift
mockFFI.queueUpdate(...) // имитирует loadChats
mockFFI.handleLoadChats(...) // знает про TDLib API детали
```

#### 4. ❌ Нет правила "когда Mock слишком сложный"

**Проблема:**
- Mock = 381 строка → это отдельный компонент требующий тестов!
- Нет порога "если Mock >100 строк → рассмотреть Integration Test"

---

### 💡 Предлагаемые действия (черновик):

#### Действие 4.1: Decision Tree "Mock vs Integration vs Fake"

**Гипотеза:** Если есть явное правило когда использовать Mock, не создадим "монстра" для OpenAI.

**Решение:**
- Добавить в TESTING.md Decision Tree:
  ```markdown
  ## Mock vs Integration Test Decision Tree

  ### Mock (простой, <100 строк):
  - ✅ HTTP API (request → response)
  - ✅ Синхронный API (нет async updates)
  - ✅ Deterministic behavior
  - Пример: OpenAI API, REST API

  ### Integration Test (реальная библиотека):
  - ✅ Асинхронный API (updates stream)
  - ✅ Сложная логика (если Mock >100 строк)
  - ✅ Есть sandbox mode / test credentials
  - Пример: TDLib, database, message queue

  ### Fake (полная имитация):
  - ⚠️ КРАЙНИЙ СЛУЧАЙ
  - Требует тестов на сам Fake!
  - Пример: MockTDLibFFI (381 строка)
  ```

**Метрика успеха (черновик):**
- При проектировании Mock для OpenAI → Decision Tree использован?

---

#### Действие 4.2: Правило "Mock >100 строк = Integration Test"

**Гипотеза:** Если ограничить сложность Mock, не создадим overhead.

**Решение:**
- Правило в TESTING.md:
  ```
  Mock complexity limit: 100 строк

  Если Mock превышает 100 строк:
  1. СТОП → рассмотреть Integration Test
  2. Если Integration Test невозможен → создаём Fake
  3. Fake требует собственных тестов
  ```

**Метрика успеха (черновик):**
- 0 новых Mock'ов >100 строк без обсуждения Integration Test

---

#### Действие 4.3: Для OpenAI API → Integration Test или простой Mock

**Гипотеза:** OpenAI API простой → простой Mock или Integration Test.

**Решение:**
- При проектировании OpenAI integration:
  1. Рассмотреть Integration Test (реальный API с test credentials)
  2. Если нужен Mock → простой dictionary (request → response)
  3. Если Mock >100 строк → СТОП

**Пример простого Mock:**
```swift
class MockOpenAI: OpenAIProtocol {
    var responses: [String: String] = [:] // ~10 строк, не 381!
}
```

**Метрика успеха (черновик):**
- Mock для OpenAI <50 строк?

---

#### Действие 4.4: Ретроспектива "Стоило ли делать MockTDLibFFI?"

**Вопрос для обсуждения:**
- Стоило ли писать 381 строку MockTDLibFFI?
- Или лучше было Integration Test с реальным TDLib?

**Плюсы Integration Test:**
- 0 строк Mock кода
- Тестируем реальное поведение
- Нет cascade changes при рефакторинге Mock

**Минусы Integration Test:**
- Требует credentials
- Медленнее? (нужно измерить)
- Flaky tests?

**Решение (черновик):**
- Для будущих сложных APIs → сначала попробовать Integration Test

---

### 📏 Индикаторы успеха (общие для блока #4):

1. **Mock complexity:**
   - Сейчас: 381 строка (5x Real)
   - Цель: Новые Mock'и <100 строк

2. **Decision Tree использован:**
   - Сейчас: не было правила
   - Цель: 100% новых APIs

3. **Integration Test рассмотрен:**
   - Сейчас: сразу начали с Mock
   - Цель: Для сложных APIs → сначала Integration Test

---

## Накопленный список действий (обновлён после блока #4)

### Группа: Architecture & Design
- **1.1** Architecture-First для concurrency/external API компонентов
- **1.5** External API checklist (thread-safety анализ)
- **2.1** Bug Severity Matrix для Swift Concurrency (P0/P1/P2/P3)
- **2.2** Автоматические BLOCKER'ы (continuation leak, double resume, etc.)
- **3.1** Spike/Research фаза для незнакомых external APIs
- **3.4** Hybrid TDD для external APIs (Spike → Inside-Out → Outside-In)

### Группа: Testing & Mocking
- **1.2** Mock boundary явно (FFI, network, filesystem - не high-level)
- **1.4** Thread Sanitizer в CI и локально
- **3.2** E2E Regression тесты для критичных путей (auth, loadChats, getChatHistory)
- **3.3** Запрет на комментирование падающих тестов
- **4.1** Decision Tree "Mock vs Integration vs Fake"
- **4.2** Mock complexity limit: 100 строк
- **4.3** Для простых APIs (OpenAI) → Integration Test или простой Mock (<50 строк)
- **4.4** Ретроспектива решения Mock vs Integration для каждого API

### Группа: Code Review & Процессы
- **1.3** Overnight Pause для критичных компонентов
- **2.3** Checklist перед пометкой "не критично" (тест воспроизводящий проблему)
- **2.4** Code Review Checklist для Concurrency кода

---

## Блок #5: Actor vs NSLock: неправильный дефолт выбор

### 📊 Наблюдения:

**Количественная оценка "actor → lock" переделок:**
- **ResponseWaiters:** actor (2aec395) → class + NSLock (7a6e20c)
- **@unchecked Sendable:** 1 место (только TDLibClient)
- **nonisolated(unsafe):** 2 использования (loggingConfigured флаг, response в ResponseWaiters)
- **NSLock использование:** TDLibClient (stopLock, counterLock) + ResponseWaiters

**Timeline переделки:**
1. **Nov 21** (2aec395): ResponseWaiters создан как `actor`
   - Комментарий: "Реализован как Swift actor для thread-safe доступа"
2. **Nov 19** (7a6e20c): Переделан в `class` с NSLock
   - Причина: "C interop с TDLib - actor не подходит для C interop"

**Контекст от пользователя:**
1. "MockTDClient был actor, а то что мокировали был class → несоответствие поведения"
2. "Много wrappers чтобы не было проблем с компиляцией Swift 6"
3. "Не получилось везде реализовать чистый structured concurrency, потому что C TDLib чихала на это"
4. "Проблема: чтобы не использовать много wrappers для компиляции"
5. "Расставленные wrappers потом нам не выстрелили в ногу"

---

### 🧪 Root Causes (гипотезы):

#### 1. ❌ Actor-first подход БЕЗ анализа C interop constraints

**Проблема:**
- По умолчанию выбирали `actor` для thread safety
- Не анализировали: "Это C API? Blocking calls? Можно ли actor?"
- Результат: переделка actor → class + NSLock

**Доказательство:**
- ResponseWaiters: сначала actor, потом class (1 переделка)

#### 2. ❌ Impedance mismatch: Swift Structured Concurrency vs C API

**Проблема:**
- **Swift Concurrency:** async/await, actor isolation, Sendable
- **C TDLib:** blocking receive(), thread-unsafe, non-Sendable [String: Any]
- **Результат:** wrappers, @unchecked Sendable, nonisolated(unsafe)

**Цитата из кода:**
```swift
// SAFETY: Dictionary [String: Any] не Sendable, но:
// - TDLib возвращает immutable JSON dictionary
// - NSLock гарантирует thread-safe
nonisolated(unsafe) let unsafeResponse = response
```

#### 3. ❌ Нет Decision Tree "когда actor, когда lock"

**Проблема:**
- Нет явных правил выбора concurrency primitive
- Нет checklist: "C API? → lock, не actor"

#### 4. ❌ MockTDClient actor vs Real class → несоответствие

**Проблема:**
- Mock был actor (для удобства async тестов)
- Real был class (из-за C API constraints)
- Несоответствие поведения → путаница

#### 5. ❌ Накопление @unchecked Sendable wrappers

**Проблема:**
- Каждый wrapper = потенциальная проблема в будущем
- "Расставленные wrappers потом нам не выстрелили в ногу"

---

### 💡 Предлагаемые действия (черновик):

#### Действие 5.1: Decision Tree "Actor vs Lock vs OSAllocatedUnfairLock"

**Гипотеза:** Если есть явное правило выбора concurrency primitive, не будет переделок.

**Решение:**
- Добавить в ARCHITECTURE.md Decision Tree:
  ```markdown
  ## Swift 6 Concurrency: Actor vs Lock Decision Tree

  ### Используй Actor когда:
  - ✅ Pure Swift код (не C interop)
  - ✅ Async operations (suspend points OK)
  - ✅ Sendable типы данных
  - ✅ Не нужен immediate synchronous access
  - Пример: бизнес-логика, state machines

  ### Используй NSLock когда:
  - ✅ C API interop (blocking calls)
  - ✅ Non-Sendable типы ([String: Any])
  - ✅ Synchronous immediate access нужен
  - ✅ Hot path (critical performance)
  - Пример: TDLibClient, ResponseWaiters

  ### Используй OSAllocatedUnfairLock когда:
  - ✅ Все что NSLock + нужна max performance
  - ✅ Swift 6.0+ only
  - ⚠️ Unfair (нет FIFO гарантий)
  - Пример: high-frequency counters, caches
  ```

**Метрика успеха (черновик):**
- При следующем concurrency компоненте → Decision Tree использован?
- 0 переделок actor → lock

---

#### Действие 5.2: Spike для C interop: проверить constraints ДО выбора primitive

**Гипотеза:** Если проверять C API constraints ДО выбора actor/lock, не будет переделок.

**Решение:**
- Расширить Spike фазу (действие 3.1) checklist для C interop:
  ```markdown
  ## Spike Checklist для C API

  При интеграции с C library:
  1. **Blocking calls?** (receive, accept, read) → lock, не actor
  2. **Thread-safe?** Документация говорит что да?
  3. **Sendable типы?** Если non-Sendable → wrapper нужен
  4. **Actor isolation совместима?** Можно ли suspend в critical section?

  Если хоть один ответ "НЕТ" → lock, не actor.
  ```

**Метрика успеха (черновик):**
- При следующем C API → checklist заполнен ДО выбора primitive?

---

#### Действие 5.3: Mock должен имитировать Real primitive (actor = actor, class = class)

**Гипотеза:** Если Mock использует ту же concurrency модель что Real, не будет несоответствия.

**Решение:**
- Правило в TESTING.md:
  ```markdown
  ## Mock Concurrency Model

  Mock ДОЛЖЕН использовать ту же concurrency primitive что Real:
  - Real = actor → Mock = actor
  - Real = class + NSLock → Mock = class + NSLock

  ❌ ЗАПРЕЩЕНО:
  - Real = class, Mock = actor (несоответствие поведения)
  ```

**Метрика успеха (черновик):**
- 0 случаев "Mock actor, Real class"

---

#### Действие 5.4: Аудит @unchecked Sendable + обязательные concurrency тесты

**Гипотеза:** Если каждый @unchecked Sendable покрыт concurrency тестом, меньше риска.

**Решение:**
- Правило в DEVELOPMENT.md:
  ```markdown
  ## @unchecked Sendable: когда допустимо

  ✅ ДОПУСТИМО когда:
  1. Mutable state защищён lock/actor
  2. SAFETY комментарий объясняет почему безопасно
  3. **Есть concurrency тест доказывающий thread safety**
  4. Нет альтернативы (C API, legacy code)

  ❌ ЗАПРЕЩЕНО:
  - "Просто чтобы скомпилировалось"
  - Без SAFETY комментария
  - **Без concurrency теста**

  **Concurrency тест должен:**
  - Параллельные вызовы (10+ threads/tasks одновременно)
  - Thread Sanitizer включён (--sanitize=thread)
  - Проверка race conditions, deadlocks

  **Пример теста:**
  ```swift
  @Test func responseWaiters_threadSafety() async throws {
      let waiters = ResponseWaiters()
      await withTaskGroup(of: Void.self) { group in
          for i in 0..<100 {
              group.addTask {
                  await waiters.addWaiter(...)
              }
          }
      }
      // Если TSan не ругается → OK
  }
  ```

  **Code Review:** Каждый @unchecked Sendable требует:
  - SAFETY комментарий
  - Concurrency тест
  - Обоснование почему нельзя использовать Sendable wrapper
  ```

**Текущий аудит:**
- TDLibClient: @unchecked Sendable (есть ли concurrency тест? проверить)
- ResponseWaiters: есть ResponseWaitersTests (проверить TSan coverage)

**Метрика успеха (черновик):**
- 100% @unchecked Sendable покрыты concurrency тестами
- TSan включён для всех concurrency тестов

---

### 📏 Индикаторы успеха (общие для блока #5):

1. **Переделки actor → lock:**
   - Сейчас: 1 (ResponseWaiters)
   - Цель: 0

2. **Decision Tree использован:**
   - Сейчас: не было правила
   - Цель: 100% concurrency компонентов

3. **@unchecked Sendable с тестом:**
   - Сейчас: проверить (ResponseWaitersTests существует)
   - Цель: 100% (все с concurrency тестом + TSan)

4. **Mock/Real consistency:**
   - Сейчас: OK (оба class)
   - Цель: 100% соответствие

---

## Накопленный список действий (обновлён после блока #5)

### Группа: Architecture & Design
- **1.1** Architecture-First для concurrency/external API компонентов
- **1.5** External API checklist (thread-safety анализ)
- **2.1** Bug Severity Matrix для Swift Concurrency (P0/P1/P2/P3)
- **2.2** Автоматические BLOCKER'ы (continuation leak, double resume, etc.)
- **3.1** Spike/Research фаза для незнакомых external APIs
- **3.4** Hybrid TDD для external APIs (Spike → Inside-Out → Outside-In)
- **5.1** Decision Tree "Actor vs Lock vs OSAllocatedUnfairLock"
- **5.2** Spike Checklist для C interop (blocking? thread-safe? sendable?)

### Группа: Testing & Mocking
- **1.2** Mock boundary явно (FFI, network, filesystem - не high-level)
- **1.4** Thread Sanitizer в CI и локально
- **3.2** E2E Regression тесты для критичных путей (auth, loadChats, getChatHistory)
- **3.3** Запрет на комментирование падающих тестов
- **4.1** Decision Tree "Mock vs Integration vs Fake"
- **4.2** Mock complexity limit: 100 строк
- **4.3** Для простых APIs (OpenAI) → Integration Test или простой Mock (<50 строк)
- **4.4** Ретроспектива решения Mock vs Integration для каждого API
- **5.3** Mock должен использовать ту же concurrency primitive что Real
- **5.4** @unchecked Sendable: SAFETY комментарий + concurrency тест + TSan

### Группа: Code Review & Процессы
- **1.3** Overnight Pause для критичных компонентов
- **2.3** Checklist перед пометкой "не критично" (тест воспроизводящий проблему)
- **2.4** Code Review Checklist для Concurrency кода

---

## Блок #6: ADR: избыточность и причины появления

### 📊 Наблюдения:

**Количественная оценка:**
- **ADR-001:** 102 строки (ChannelMessageSource.fetchUnreadMessages, 2025-11-17)
- **ADR-002:** 183 строки (TDLib Unified Background Loop, 2025-11-19)
- **Общий объём:** 285 строк архитектурной документации
- **Формат:** 4 блока (Производительность, Память, Отказоустойчивость, Логирование)

**Timeline ADR-001:**
1. bfba310 (Nov 17 22:03): добавить модели Message, GetChatHistoryRequest
2. 46a2e79 (Nov 17 22:06): unit-тесты для моделей
3. **118cee6 (Nov 17 22:56): РЕАЛИЗОВАТЬ getChatHistory()**
4. **726098c (Nov 17 22:06): усилить архитектурный анализ + ADR-001** ← **ПОСЛЕ реализации!**

**Timeline ADR-002:**
1. **7a6e20c (Nov 19 08:08:12): refactor устранить race condition через unified loop**
2. **b7845b6 (Nov 19 08:08:38): добавить ADR-002** ← **26 секунд ПОСЛЕ реализации!**

**Контекст от пользователя:**
1. "ADR писались уже после... не понял пользы написания этих документов"
2. "Лучше иметь подробно описанную проблему, чем снова потом изучать ее"
3. **"По хорошему да, в идеале нужно было это все увидеть на этапе research, проработки архитектуры"**
4. **⚠️ "ADR-002 был создан с неправильным выводом (continuation leak = 'известная проблема')"**
5. **⚠️ "Слишком сложный был рефакторинг, я уже не мог оценить правильность"**
6. **⚠️ "Тяжеловесность для быстрого доступа к контексту Claude CLI"**
7. **⚠️ "Раздуваем контекст → Claude CLI не дотягивается до нужных файлов → постоянно тыкать Read"**
8. "Overnight pause/code review - да надо попробовать"

---

### 🧪 Root Causes (гипотезы):

#### 1. ❌ ADR написаны post-factum как документация, а не как design tool

**Что произошло:**
- ADR-001: написан **ПОСЛЕ реализации** getChatHistory() (50 минут спустя)
- ADR-002: написан **26 секунд ПОСЛЕ** фикса race condition
- ADR = "зафиксировать что уже сделано", НЕ "проанализировать ДО начала"

**Польза:** "Лучше иметь описанную проблему, чем снова изучать" (архивная документация)
**Проблема:** ADR не помог избежать проблем, не помог принять решения

**Идеал (от PO):** "Нужно было это все увидеть на этапе research, проработки архитектуры"

#### 2. ❌ ADR может содержать неправильные выводы (continuation leak = "известная проблема")

**КРИТИЧНО:**
- ADR-002 документирует continuation leak как "⚠️ Известная проблема"
- Реальность: continuation leak = **P0 BLOCKER** (из блока #2)
- Пользователь: "Слишком сложный был рефакторинг, я уже не мог оценить правильность"

**Опасность:**
- ADR вводит в заблуждение (future developer прочитает "не критично" → пропустит BLOCKER)
- Написан post-factum без проверки выводов

**Связь с блоком #2:** Та же недооценка критичности багов

#### 3. ❌ Тяжеловесный формат ADR раздувает контекст → Claude CLI не дотягивается

**КРИТИЧНО (связь с блоком #14):**
- 285 строк ADR занимают место в контексте
- Claude CLI: "Раздуваем контекст → не дотягивается до нужных файлов → постоянно тыкать Read"
- Формат 4 блока (Производительность, Память, Отказоустойчивость, Логирование) = overhead

**Проблема:**
- Для каждого метода писать 100+ строк ADR = контекст раздувается
- Lightweight компоненты не требуют такой детализации
- Нужен lightweight формат

#### 4. ❌ Нет процесса "Architecture First" перед началом реализации

**Проблема:**
- Outside-In TDD начинается сразу с тестов/кода
- Архитектурный анализ (если делается) = пост-фактум

**Доказательство:**
- Timeline: models → tests → implementation → **ADR**
- Правильный порядок: **Research/Spike → ADR → models → tests → implementation**

**Связь с блоками #1, #3:** Та же проблема "нет Spike/Architecture-First"

#### 5. ❌ Нет checklist "когда писать ADR"

**Проблема:**
- Не было правила: "Для каких компонентов нужен ADR ДО реализации?"
- Результат: либо пишем Full ADR для всего (overhead), либо не пишем вообще

**Нужен checklist:**
- Concurrency компонент? → Full ADR обязателен ДО реализации
- External API? → Full ADR обязателен ДО реализации
- Простой helper (<100 строк)? → Lightweight ADR в DocC достаточно
- Тривиальный код (<20 строк)? → ADR не нужен

---

### 💡 Предлагаемые действия (черновик):

#### Действие 6.1: ADR пишется ДО реализации (для critical компонентов)

**Гипотеза:** Если писать ADR ДО начала реализации, то обнаружим проблемы на этапе design.

**Решение:**
- Правило в ARCHITECTURE.md: "ADR = design tool, НЕ документация post-factum"
- Workflow для критичных компонентов:
  1. **Spike/Research (1-2 часа):** изучить external API, constraints
  2. **ADR (15-30 мин):** архитектурный анализ (только для critical!)
  3. **Review ADR:** overnight pause или с другим человеком
  4. **Approve ADR:** решения приняты, выводы проверены
  5. **Outside-In TDD:** models → tests → implementation

**Объединить с действием 1.1 (Architecture-First) и 3.1 (Spike-First)**

**Метрика успеха (черновик):**
- При следующем critical компоненте → ADR написан ДО первого теста?
- 0 случаев "ADR написан после реализации"

---

#### Действие 6.2: Lightweight ADR формат (НЕ 4 блока для простых компонентов)

**Гипотеза:** Если есть lightweight формат, не раздуваем контекст Claude CLI.

**Решение:**
- Добавить в ARCHITECTURE.md два формата ADR:

  **Full ADR (4 блока: Performance, Memory, Failure Resistance, Logging):**
  - Когда: Concurrency, External API, Performance-critical (>1000 ops/sec), >200 строк
  - Где: ARCHITECTURE.md (отдельная секция)
  - Размер: 100-200 строк OK

  **Lightweight ADR (1-2 параграфа в DocC комментарии):**
  - Когда: Простой helper (<100 строк), бизнес-логика БЕЗ concurrency
  - Где: DocC комментарий в коде (НЕ отдельный файл)
  - Формат:
    ```swift
    /// ## Architecture Decision
    ///
    /// **Context:** Почему этот компонент нужен
    /// **Decision:** Ключевое решение (1-2 параграфа)
    /// **Trade-offs:** Альтернативы и почему не выбраны
    ```

**Польза:**
- Меньше раздувает контекст Claude CLI
- Lightweight ADR = часть кода (всегда под рукой)
- Full ADR = только для critical (архивная документация)

**Метрика успеха (черновик):**
- Простые компоненты → Lightweight ADR в DocC
- Критичные компоненты → Full ADR в ARCHITECTURE.md
- Контекст ARCHITECTURE.md не растёт >1000 строк

---

#### Действие 6.3: ADR Decision Checklist + ОБЯЗАТЕЛЬНАЯ верификация выводов

**Гипотеза:** Если есть checklist + проверка выводов, не будет ADR с неправильными conclusions.

**Решение:**
- Checklist в CLAUDE.md / ARCHITECTURE.md:
  ```markdown
  ## ADR Decision Checklist

  Перед началом реализации:

  ✅ Full ADR нужен если хотя бы один "ДА":
  - [ ] Concurrency (actor, lock, async/await)?
  - [ ] External API (C library, network, database)?
  - [ ] Performance-critical (>1000 ops/sec)?
  - [ ] Сложная логика (>200 строк)?

  ✅ Lightweight ADR (DocC) достаточно если:
  - [ ] Простой helper (<100 строк)
  - [ ] Бизнес-логика БЕЗ concurrency

  ❌ ADR НЕ нужен если:
  - [ ] Тривиальный код (<20 строк)
  - [ ] Тест (не требует ADR)
  ```

- **КРИТИЧНО:** Верификация выводов ADR:
  ```markdown
  После написания ADR (перед реализацией):
  1. Проверить severity оценки (continuation leak = P0, не "известная проблема")
  2. Overnight pause или review с другим человеком
  3. Если сомневаешься в выводах → спроси пользователя
  ```

**Метрика успеха (черновик):**
- 0 случаев "ADR содержит неправильную severity оценку"
- 100% Full ADR прошли overnight review перед реализацией

---

#### Действие 6.4: Code Review / Overnight Pause выявляет race conditions

**Гипотеза:** Если делать overnight pause, то увидим race condition (два вызова receive()).

**Решение:**
- Уже предложено в действии 1.3: Overnight Pause
- Расширить Code Review Checklist (действие 2.4):
  - Есть ли два места вызывающие blocking call одновременно?
  - Какие данные shared между threads?
  - Thread-safety доказана? (lock, actor, Sendable)

**Пользователь:** "Да надо обязательно такой формат попробовать"

**Метрика успеха (черновик):**
- При overnight review TDLibClient → race condition был бы обнаружен?
- 0 race conditions пропущены review в следующих 3 компонентах

---

### 📏 Индикаторы успеха (общие для блока #6):

1. **ADR написаны ДО реализации:**
   - Сейчас: 0% (оба ADR post-factum)
   - Цель: 100% critical компонентов

2. **ADR формат соответствует сложности:**
   - Сейчас: Full ADR (4 блока) для всех
   - Цель: Full для critical, Lightweight для простых

3. **ADR выводы верифицированы:**
   - Сейчас: 0% (ADR-002 с неправильной severity оценкой)
   - Цель: 100% (overnight review перед реализацией)

4. **Контекст ARCHITECTURE.md не раздут:**
   - Сейчас: 285 строк ADR (2 компонента)
   - Цель: <1000 строк (lightweight ADR в DocC, Full ADR только critical)

---

## Накопленный список действий (обновлён после блока #6)

### Группа: Architecture & Design
- **1.1** Architecture-First для concurrency/external API компонентов
- **1.5** External API checklist (thread-safety анализ)
- **2.1** Bug Severity Matrix для Swift Concurrency (P0/P1/P2/P3)
- **2.2** Автоматические BLOCKER'ы (continuation leak, double resume, etc.)
- **3.1** Spike/Research фаза для незнакомых external APIs
- **3.4** Hybrid TDD для external APIs (Spike → Inside-Out → Outside-In)
- **5.1** Decision Tree "Actor vs Lock vs OSAllocatedUnfairLock"
- **5.2** Spike Checklist для C interop (blocking? thread-safe? sendable?)
- **6.1** ADR пишется ДО реализации (Spike → ADR → Review → TDD)
- **6.2** Lightweight ADR формат (DocC для простых, Full для critical)
- **6.3** ADR Decision Checklist + верификация выводов (overnight review)

### Группа: Testing & Mocking
- **1.2** Mock boundary явно (FFI, network, filesystem - не high-level)
- **1.4** Thread Sanitizer в CI и локально
- **3.2** E2E Regression тесты для критичных путей (auth, loadChats, getChatHistory)
- **3.3** Запрет на комментирование падающих тестов
- **4.1** Decision Tree "Mock vs Integration vs Fake"
- **4.2** Mock complexity limit: 100 строк
- **4.3** Для простых APIs (OpenAI) → Integration Test или простой Mock (<50 строк)
- **4.4** Ретроспектива решения Mock vs Integration для каждого API
- **5.3** Mock должен использовать ту же concurrency primitive что Real
- **5.4** @unchecked Sendable: SAFETY комментарий + concurrency тест + TSan

### Группа: Code Review & Процессы
- **1.3** Overnight Pause для критичных компонентов
- **2.3** Checklist перед пометкой "не критично" (тест воспроизводящий проблему)
- **2.4** Code Review Checklist для Concurrency кода (race conditions, blocking calls)
- **6.4** Overnight review обязателен для ADR (верификация выводов)

---

## Блок #7: Test Coverage и Concurrency

### 📊 Наблюдения:

**Количественная оценка тестов:**
- **Всего тестов:** 129 (@Test методов)
- **Test файлов:** 34 файла
- **Async/await usage:** 90 строк в тестах
- **Структура:**
  - Unit Tests: 28 файлов (модели, кодирование, TDLibClient, ResponseWaiters)
  - Component Tests: 4 файла (ChannelMessageSource, AuthenticationFlow, MockLogger)
  - E2E Tests: 1 файл (FetchUnreadMessagesScenario)

**Concurrency тесты (найдено 3 теста на параллельные запросы):**
1. `parallelRequestsMatchByExtra()` - 2 параллельных getChat
2. `parallelGetChatRequestsMatchByExtra()` - 100 параллельных getChat
3. `parallelGetMeRequestsRaceCondition()` - 100 параллельных getMe

**Thread Sanitizer:**
- В коде: `--sanitize=thread` упоминается 10 раз в RETRO-ANALYSIS.md (предложения)
- В CI: **НЕ используется** (.github/workflows/linux-build.yml: `swift test --skip TgClientE2ETests`)
- **Вывод:** TSan предложен в блоках #1-5, но **НЕ внедрён**

**Mock в Component тестах:**
- ChannelMessageSourceTests: 4 упоминания MockTDLibFFI
- AuthenticationFlowTests: 5 упоминаний MockTDLibFFI
- **Проблема:** Component тесты используют Mock вместо реального TDLib

**E2E тесты:**
- `FetchUnreadMessagesScenarioTests.swift` - 1 файл
- **В CI:** пропущены (`--skip TgClientE2ETests`)
- **Причина:** требуют credentials

**Контекст от пользователя (из RETROSPECTIVE.md):**
1. "Нет тестов на параллельные запросы (10x getChatHistory одновременно)"
2. "E2E тесты не проверяют race conditions"
3. "Component тесты используют синхронный Mock вместо async реального поведения"
4. Предложения:
   - Concurrency Stress Tests (100 параллельных запросов)
   - Thread Sanitizer в CI
   - E2E тесты должны использовать реальный TDLib

---

### 🧪 Root Causes (гипотезы):

#### 1. ❌ Есть concurrency тесты, но они используют Mock (не реальный TDLib)

**Факты:**
- Есть 3 теста на параллельные запросы (100 параллельных getChat/getMe)
- НО: они используют MockTDLibFFI, а не реальный TDLib
- MockTDLibFFI = синхронный (добавляет response в send(), читает в receive())

**Проблема:**
- Mock не воспроизводит реальное async поведение TDLib
- Race conditions в реальном TDLib могут не проявиться в Mock

**Цитата из PO:**
> "Component тесты используют синхронный Mock вместо async реального поведения"

#### 2. ❌ Thread Sanitizer предложен в блоках #1-5, но НЕ внедрён

**Проблема:**
- TSan упоминается 10 раз в RETRO-ANALYSIS.md (действия 1.4, 2.4, 5.4)
- CI: `swift test` БЕЗ `--sanitize=thread`
- Локально: никто не запускает TSan

**Результат:**
- Race conditions не обнаруживаются автоматически
- Полагаемся на ручной дебаг (как в блоке #1)

#### 3. ❌ E2E тесты пропущены в CI (требуют credentials)

**Проблема:**
- E2E тесты существуют (FetchUnreadMessagesScenarioTests)
- НО: в CI пропущены (`--skip TgClientE2ETests`)
- Причина: требуют реальные Telegram credentials

**Результат:**
- E2E regression suite не автоматизирована
- Cascade changes (блок #3) не обнаруживаются автоматически

#### 4. ❌ Нет Concurrency Stress Tests (1000+ параллельных запросов)

**Факты:**
- Есть `parallelGetChatRequestsMatchByExtra` - 100 параллельных
- НО: это Unit тест на MockTDLibFFI, не stress test на реальном TDLib

**Что нужно:**
- Stress test: 1000+ параллельных getChatHistory() на реальном TDLib
- С TSan для обнаружения race conditions
- С проверкой memory leaks

#### 5. ❌ Component тесты не покрывают async behavior (синхронный Mock)

**Проблема:**
- MockTDLibFFI синхронный: `send()` → сразу добавляет response → `receive()` читает
- Реальный TDLib: `send()` → async обработка → `receive()` через некоторое время

**Пример из кода MockTDLibFFI:**
```swift
// Комментарий: "race condition: MockTDLibFFI добавляет responses СИНХРОННО в send()"
```

**Результат:**
- Component тесты не покрывают async timing issues
- Реальные race conditions пропускаются

---

### 💡 Предлагаемые действия (черновик):

#### Действие 7.1: Включить Thread Sanitizer в CI и локально (реализовать 1.4)

**Гипотеза:** Если включить TSan, то автоматически обнаружим race conditions.

**Решение:**
- **CI (.github/workflows/linux-build.yml):**
  ```yaml
  - name: Run tests with Thread Sanitizer
    run: swift test --sanitize=thread --skip TgClientE2ETests
    timeout-minutes: 10
  ```

- **Локально:** документировать в SETUP.md
  ```bash
  # Concurrency компонент → обязательно TSan
  swift test --sanitize=thread --filter TDLibClientTests
  ```

- **DEVELOPMENT.md:** правило "Concurrency код → TSan обязателен перед коммитом"

**Метрика успеха (черновик):**
- TSan включён в CI?
- TSan обнаружил race conditions в существующем коде?
- 0 race conditions пропущены TSan в следующих 3 компонентах

---

#### Действие 7.2: Concurrency Stress Tests на реальном TDLib (Integration Test)

**Гипотеза:** Если делать stress test на реальном TDLib, то обнаружим race conditions которые Mock пропускает.

**Решение:**
- Создать Integration Test Suite (отдельная директория `Tests/TgClientIntegrationTests/`)
- **Stress Test пример:**
  ```swift
  @Test func tdlibClient_parallelGetChatHistory_1000requests() async throws {
      // Real TDLibClient (не Mock!)
      let client = try TDLibClient()
      try await client.authorize(...)

      // 1000 параллельных запросов
      try await withThrowingTaskGroup(of: [Message].self) { group in
          for chatId in chatIds {  // 1000 chats
              group.addTask {
                  try await client.getChatHistory(chatId: chatId, limit: 10)
              }
          }
      }

      // Если TSan не ругается → OK
      // Если memory leak → FAIL
  }
  ```

- **Запуск:** локально (требует credentials), НЕ в CI

**Метрика успеха (черновик):**
- Integration Test создан?
- Stress test с 1000 параллельными запросами прошёл без TSan warnings?

---

#### Действие 7.3: E2E Regression Suite автоматизирована (реализовать 3.2)

**Гипотеза:** Если автоматизировать E2E тесты, то обнаружим cascade changes.

**Решение:**
- **CI:** использовать test credentials (TG_TEST_PHONE, TG_TEST_CODE из secrets)
- **E2E Suite:**
  - test_auth_phone_code.sh
  - test_load_chats.sh
  - test_get_chat_history.sh
- **GitHub Actions:**
  ```yaml
  - name: Run E2E tests
    run: swift test --filter TgClientE2ETests
    env:
      TG_PHONE: ${{ secrets.TG_TEST_PHONE }}
      TG_CODE: ${{ secrets.TG_TEST_CODE }}
  ```

**Метрика успеха (черновик):**
- E2E тесты запускаются в CI?
- При следующем рефакторинге TDLibClient → E2E suite обнаружил регрессию?

---

#### Действие 7.4: Component тесты должны воспроизводить async timing (или использовать Real TDLib)

**Гипотеза:** Если Mock воспроизводит async behavior, то не пропустим timing issues.

**Решение (вариант 1): Async Mock:**
```swift
class MockTDLibFFI {
    func send(_ request: String) {
        Task {
            try? await Task.sleep(for: .milliseconds(10))  // Simulate async
            lock.lock()
            responses.append(mockedResponse)
            lock.unlock()
        }
    }
}
```

**Решение (вариант 2): Integration Test вместо Component Test:**
- Component тесты (ChannelMessageSourceTests) используют реальный TDLibClient
- Требуют credentials → запускаются только локально

**Метрика успеха (черновик):**
- Mock воспроизводит async delay?
- Или Component тесты переписаны на Integration тесты?

---

### 📏 Индикаторы успеха (общие для блока #7):

1. **Thread Sanitizer включён:**
   - Сейчас: НЕТ (предложен в блоках #1-5, не внедрён)
   - Цель: ДА (CI + локально для concurrency компонентов)

2. **Concurrency Stress Tests:**
   - Сейчас: 100 параллельных (Mock)
   - Цель: 1000+ параллельных (Real TDLib)

3. **E2E Regression Suite:**
   - Сейчас: пропущены в CI
   - Цель: автоматизированы (test credentials)

4. **Component тесты async behavior:**
   - Сейчас: синхронный Mock
   - Цель: async Mock или Integration Test

---

## Накопленный список действий (обновлён после блока #7)

### Группа: Architecture & Design
- **1.1** Architecture-First для concurrency/external API компонентов
- **1.5** External API checklist (thread-safety анализ)
- **2.1** Bug Severity Matrix для Swift Concurrency (P0/P1/P2/P3)
- **2.2** Автоматические BLOCKER'ы (continuation leak, double resume, etc.)
- **3.1** Spike/Research фаза для незнакомых external APIs
- **3.4** Hybrid TDD для external APIs (Spike → Inside-Out → Outside-In)
- **5.1** Decision Tree "Actor vs Lock vs OSAllocatedUnfairLock"
- **5.2** Spike Checklist для C interop (blocking? thread-safe? sendable?)
- **6.1** ADR пишется ДО реализации (Spike → ADR → Review → TDD)
- **6.2** Lightweight ADR формат (DocC для простых, Full для critical)
- **6.3** ADR Decision Checklist + верификация выводов (overnight review)

### Группа: Testing & Mocking
- **1.2** Mock boundary явно (FFI, network, filesystem - не high-level)
- **1.4** Thread Sanitizer в CI и локально **(реализовать → 7.1)**
- **3.2** E2E Regression тесты для критичных путей **(реализовать → 7.3)**
- **3.3** Запрет на комментирование падающих тестов
- **4.1** Decision Tree "Mock vs Integration vs Fake"
- **4.2** Mock complexity limit: 100 строк
- **4.3** Для простых APIs (OpenAI) → Integration Test или простой Mock (<50 строк)
- **4.4** Ретроспектива решения Mock vs Integration для каждого API
- **5.3** Mock должен использовать ту же concurrency primitive что Real
- **5.4** @unchecked Sendable: SAFETY комментарий + concurrency тест + TSan
- **7.1** Thread Sanitizer в CI + локально (реализовать 1.4)
- **7.2** Concurrency Stress Tests на реальном TDLib (1000+ параллельных)
- **7.3** E2E Regression Suite автоматизирована (test credentials в CI)
- **7.4** Component тесты воспроизводят async timing (async Mock или Integration Test)

### Группа: Code Review & Процессы
- **1.3** Overnight Pause для критичных компонентов
- **2.3** Checklist перед пометкой "не критично" (тест воспроизводящий проблему)
- **2.4** Code Review Checklist для Concurrency кода (race conditions, blocking calls)
- **6.4** Overnight review обязателен для ADR (верификация выводов)

---

## Блок #8: Архитектурные решения: когда делать анализ?

### 📊 Наблюдения:

**Конкретный пример:**
- ResponseWaiters: Component тест проходил, но при написании Unit теста обнаружилась проблема с организацией loop (@extra, fire-and-forget)
- Outside-In TDD: высокоуровневый тест ✅ → спускаемся ниже → 💥 concurrency проблема

**Вывод:** Outside-In TDD фокусируется на интерфейсах (input → output), но пропускает internal concurrency. Проблема обнаруживается только на уровне Unit тестов или runtime.

---

### 🔗 Связь с другими блоками:

Блок #8 - это **та же проблема** что в блоках #1, #3, #6, описанная с другого угла:

| Блок | Что покрывает | Действие |
|------|---------------|----------|
| #1 | Architecture-First для concurrency | 1.1 |
| #3 | Исследование ДО реализации для external APIs | 3.1 |
| #6 | ADR ДО реализации, ADR Checklist | 6.1, 6.3 |

**Новых действий не требуется** - покрыто существующими.

---

### 📏 Индикатор успеха:

- При следующем concurrency компоненте → architectural analysis ДО Component теста?
- 0 случаев "Component тест прошёл, Unit тест обнаружил concurrency проблему"

---

_Следующий блок: #9 - Integration vs Unit Testing баланс_

---

## Блок #9: Integration vs Unit Testing баланс

### 📊 Наблюдения:

**Текущее распределение тестов:**
| Тип | @Test | % |
|-----|-------|---|
| Unit | 121 | 94% |
| Component | 6 | 5% |
| E2E | 1 | 1% |

**Распределение Unit тестов:**
- 50% (61) = Codable модели (boilerplate, решится автогенерацией)
- 36% (44) = TDLibAdapter core (реальная логика)
- 13% (16) = FoundationExtensions

**Контекст от пользователя:**
1. Codable тесты = boilerplate, решится с переходом на автогенерацию
2. Edge cases (ошибки, логирование критичных) - нужно поддержать
3. **Подход:** Инцидент → Регрессионный Component тест (не тормозить разработку)

---

### 🧪 Root Cause:

#### ❌ Перекос в Unit тесты + недостаток Component тестов на edge cases

**Проблема:**
- 50% Unit тестов = Codable boilerplate
- Component тесты покрывают happy path, не edge cases
- Нет тестов на: ошибки авторизации, таймауты, критичное логирование

**Решение:**
- Codable boilerplate → автогенерация (в будущем)
- Edge cases → регрессионные тесты после инцидентов

---

### 💡 Предлагаемые действия (черновик):

#### Действие 9.1: Инцидент → Регрессионный Component тест

**Гипотеза:** Если после каждого инцидента писать регрессионный тест, то не повторятся те же ошибки.

**Решение:**
- Правило в DEVELOPMENT.md:
  ```markdown
  ## Регрессионные тесты

  После инцидента (баг в production, критичное не залогировано, ошибка не обработана):
  1. Написать Component тест воспроизводящий инцидент
  2. Убедиться что тест падает БЕЗ фикса
  3. Применить фикс
  4. Убедиться что тест проходит
  5. Коммит: "test(regression): <описание инцидента>"
  ```

**Примеры инцидентов → тестов:**
- "Критичная ошибка не залогирована" → Component тест на логирование ошибок
- "Таймаут getChatHistory не обработан" → Component тест на таймаут
- "Пустой список чатов сломал UI" → Component тест на empty state

**Метрика успеха (черновик):**
- Каждый инцидент имеет регрессионный тест?
- 0 повторных инцидентов того же типа

---

#### Действие 9.2: Codable тесты → автогенерация (низкий приоритет)

**Гипотеза:** Если автогенерировать Codable тесты, освободим время для Component тестов.

**Приоритет:** Низкий (50% Unit тестов = boilerplate, но не блокирует)

---

### 📏 Индикаторы успеха:

1. **Инцидент → регрессионный тест:**
   - Сейчас: нет правила
   - Цель: 100% инцидентов покрыты регрессионными тестами

2. **Повторные инциденты:**
   - Цель: 0 повторений после регрессионного теста

---

## Накопленный список действий (обновлён после блока #9)

### Группа: Architecture & Design
- **1.1** Architecture-First для concurrency/external API компонентов
- **1.5** External API checklist (thread-safety анализ)
- **2.1** Bug Severity Matrix для Swift Concurrency (P0/P1/P2/P3)
- **2.2** Автоматические BLOCKER'ы (continuation leak, double resume, etc.)
- **3.1** Исследование ДО реализации для незнакомых external APIs
- **3.4** Hybrid TDD для external APIs (Исследование → Inside-Out → Outside-In)
- **5.1** Decision Tree "Actor vs Lock vs OSAllocatedUnfairLock"
- **5.2** Checklist для C interop (blocking? thread-safe? sendable?)
- **6.1** ADR пишется ДО реализации (Исследование → ADR → Review → TDD)
- **6.2** Lightweight ADR формат (DocC для простых, Full для critical)
- **6.3** ADR Decision Checklist + верификация выводов (overnight review)

### Группа: Testing & Mocking
- **1.2** Mock boundary явно (FFI, network, filesystem - не high-level)
- **1.4** Thread Sanitizer в CI и локально **(реализовать → 7.1)**
- **3.2** E2E Regression тесты для критичных путей **(реализовать → 7.3)**
- **3.3** Запрет на комментирование падающих тестов
- **4.1** Decision Tree "Mock vs Integration vs Fake"
- **4.2** Mock complexity limit: 100 строк
- **4.3** Для простых APIs (OpenAI) → Integration Test или простой Mock (<50 строк)
- **4.4** Ретроспектива решения Mock vs Integration для каждого API
- **5.3** Mock должен использовать ту же concurrency primitive что Real
- **5.4** @unchecked Sendable: SAFETY комментарий + concurrency тест + TSan
- **7.1** Thread Sanitizer в CI + локально (реализовать 1.4)
- **7.2** Concurrency Stress Tests на реальном TDLib (1000+ параллельных)
- **7.3** E2E Regression Suite автоматизирована (test credentials в CI)
- **7.4** Component тесты воспроизводят async timing (async Mock или Integration Test)
- **9.1** Инцидент → Регрессионный Component тест (обязательно)
- **9.2** Codable тесты → автогенерация (низкий приоритет)

### Группа: Code Review & Процессы
- **1.3** Overnight Pause для критичных компонентов
- **2.3** Checklist перед пометкой "не критично" (тест воспроизводящий проблему)
- **2.4** Code Review Checklist для Concurrency кода (race conditions, blocking calls)
- **6.4** Overnight review обязателен для ADR (верификация выводов)

**Всего: 30 действий** (11 Architecture + 16 Testing + 4 Code Review)

---

_Следующий блок: #10 - Swift 6 Concurrency: Best Practices_

---

## Блок #10: Swift 6 Concurrency: Best Practices

### 📊 Наблюдения:

**Из RETROSPECTIVE.md:**
- Нет единого стиля для concurrency (actor, class + lock, Sendable)
- Путаница между actor isolation и thread safety

---

### 🔗 Связь с другими блоками:

Блок #10 - **полностью покрыт** в блоке #5 (Actor vs NSLock):

| Предложение | Действие |
|-------------|----------|
| Decision Tree "actor vs lock" | 5.1 |
| Checklist для C interop | 5.2 |
| Code Review checklist для concurrency | 2.4 |
| @unchecked Sendable правила | 5.4 |

**Новых действий не требуется.**

---

## Накопленный список действий (без изменений после блока #10)

Остаётся **30 действий**.

---

_Следующий блок: #11 - Позднее обнаружение "точки перехода" от C к Swift Concurrency_

---

## Блок #11: Позднее обнаружение "точки перехода" от C к Swift Concurrency

### 📊 Наблюдения:

**Проблема:**
- Долго жили с `[String: Any]` + `@unchecked Sendable`
- Только на ~20-й сессии пришла идея сделать `[String: Sendable]`

**Вывод:** Проблема не в отсутствии правил, а в том что долго не применили существующие знания.

---

### 🔗 Связь с другими блоками:

Блок #11 - **покрыт** в блоке #5:

| Аспект | Действие |
|--------|----------|
| @unchecked Sendable правила | 5.4 |
| Checklist для C interop | 5.2 |

**Новых действий не требуется.**

---

## Накопленный список действий (без изменений после блока #11)

Остаётся **30 действий**.

---

_Следующий блок: #12 - Узкое мышление при решении проблем_

---

## Блок #12: Узкое мышление при решении проблем

### 📊 Наблюдения:

**Пример 1: Task.yield() упорство**
- Claude дважды предлагал `Task.yield()` как решение
- Пользователь дважды отклонял
- Claude продолжал настаивать вместо того чтобы спросить "почему не подходит?"
- Итог: пользователю пришлось самому предложить альтернативу (AsyncStream + callback)

**Пример 2: @type в Response моделях**
- После 2+ часов дебага нашли: MessagesResponse не включал `@type`
- Claude исправил один файл → "работает? идём дальше!"
- На следующий день — та же проблема в другой Response модели
- **Не было системного анализа:** проблема очевидно была и в других моделях

**Контекст от пользователя:**
1. "Claude упорно склонял к решению, которое было неверное"
2. "Проблема очевидно была и в других моделях тоже"
3. "Нужен системный анализ: есть ли потенциальная проблема в других похожих местах? Нужен ли доп тест?"

---

### 🧪 Root Causes (гипотезы):

#### 1. ❌ Игнорирование feedback пользователя

**Проблема:**
- Пользователь отклонил решение → Claude продолжает настаивать
- Нет вопроса "Почему не подходит? Что не так?"

**Антипаттерн:**
```
Пользователь: "Нет, Task.yield() не подходит"
Claude: "Давай попробуем Task.yield() с другими параметрами"  ← НЕПРАВИЛЬНО
```

**Правильно:**
```
Пользователь: "Нет, Task.yield() не подходит"
Claude: "Понял. Что именно не устраивает? Какие альтернативы рассмотреть?"
```

#### 2. ❌ Отсутствие системного анализа при исправлении бага

**Проблема:**
- Баг найден после долгого дебага → исправлен один файл → идём дальше
- Не задан вопрос: "Это систематическая проблема?"

**Антипаттерн:**
```
Баг в MessagesResponse (@type) → fix → "работает? идём дальше!"
На следующий день: тот же баг в другой Response модели
```

**Правильно:**
```
Баг в MessagesResponse (@type) → СТОП → системный анализ:
1. Есть ли аналогичные места? (grep по Response моделям)
2. Нужна ли автоматическая защита? (assertValidEncoding)
3. Нужен ли регрессионный тест?
```

---

### 💡 Предлагаемые действия (черновик):

#### Действие 12.1: При отклонении решения → спросить "почему?"

**Гипотеза:** Если спрашивать "почему не подходит?", то найдём правильное решение быстрее.

**Решение:**
- Правило в CLAUDE.md:
  ```markdown
  ## При отклонении решения пользователем

  Если пользователь отклонил предложенное решение:
  1. НЕ настаивать на том же решении с вариациями
  2. Спросить: "Что именно не устраивает?"
  3. Предложить 2-3 альтернативных подхода
  4. Если не знаешь альтернатив → честно сказать и спросить пользователя
  ```

**Метрика успеха (черновик):**
- 0 случаев "Claude настаивает после отклонения"

---

#### Действие 12.2: Системный анализ при исправлении бага

**Гипотеза:** Если после исправления бага делать системный анализ, то не пропустим аналогичные проблемы.

**Решение:**
- Правило в DEVELOPMENT.md:
  ```markdown
  ## Системный анализ при исправлении бага

  После исправления бага (особенно найденного после долгого дебага):

  1. **Проверить аналогичные места:**
     - grep/search по похожим паттернам
     - Проверить все модели/компоненты того же типа

  2. **Предложить автоматическую защиту:**
     - Нужен ли assertValidEncoding / validate() / unit test?
     - Можно ли поймать эту ошибку автоматически в будущем?

  3. **Регрессионный тест:**
     - Написать тест воспроизводящий баг (связь с действием 9.1)

  **Вопросы после fix:**
  - "Есть ли потенциальная проблема в других похожих местах?"
  - "Нужен ли доп тест на это?"
  ```

**Метрика успеха (черновик):**
- После каждого бага → системный анализ проведён?
- 0 случаев "тот же баг в другом месте на следующий день"

---

### 📏 Индикаторы успеха:

1. **Игнорирование feedback:**
   - Сейчас: Claude настаивает после отклонения
   - Цель: 0 случаев (спрашивает "почему?")

2. **Системный анализ:**
   - Сейчас: fix одного места → идём дальше
   - Цель: 100% багов → системный анализ (grep, автозащита, тест)

---

## Накопленный список действий (обновлён после блока #12)

### Группа: Architecture & Design
- **1.1** Architecture-First для concurrency/external API компонентов
- **1.5** External API checklist (thread-safety анализ)
- **2.1** Bug Severity Matrix для Swift Concurrency (P0/P1/P2/P3)
- **2.2** Автоматические BLOCKER'ы (continuation leak, double resume, etc.)
- **3.1** Исследование ДО реализации для незнакомых external APIs
- **3.4** Hybrid TDD для external APIs (Исследование → Inside-Out → Outside-In)
- **5.1** Decision Tree "Actor vs Lock vs OSAllocatedUnfairLock"
- **5.2** Checklist для C interop (blocking? thread-safe? sendable?)
- **6.1** ADR пишется ДО реализации (Исследование → ADR → Review → TDD)
- **6.2** Lightweight ADR формат (DocC для простых, Full для critical)
- **6.3** ADR Decision Checklist + верификация выводов (overnight review)

### Группа: Testing & Mocking
- **1.2** Mock boundary явно (FFI, network, filesystem - не high-level)
- **1.4** Thread Sanitizer в CI и локально (реализовать → 7.1)
- **3.2** E2E Regression тесты для критичных путей (реализовать → 7.3)
- **3.3** Запрет на комментирование падающих тестов
- **4.1** Decision Tree "Mock vs Integration vs Fake"
- **4.2** Mock complexity limit: 100 строк
- **4.3** Для простых APIs (OpenAI) → Integration Test или простой Mock (<50 строк)
- **4.4** Ретроспектива решения Mock vs Integration для каждого API
- **5.3** Mock должен использовать ту же concurrency primitive что Real
- **5.4** @unchecked Sendable: SAFETY комментарий + concurrency тест + TSan
- **7.1** Thread Sanitizer в CI + локально (реализовать 1.4)
- **7.2** Concurrency Stress Tests на реальном TDLib (1000+ параллельных)
- **7.3** E2E Regression Suite автоматизирована (test credentials в CI)
- **7.4** Component тесты воспроизводят async timing (async Mock или Integration Test)
- **9.1** Инцидент → Регрессионный Component тест (обязательно)
- **9.2** Codable тесты → автогенерация (низкий приоритет)

### Группа: Code Review & Процессы
- **1.3** Overnight Pause для критичных компонентов
- **2.3** Checklist перед пометкой "не критично"
- **2.4** Code Review Checklist для Concurrency кода
- **6.4** Overnight review обязателен для ADR
- **12.1** При отклонении решения → спросить "почему?" (не настаивать)
- **12.2** Системный анализ при исправлении бага (grep, автозащита, тест)

**Всего: 32 действия** (11 Architecture + 16 Testing + 6 Code Review)

---

_Следующий блок: #13 - Придумывание несуществующих требований (@extra)_
