# Задачи проекта

## 🚀 Инструкции для новой сессии

**При запуске новой сессии:**
1. Прочитай топ-3 приоритета ниже
2. Предложи продолжить работу с фокусом на MVP
3. При необходимости (если задача неясна) — посмотри детали в [MVP.md](.claude/MVP.md)
4. **TDD обязателен**: пиши тесты ДО реализации (см. [TESTING.md](.claude/TESTING.md))

**Перед завершением сессии:**
- Обнови статус задач в этом файле
- **Актуализируй Swift-DocC документацию** (при добавлении новых модулей/компонентов)
- Запиши выполненные задачи в [CHANGELOG.md](.claude/CHANGELOG.md) (только prepend через bash)
- Если нужно — добавь идеи в [BACKLOG.md](.claude/BACKLOG.md)

---

> 🎯 **MVP (цели и scope):** [MVP.md](.claude/MVP.md) — читать по требованию (большой файл)
> 💡 **Будущие фичи:** [BACKLOG.md](.claude/BACKLOG.md) — бэклог для версий после MVP
> 📝 **История изменений:** [CHANGELOG.md](.claude/CHANGELOG.md) — логи завершенных сессий, читать только по требованию (большой файл)
> 📋 **Последнее обновление:** 2025-11-28 (сессия 10)

---

## ⚠️ GIT SAFETY ПРАВИЛА

**Деструктивные операции:**
- **ЗАПРЕЩЕНО** `git reset --hard`, `git clean -fd` БЕЗ явного предупреждения
- Перед деструктивной операцией: показать `git status`, перечислить ЧТО будет потеряно, спросить подтверждение
- Для отката изменений использовать `git stash` вместо `git reset --hard`

**Работа с файлами:**
- **НИКОГДА не используй `cd`** во время git операций (используй absolute paths)
- **Всегда используй `git mv`** для tracked файлов (сохраняет history)
- **Коммить промежуточные состояния** при больших рефакторингах (после каждого логического шага)

**См. детали:** [CLAUDE.md](.claude/CLAUDE.md) → "Правила для ассистента" → "Безопасность работы с git"

---

## 🎯 Текущие задачи

⚠️ **СТАТУС:** Race Condition исправлен (сессия 10), но обнаружен false positive WARNING в логах.
Релиз v0.2.0 отложен до устранения мусорного лога и проверки на Linux.

---

### 🔥 ПРИОРИТЕТ 1: Убрать @extra из fire-and-forget запросов

**Проблема:**
- В логах появляется WARNING: `"no waiter for @extra='XXX'"` для auth flow запросов
- Сейчас `send()` (fire-and-forget) генерирует @extra, TDLib возвращает response, но waiter НЕ нужен
- Это **false positive** — мы специально не ждём response для auth запросов

**Почему это проблема:**
- ❌ **Мусорный лог** — затрудняет debugging (WARNING при норме)
- ❌ **Невозможно отличить** критичную ошибку (потерянный waiter для `sendAndWait()`) от нормы
- ❌ **WARNING для `sendAndWait()` должен быть ERROR** — это deadlock!

**Решение: Убрать генерацию @extra из `send()` (fire-and-forget)**

**Почему это правильно:**
- ✅ TDLib **НЕ требует @extra** (это optional поле для client convenience)
- ✅ Для fire-and-forget @extra не нужен (мы не ждём response)
- ✅ Тогда "no waiter for @extra" **ВСЕГДА** = критическая ошибка (ERROR level)
- ✅ Семантика: только `sendAndWait()` работает с @extra

**Новая архитектура:**
```swift
// Fire-and-forget (auth flow) - БЕЗ @extra
send(CheckDatabaseEncryptionKeyRequest())
  → encode WITHOUT @extra
  → ffi.send(json)
  → TDLib возвращает response БЕЗ @extra (OK)

// Request-Response - С @extra
sendAndWait(GetMeRequest())
  → generateExtra() → "123"
  → addWaiter(forExtra: "123")
  → encode WITH @extra
  → ffi.send(json)
  → TDLib возвращает response С @extra="123"
  → waiter получает response
```

**Детальный план реализации:**

**Шаг 1: Исследование**
- [ ] Проверить TDLib docs: @extra действительно optional?
- [ ] Найти все места использования `send()` (fire-and-forget) vs `sendAndWait()`
- [ ] Определить какие запросы идут через send() (auth flow)

**Шаг 2: MockTDLibFFI — опциональный @extra**
- [ ] Изменить `MockTDLibFFI.send()`: сделать @extra **optional** при парсинге
  ```swift
  let extra = parsed["@extra"] as? String  // Optional!
  if let extra = extra {
      // Сохраняем response с @extra (для sendAndWait)
      responsesByExtra[extra] = jsonString
  } else {
      // Fire-and-forget: response игнорируем (или логируем debug)
  }
  ```
- [ ] Обновить тесты если нужно

**Шаг 3: TDLibClient.send() — БЕЗ @extra**
- [ ] Убрать `generateExtra()` из `send()`
- [ ] Использовать `encoder.encode(request)` (без withExtra)
  ```swift
  func send(_ request: TDLibRequest) {
      let encoder = TDLibRequestEncoder()
      guard let data = try? encoder.encode(request) else {  // БЕЗ withExtra!
          fatalError("TDLibClient.send(): encoder failed for \(request.type)")
      }
      let json = String(decoding: data, as: UTF8.self)
      ffi.send(json)
  }
  ```

**Шаг 4: Обновить логирование в startUpdatesLoop()**
- [ ] Изменить WARNING "no waiter for @extra" на **ERROR** уровень
- [ ] Добавить комментарий: "КРИТИЧНО: deadlock detected - waiter потерян!"
- [ ] Для responses БЕЗ @extra (от send) — не логировать вообще (или DEBUG)

**Шаг 5: Написать Unit тест**
- [ ] Тест: send() отправляет запрос БЕЗ @extra
- [ ] Тест: sendAndWait() отправляет запрос С @extra
- [ ] Тест: MockFFI корректно обрабатывает оба случая

**Шаг 6: Проверка**
- [ ] Запустить все Unit тесты: `swift test 2>&1`
- [ ] Запустить E2E тесты (из Xcode)
- [ ] Запустить боевой клиент на macOS: `(set -a; source .env; swift run tg-client)`
  - **Ожидаем:** НЕТ WARNING "no waiter for @extra" для auth flow
- [ ] Проверка на Linux VPS

**Шаг 7: Коммит**
- [ ] Коммит (fix): "fix(TDLibClient): убрать @extra из fire-and-forget запросов"
  - Убрана генерация @extra из send()
  - MockTDLibFFI поддерживает optional @extra
  - WARNING → ERROR для потерянных waiters
  - Добавлены Unit тесты

**Критичные проверки:**
- ✅ 127+ тестов GREEN
- ✅ НЕТ WARNING "no waiter" в логах auth flow
- ✅ Боевой клиент работает на macOS
- ✅ Боевой клиент работает на Linux

---

### 🔥 ПРИОРИТЕТ 2: Создать релиз v0.2.0

**После исправления ПРИОРИТЕТ 1:**
- [ ] Все Unit/Component тесты GREEN (127+ тестов)
- [ ] Клиент работает на Linux VPS (доходит до конца main.swift)
- [ ] **Сгенерировать DoCC документацию** (после всех тестов и изменений)
  ```bash
  ./scripts/generate-docc-from-tests.sh
  ```
- [ ] Создать 4 коммита:
  1. `refactor(TDLibFFI): переместить генерацию @extra в TDLibClient`
  2. `feat(TDLibClient): реализовать sendAndWait() для устранения Race Condition`
  3. `test: добавить regression тест для Race Condition (100 параллельных getMe)`
  4. `fix(TDLibClient): убрать @extra из fire-and-forget запросов`
  5. `docs: актуализировать DoCC документацию для v0.2.0` (опционально, если были изменения)
- [ ] Создать git tag v0.2.0
- [ ] Push tag на remote
- [ ] Создать GitHub Release с описанием изменений

---

### 📋 MVP доработки (backlog)

_Пока нет задач_

---

## ✅ Недавно завершено

- **2025-11-28 (сессия 10):** ✅ Race Condition исправлен! Архитектурный рефакторинг: генерация @extra перемещена из FFI в TDLibClient, реализован метод `sendAndWait()` (атомарная операция: generateExtra → addWaiter → send), переписано высокоуровневое API (getMe/loadChats/getChat/getChatHistory). 127 тестов GREEN, E2E GREEN (Xcode), боевой клиент работает на macOS (453 чата, 1 сообщение). ⚠️ Обнаружен false positive WARNING "no waiter for @extra" для auth flow (fire-and-forget запросы) - требует исправления в следующей сессии.
- **2025-11-28 (сессия 9):** 4 коммита запушены (refactor getChats, docs cleanup, внутренняя документация, TDLib docs ссылки в тестах). 128 тестов GREEN на macOS (122 Unit + 6 Component). Код работает на Linux (Device model: Linux, авторизация успешна). ⚠️ Обнаружен критический баг: Race Condition в ResponseWaiters - клиент зависает на getMe() (addWaiter() через Task ПОСЛЕ send()). Релиз v0.2.0 отложен до исправления.
- **2025-11-28 (сессия 8):** Удален неиспользуемый getChats/GetChatsRequest/ChatsResponse (через git rm -f). ChatList enum перемещен в LoadChatsRequest.swift. Убраны все ссылки на .claude/*.md из комментариев кода. Исправлены битые ссылки TDLib docs (.claude/ → docs/). Документация обновлена (примеры с getChats → loadChats/getChatHistory). DoCC регенерирована. 128 тестов GREEN (122 Unit + 6 Component). Клиент работает в production (566 чатов, 23 с непрочитанными, 3 сообщения).
- **2025-11-28 (сессия 7):** DoCC документация актуализирована для v0.2.0 (раздел "Аудит безопасности", убраны упоминания нереализованного функционала, проверка линковки E2E → Component → Unit тестов, линковка хелперов через обновлённый скрипт)
- **2025-11-28 (сессия 6):** deviceModel кросс-платформенность (macOS/Linux через #if), E2E условная компиляция (#if ENABLE_E2E_TESTS), документация обновлена (TESTING.md + TASKS.md)
- **2025-11-28 (сессия 5):** E2E тест починен, TDLib логирование настроено
- **2025-11-28 (сессии 1-4):** @extra matching, assertValidEncoding(), инциденты задокументированы

---

## 📚 Документация

**Основные документы:**
- [ARCHITECTURE.md](.claude/ARCHITECTURE.md) - архитектура проекта
- [DEVELOPMENT.md](.claude/DEVELOPMENT.md) - правила разработки
- [TESTING.md](.claude/TESTING.md) - стратегия тестирования
- [PROMPTS.md](.claude/PROMPTS.md) - роли для разных типов задач

**Инфраструктура (по требованию):**
- [SETUP.md](.claude/SETUP.md) - настройка окружения
- [DEPLOY.md](.claude/DEPLOY.md) - деплой на Linux VPS
- [CREDENTIALS.md](.claude/CREDENTIALS.md) - управление секретами
- [TROUBLESHOOTING.md](.claude/TROUBLESHOOTING.md) - частые проблемы

---

**Последнее обновление:** 2025-11-28 (сессия 7)
**Завершённые задачи:** См. [CHANGELOG.md](.claude/CHANGELOG.md)
