# v0.6.0 VK Bridge TDD Migration RFC

> **Статус:** Draft (части 1–2 заполнены, 3–10 — TODO)
> **Дата:** 2026-05-09
> **Автор:** Sergey + Claude
> **Целевая версия:** v0.6.0 (после v0.5.0 BotNotifier)
> **Целевая ветка:** `feature/vk-bridge-tdd` (от `origin/main`)
> **Spike reference:** `feature/vk-bot-bridge` @ tag `spike/vk-bridge-2026-05-09`

---

## 🎯 Контекст и цель

В spike `feature/vk-bot-bridge` мы за одну итерацию (2026-05-06) построили работающий **VK Bot Bridge** — альтернативный канал команд через VK сообщество, когда мобильный интернет блокирует Telegram. Spike **успешно крутится в проде на VPS уже несколько недель** под личной нагрузкой автора.

Параллельно в `origin/main` появились:
- v0.4.0 (mark-as-read, релиз)
- v0.5.0 (in progress): `BotNotifierProtocol` + `TelegramBotNotifier` с **полным набором Unit + Component + E2E тестов**

**Цель этого RFC:** **повторно реализовать VK Bridge поверх `BotNotifierProtocol` с полным TDD циклом**, используя spike как research artifact / источник реальных требований и боевых багов.

**Не цель:** портировать spike-код "как есть" с дописыванием тестов сверху. TDD = тесты ведут дизайн, не наоборот.

---

## 📋 Раздел 1 — Spike Findings (что узнали в проде)

> Это ключевой раздел. Без него миграция превратится в "переписать то же самое". Эти находки = **реальные требования**, найденные на эксплуатации, а не в кабинете.

### 1.1 Что подтвердилось рабочим (оставляем в архитектуре v2)

| # | Находка | Решение из spike | Сохранить? |
|---|---|---|---|
| F-01 | **VK Callback API webhook** работает | webhook через nginx → :8082 | ❌ **пересмотрено** в v2 — переход на VK **Long Poll** (см. 1.5) |
| F-02 | **Hummingbird 2.x** на Linux работает без сюрпризов | `from: "2.0.0"` в Package.swift | ❌ **пересмотрено** — в long-poll архитектуре не нужен (см. 1.5) |
| F-03 | **Плоский plain-text raw mode** (без AI) — то что нужно "в дороге" | чанки ≤ 4096 chars | ✅ да |
| F-04 | **Service mode** (long-running) обязателен — cold start TDLib 30–60 сек | CLI flag `tg-client service` | ✅ да |
| F-05 | **Whitelist by from_id** — достаточная защита для personal use | env: `VK_BOT_OWNER_IDS` | ✅ да (без `VK_CALLBACK_SECRET` — в long-poll нет публичного endpoint) |
| F-06 | **Меню непрочитанных** + `/get N` (а не "сразу всё") — лучший UX | `/digest` → `/get N` | ✅ да |
| F-07 | **Iterative pagination для `/last`** — TDLib возвращает messages chronologically backward | многошаговый цикл getChatHistory | ✅ да |
| F-08 | **`/to <username>` через `searchPublicChat`** — неожиданно полезная фича | сохранить как story | ✅ да |
| F-09 | **Async обработка событий** | spike делал webhook-immediate-ok + Task; в long-poll: Task per-update | ✅ да (адаптируется) |

### 1.2 Что лажало или болело в проде (фиксим в v2)

| # | Симптом | Корневая причина | Что делаем в v2 |
|---|---|---|---|
| B-01 | **TDLib SEGV** после ~24h uptime раз в несколько дней | Continuation leak (known issue, см. CLAUDE.md) | Smoke E2E + retry/restart logic + ResponseWaiters паттерн (как в main) |
| B-02 | **Один раз забыл `VK_CALLBACK_SECRET`** → endpoint принимал случайные POST | Нет required-проверки конфига при старте | Config validation на старте, fail-fast если secret пустой |
| B-03 | **TG entities игнорируются** (bold/italic/ссылки) — пользователь ощутимо теряет контекст | RawDigestFormatter сбрасывает entities | E2E тест: проверка что bold/italic/link конвертируются (unicode bold + `[text](url)` plain) |
| B-04 | **Медиа без caption** = "пустые" сообщения, юзер не понимает что это | MessageContent: некоторые типы не маркируются | Маркеры `[🖼️ photo]`, `[🎵 audio]`, `[🎥 video]`, `[📎 file]`, `[📍 location]` |
| B-05 | **Truncate ломал Cyrillic char пополам** (не точно подтверждено, наблюдалось 1-2 раза) | Резка по байтам, не по grapheme cluster | Тест на длинной кириллической строке + `String.prefix(_:)` через `Character` |
| B-06 | **markaread не автоматический после `/get N`** — UX-боль, пользователь должен помнить | Spike сделан минимально | Опция `--auto-mark` в `/get`; либо `/get N read` суффикс |
| B-07 | **Service mode требует interactive auth первый раз** | Один баг при clean deploy | Подкоманда `tg-client auth` для одноразовой авторизации; либо чёткий runbook в DEPLOY.md |
| B-08 | **Concurrent guard работает только на одном owner** | actor mutex per-process, но если whitelist расширим — появятся race | Per-owner mutex или единый serial executor на CommandProcessor |
| B-09 | **Логов не хватало для дебага** иногда (когда команда "тихо ничего не сделала") | Логи только на error, info-уровень не везде | Audit trail: каждая команда → один INFO лог `{owner_id, cmd, decision, duration_ms}` |

### 1.3 Что НЕ делать (учли по spike)

| # | Соблазн | Почему отбрасываем |
|---|---|---|
| N-01 | "Длинные TDD циклы тормозят, опять сделаем без тестов" | Этот RFC и есть формальный отказ от такого подхода. Spike доказал value, теперь TDD |
| N-02 | "Очередь команд (FIFO) для concurrent requests" | Spike показал: "Уже обрабатываю, подожди" достаточно для personal use, очередь — overengineering |
| ~~N-03~~ | ~~"Long Poll fallback"~~ | **Пересмотрено в v2** — Long Poll становится primary архитектурой (см. 1.5) |
| N-04 | "Multi-tenant (несколько owner-сообществ)" | Spike — один owner, и в обозримом будущем тоже. Удерживаем YAGNI |
| N-05 | "AI саммари в VK ответе по умолчанию" | Spike показал что raw — это то что нужно "в дороге" (без AI задержки + затрат). Опционально через флаг — позже |
| N-06 | "Прометеус метрики" | Audit log в syslog достаточен, метрики добавим когда станут нужны |

### 1.5 Architectural pivot — webhook → long-poll (2026-05-10)

**Изначально в spike** мы выбрали webhook потому что:
- На сервере уже был nginx + cert (cashflow-game.ru)
- Webhook latency ~100ms vs long-poll ~1-3 сек

**При планировании v0.6.0 пересмотрели:**

| Аспект | Webhook (spike) | Long Poll (v0.6.0) | Кто выигрывает |
|---|---|---|---|
| Зависимости | +5 транзитивных (Hummingbird, swift-nio, ...) | 0 новых (URLSession уже есть) | 🏆 long-poll |
| Build time на Linux | +20–40 сек | без изменений | 🏆 long-poll |
| nginx config | нужен `/vkWebHook` location | не нужен | 🏆 long-poll |
| HTTPS-домен | требуется | **не требуется** | 🏆 long-poll (open-source friendliness) |
| Secret check | нужен (B-02 был именно про забытый secret) | не нужен (нет публичного endpoint) | 🏆 long-poll |
| Latency команда→ответ | ~100ms | 1–3 сек | webhook |
| Idle traffic | 0 | ~5 KB/мин | webhook |
| Spike проверял в проде | ✅ | ❌ нужен mini-spike Phase 1 | webhook |
| TDD friendliness | требует Hummingbird testing helpers | один `MockHTTPClient` для всего | 🏆 long-poll |

**Вывод:** для personal use 1–3 сек latency незаметна (вся команда `/digest` идёт 5–10 сек). Идл-трафик 5 KB/мин ничтожен. Все остальные пункты — в пользу long-poll. **Решено перейти на long-poll** в v0.6.0.

Spike (webhook+Hummingbird) остаётся как research artifact + production fallback до полной TDD-миграции.

### 1.4 Сравнение spike vs origin/main (overlap с TelegramBotNotifier)

| Аспект | Spike (BotBridge таргет) | origin/main (DigestCore/Notifiers) |
|---|---|---|
| **Куда живёт** | Отдельный таргет `BotBridge` | Внутри `DigestCore` через `BotNotifierProtocol` |
| **Транспорт** | VK Callback API → VK API | Telegram Bot API |
| **Назначение** | Двусторонний (input cmd + output reply) | Односторонний (notify пользователя дайджестом) |
| **Тесты** | ❌ нет | ✅ Unit + Component + E2E |
| **Конфигурация** | env через `BotBridgeConfig` | env через DigestCore конфиги |
| **Зависимости** | + Hummingbird (5–6 каскадных) | Только URLSession |

**Вывод по архитектуре v2:** `BotNotifierProtocol` из main — это **выходной канал** (digest → user). VK Bridge — это **двусторонний шлюз** (user → cmd → digest → user). Поэтому архитектурно они **дополняют друг друга**, а не конфликтуют:

```
VK user → VK servers
              │ events (long-poll)
              ▼
VKLongPollClient.run() (новый актор-цикл)
              │
              ▼
VKEventDispatcher → CommandRouter (новый, actor)
              │
              ├─ читает: ChannelMessageSource (existing)
              ├─ пишет:  VKBotNotifier: BotNotifierProtocol (новый, поверх протокола из main)
              │           ↳ под капотом VKAPIClient → URLSession
              └─ markaread: TDLib viewMessages (existing)
```

Получается **VKBotNotifier — это просто ещё одна реализация уже существующего `BotNotifierProtocol`**, как `TelegramBotNotifier`. Механика входа (LongPollClient + EventDispatcher + CommandRouter) — отдельный таргет `VKBridge`.

---

## 📋 Раздел 2 — User Stories с Acceptance Criteria

> Семь stories. Каждая = одна полная итерация Outside-In TDD (E2E → Component → Unit → GREEN → REFACTOR). Приоритет = порядок реализации.

### Формат
- **Story:** As [роль], когда [условие], я хочу [результат], чтобы [цель]
- **AC (Given/When/Then):** acceptance criteria для приёмки
- **TDD entry point:** какой тест писать первым

---

### US-1: Health check / Ping (P0, первая по TDD)

**Story:** Как владелец сервиса, когда я отправляю `/ping` в VK сообщество, я хочу мгновенно получить статус бота, чтобы убедиться что сервис жив и TDLib подключён.

**AC:**
- **Given** сервис запущен и TDLib авторизован
  **When** owner шлёт `/ping`
  **Then** в течение 2 сек получает ответ `pong (tdlib: connected, uptime: 3h12m)`

- **Given** сервис запущен но TDLib потерял соединение
  **When** owner шлёт `/ping`
  **Then** получает `pong (tdlib: disconnected, last_error: ...)` БЕЗ зависания обработчика

- **Given** не-owner шлёт `/ping`
  **When** webhook принимает событие
  **Then** ответ "ok" в VK без отправки сообщения, в логах `decision=deny from_id=X`

**TDD entry point:** E2E тест с mock TDLib + mock VKAPIClient — полный путь webhook→handler→VKBotNotifier.

**Почему первая:** самая простая story, помогает обкатать всю инфраструктуру (webhook парсинг, whitelist, reply path, audit log) без бизнес-логики.

---

### US-2: Дайджест непрочитанных — меню (P0)

**Story:** Как владелец, когда мобильный интернет блокирует Telegram, я хочу командой `/digest` получить **меню** всех непрочитанных чатов (с unreadCount > 0), сгруппированное по типу (каналы / группы / ЛС), чтобы выбрать что читать.

**AC:**
- **Given** есть 3 канала, 2 группы, 1 ЛС с непрочитанными
  **When** owner шлёт `/digest`
  **Then** получает меню вида:
  ```
  📺 Каналы (3):
   1. Channel A — 5 unread
   2. Channel B — 12 unread
   3. Channel C — 1 unread
  👥 Группы (2):
   4. Group X — 3 unread
   5. Group Y — 7 unread
  💬 Личные (1):
   6. @alice — 1 unread
  
  Команды: /get N, /get all, /get channels|groups|dm, /last N [count]
  ```

- **Given** нет непрочитанных вообще
  **When** owner шлёт `/digest`
  **Then** получает `Нет непрочитанных сообщений ✅`

- **Given** есть >50 непрочитанных чатов
  **When** owner шлёт `/digest`
  **Then** меню режется на чанки ≤ 4096 char без потери нумерации между чанками

- **Given** TDLib недоступен (коннект упал)
  **When** owner шлёт `/digest`
  **Then** получает `Ошибка: TDLib недоступен. Проверь логи.` БЕЗ 5xx ответа в VK webhook

- **Given** owner в blacklist архивных папок
  **When** `/digest`
  **Then** архивные чаты в меню НЕ попадают (как и в существующем `fetchUnreadMessages`)

**TDD entry point:** E2E тест с mock TDLib возвращающим 6 чатов, проверка что меню сгенерировано и отправлено в VK правильным форматом.

---

### US-3: Загрузить чат N из меню (P0)

**Story:** Как владелец, после `/digest` я хочу командой `/get N` получить **сырой текст** всех непрочитанных сообщений выбранного чата, чтобы прочитать их в VK.

**AC:**
- **Given** меню `/digest` отправлено, в текущей session_state хранится список чатов
  **When** owner шлёт `/get 3`
  **Then** получает сообщения чата #3 в формате:
  ```
  📺 Channel C
  
  [12:34] Message 1 text...
  [12:45] Message 2 text...
  ```
  с правильным сохранением **bold/italic/links** (unicode + plain `[text](url)`).

- **Given** меню не было отправлено в текущей сессии
  **When** owner шлёт `/get 3`
  **Then** получает `Сначала вызови /digest, чтобы построить меню.`

- **Given** N выходит за пределы меню
  **When** `/get 99`
  **Then** получает `В меню только 6 чатов. Выбери 1–6 или /digest заново.`

- **Given** chat #3 содержит фото без подписи
  **When** `/get 3`
  **Then** в выводе появляется `[🖼️ photo]` (маркер), а не пустое место

- **Given** owner шлёт `/get all`
  **When** обработка
  **Then** все чаты из меню по очереди, разделённые `--- N. <name> ---`

- **Given** owner шлёт `/get channels` (или `groups` / `dm`)
  **When** обработка
  **Then** только чаты этой категории

- **Given** owner шлёт `/get 3 read`
  **When** обработка
  **Then** после доставки чат помечается как прочитанный через TDLib `viewMessages` (опционально — закрывает B-06)

**TDD entry point:** Component тест на форматтере (с mock messages) → E2E тест на полном пути.

---

### US-4: История сообщений чата (P1)

**Story:** Как владелец, я хочу командой `/last N [count]` получить **последние count сообщений** чата #N (включая прочитанные), чтобы поднять контекст по диалогу из VK.

**AC:**
- **Given** меню есть, count не указан
  **When** owner шлёт `/last 3`
  **Then** получает 20 последних сообщений (default)

- **Given** count = 100
  **When** `/last 3 100`
  **Then** получает 100 сообщений (возможно через несколько TDLib итераций — getChatHistory отдаёт батчами по 100)

- **Given** count > 500
  **When** `/last 3 1000`
  **Then** получает `count ограничен 500. Используй /last N 500.`

- **Given** в чате <count сообщений
  **When** `/last 3 1000`
  **Then** возвращает все доступные + хвостовая строка `(ещё нет данных)`

**TDD entry point:** Component тест на iterative pagination logic с mock TDLib batches.

---

### US-5: Ответить в чат через VK (P1)

**Story:** Как владелец, я хочу командой `/reply N <текст>` отправить сообщение в Telegram-чат #N от своего имени, чтобы участвовать в диалоге не открывая Telegram.

**AC:**
- **Given** меню есть, чат #3 — ЛС с @alice
  **When** owner шлёт `/reply 3 Привет!`
  **Then** в TG отправляется "Привет!" в чат с @alice через TDLib `sendMessage`,
  в VK owner получает `✅ Отправлено в Channel/Group/DM "Channel C"`

- **Given** текст содержит спецсимволы или эмодзи
  **When** `/reply 3 Йоу 🚀 #тест @bob`
  **Then** TDLib отправляет как plain text без интерпретации markdown

- **Given** sendMessage упал (rate limit, чат удалён)
  **When** `/reply 3 ...`
  **Then** owner получает `❌ Не удалось отправить: <причина>` с error из TDLib

**TDD entry point:** E2E тест с mock TDLib `sendMessage` (success + failure paths).

---

### US-6: Сообщение по @username (P2)

**Story:** Как владелец, я хочу командой `/to <username> <текст>` отправить сообщение **любому** пользователю Telegram по @username, даже если у меня с ним нет существующего чата.

**AC:**
- **Given** @bob существует и принимает сообщения
  **When** owner шлёт `/to bob Здарова`
  **Then** через `searchPublicChat` находит chat_id и шлёт через `sendMessage`,
  в VK получает `✅ Отправлено @bob`

- **Given** @nonexistent не существует
  **When** `/to nonexistent привет`
  **Then** owner получает `❌ Пользователь @nonexistent не найден`

- **Given** @bob закрыл сообщения от незнакомцев
  **When** `/to bob привет`
  **Then** owner получает `❌ @bob запретил сообщения от незнакомцев` (TDLib error)

- **Given** username с/без @ префикса
  **When** `/to @bob привет` или `/to bob привет`
  **Then** одинаково работают (нормализация)

**TDD entry point:** Component тест на нормализацию username + E2E с mock searchPublicChat.

---

### US-7: Отметить чат как прочитанный (P2)

**Story:** Как владелец, я хочу командой `/read N` пометить чат #N как прочитанный (без открытия мессенджера), чтобы в `/digest` он больше не появлялся.

**AC:**
- **Given** меню есть, чат #3 имеет 5 непрочитанных
  **When** owner шлёт `/read 3`
  **Then** через TDLib `viewMessages` помечает все непрочитанные как viewed,
  в VK получает `✅ Channel C помечен прочитанным (5 сообщений)`

- **Given** чат уже без непрочитанных
  **When** `/read 3`
  **Then** owner получает `✅ Channel C уже прочитан`

- **Given** TDLib недоступен
  **When** `/read 3`
  **Then** owner получает `❌ TDLib недоступен`

**TDD entry point:** E2E с mock TDLib viewMessages.

---

### Сводная таблица приоритетов

| # | Story | Priority | TDD entry | Phase |
|---|---|---|---|---|
| US-1 | /ping | P0 | E2E full path | Phase 1 (infra) |
| US-2 | /digest menu | P0 | E2E + Component | Phase 2 |
| US-3 | /get N | P0 | Component formatter + E2E | Phase 2 |
| US-4 | /last N count | P1 | Component pagination | Phase 3 |
| US-5 | /reply N text | P1 | E2E send | Phase 3 |
| US-6 | /to username | P2 | Component normalize | Phase 3 |
| US-7 | /read N | P2 | E2E viewMessages | Phase 3 |

---

## 📋 Раздел 3 — Test Matrix

> Для каждой US: какие тесты пишем (E2E / Component / Unit) и где **главный фокус покрытия**. Шкала: 🎯 = основной (driver story) / ⚙️ = вспомогательный / — = не нужен.

### 3.1 Стратегия моков

| Граница | Mock? | Чем |
|---|---|---|
| **TDLib** (TDLibClientProtocol) | ✅ всегда | `MockTDLibClient` (in-memory) — следуем паттерну main `TelegramBotNotifierTests` |
| **VK API outbound** (sendMessage) | ✅ всегда | `MockHTTPClient` (URLSession-protocol-mocked, как в main) |
| **VK Long Poll inbound** (long-poll responses) | ✅ всегда | Тот же `MockHTTPClient` — VKLongPollClient ходит в `groups.getLongPollServer` + `<server>` через тот же `HTTPClientProtocol`. Скриптуем последовательность ответов (failed=1/2/3, updates batch, network error) |
| **Time / random_id** | ✅ для unit | инжектим `() -> Date` и `() -> UInt64` через config |
| **Backoff sleep** | ✅ для unit | `BackoffPolicy` принимает `Clock` (Swift Concurrency Clock) — в тестах `TestClock` ускоряет до мгновенного |
| **Logger** | ❌ | `Logger(label: "test")`, реальный, проверяем через `LogCollector` где это часть AC (audit trail US-1, B-09) |
| **Env** | ✅ | `VKBridgeConfig.init(env:)` — pure |

**Большое преимущество long-poll:** **один тип мока** (`MockHTTPClient`) покрывает и входящий поток событий, и исходящий поток ответов. В webhook-архитектуре нужны были две разные моковые границы (Hummingbird + URLSession).

### 3.2 Покрытие per-story

| US | E2E | Component | Unit | Главный focus |
|---|---|---|---|---|
| **US-1 /ping** | 🎯 full long-poll→event→reply path с mock TDLib + mock VK API. **Включает** VKLongPollClient lifecycle (fetchSession + pollOnce + handle) | ⚙️ WhitelistFilter, AuditLogger, VKEventDispatcher (filter и роутинг events) | ⚙️ VKModels (message_new), VKBridgeConfig (env validation, fail-fast), VKLongPollSession parsing | E2E: 1 happy + 1 deny + 1 TDLib disconnected = 3 теста |
| **US-2 /digest menu** | 🎯 menu generation + chunking | 🎯 MenuFormatter (по типу чата), ChatGrouper | ⚙️ ChunkSplitter (4096 char без потери нумерации) | Component: пустое меню / 50+ чатов / архивные исключаются |
| **US-3 /get N** | 🎯 N→messages→formatted→sent | 🎯 RawDigestFormatter (entities, media markers), SessionState (в spike — `BotSessionState`, переосмысливаем) | 🎯 EntityConverter (bold→𝐛𝐨𝐥𝐝, italic→𝑖𝑡𝑎𝑙𝑖𝑐, link→`text (url)`), MediaMarker, GraphemeSafeTruncate (B-05) | Unit: 30+ entity/media сценариев. Самая богатая на unit story |
| **US-4 /last N count** | ⚙️ smoke (1 сценарий) | 🎯 IterativePaginator (TDLib batch=100, total=count, ranges) | ⚙️ count cap validation | Component: <count messages / exact count / count > available |
| **US-5 /reply N text** | 🎯 send happy + send fail | ⚙️ SessionState lookup | ⚙️ — | E2E: success + TDLib rate-limit + chat deleted |
| **US-6 /to username** | ⚙️ smoke (1 happy) | 🎯 UsernameNormalizer + searchPublicChat handler | ⚙️ Normalizer (`@bob`, `bob`, `@@bob`, `Bob`) | Component: not_found / closed_dm / success |
| **US-7 /read N** | ⚙️ smoke (1 happy) | ⚙️ ReadMarker | ⚙️ — | E2E: marked / already read / TDLib unavailable |

### 3.3 Интеграционные / cross-cutting тесты

| Тест | Тип | Что проверяет |
|---|---|---|
| **VK required env missing** (token / group_id / owner_ids) | E2E (config) | приложение НЕ стартует, exit code != 0, лог-сообщение (закрывает B-02) |
| **Long-poll `failed: 1`** | Component | дошёл новый `ts`, `session` сохраняется, цикл продолжается без повторного `getLongPollServer` |
| **Long-poll `failed: 2`** | Component | вызывается заново `getLongPollServer`, `key` обновляется, цикл продолжается |
| **Long-poll `failed: 3`** | Component | то же что `failed: 2` |
| **Long-poll network error → backoff** | Component | exponential delay: 1→2→4→8 сек (через `TestClock`), потом retry |
| **Long-poll 10+ ошибок подряд** | Component | лог уровень `error`, цикл всё равно продолжается (не падаем) |
| **Long-poll graceful shutdown** | Component | `client.stop()` → текущий poll допрабатывается до 30 сек → loop exit без exception |
| **Concurrent /digest** (два события от owner за 1 сек) | E2E | второе получает "Уже обрабатываю, подожди" БЕЗ запуска второго fetchUnreadChats |
| **Audit log** для каждой команды | E2E | один INFO лог `cmd=X owner=Y decision=Z duration_ms=N` (закрывает B-09) |
| **Cyrillic + emoji в truncate** | Unit | `String.prefix(_:)` через grapheme cluster (закрывает B-05) |
| **Не-owner — silent ignore** | E2E | **никаких** исходящих VK API вызовов, лог `decision=deny` |

### 3.4 Что НЕ покрываем тестами (явно)

| Что | Почему |
|---|---|
| Реальный VK API (live API ключ) | Spike доказал что API работает; CI не должен зависеть от внешних |
| Реальный TDLib (без mock) | E2E с реальным TDLib в main уже есть, дубль не нужен; для нового кода mock достаточен |
| Производительность под нагрузкой | Personal use, ~10 команд/день, не нужно |
| HTTPS termination (nginx) | Системное, тестим вручную smoke на сервере |
| systemd lifecycle | Не покрываем кодом, только runbook'ом |

---

## 📋 Раздел 4 — Reuse Map (что из spike → как)

> Каждый артефакт spike → одно из: **COPY** (можно перенести с минимальными правками + тесты поверх) / **INSPIRE** (логика правильная, переписать с тестами и с правильным DI) / **DISCARD** (заменим иначе или вообще не нужно).

### 4.1 Sources/BotBridge/

| Файл | Решение | Почему | Что меняется в v2 |
|---|---|---|---|
| `Config/BotBridgeConfig.swift` | **COPY** | Чистый env-loader, без зависимостей. Логика валидации работает | Rename → `VKBridgeConfig`, **fail-fast** на required env (B-02), плюс unit тесты. Убрать `VK_CALLBACK_SECRET` (не нужен — нет публичного endpoint). Убрать `VK_CONFIRMATION_TOKEN` (не нужен в long-poll) |
| `VK/VKModels.swift` | **PARTIAL COPY** | Codable модели сообщений переиспользуются (message_new event одинаковый между webhook и long-poll) | Сохранить: `VKMessage`, `VKMessageObject`. Убрать: `VKConfirmation` (не нужен). Добавить: `VKLongPollSession`, `VKLongPollResponse`, `VKLongPollFailure` |
| `VK/VKAPIClient.swift` | **COPY** | Уже принимает `HTTPClientProtocol` через DI (мокабельный), `messages.send` работает корректно | + Unit тесты на encodeFormBody / params; + Component тесты с MockHTTPClient |
| `VK/VKWebhookHandler.swift` | **DISCARD** | Hummingbird-специфичен, в long-poll архитектуре webhook не существует | Заменяется на `VKEventDispatcher` (новый, без Hummingbird) |
| `Commands/CommandProcessor.swift` (557 строк!) | **DISCARD + INSPIRE** | Слишком толстый actor, всё в одном файле, нет тестов. Логика команд верна но смешана с парсингом, форматтером, sendingом | Разбить на: `CommandParser` (Unit), `CommandRouter` (actor с whitelist + concurrent guard), отдельные `Handler` per command |
| `Commands/BotSessionState.swift` | **INSPIRE** | Идея session state per-owner верная, но как actor простой | Переписать как `OwnerSessionStore` с тестами на TTL/expiration |
| `HTTP/BotBridgeServer.swift` | **DISCARD** | Hummingbird app — в long-poll архитектуре HTTP сервер не нужен | Заменяется на `VKLongPollClient` (новый, URLSession-based, актор-цикл) |

### 4.2 Sources/TgClientModels/

| Файл | Решение | Почему |
|---|---|---|
| `Requests/GetChatsRequest.swift` | **COPY** | TDLib request DTO, чистый Codable |
| `Requests/SearchPublicChatRequest.swift` | **COPY** | Same |
| `Requests/SendMessageRequest.swift` | **COPY** | Same |
| `Requests/ViewMessagesRequest.swift` | **COPY** | Same |
| `DigestCore/UnreadChatInfo.swift` | **COPY** | Простой DTO с unreadCount, type, name |
| `Responses/ChatResponse.swift` (`init вне DEBUG`) | **COPY** | Production fix, нужен в любом случае |
| `Responses/FormattedText.swift` (`init вне DEBUG`) | **COPY** | Same |
| `Responses/Message.swift` (`: TDLibResponse`) | **COPY** | Корректное наследование |
| `Responses/MessageContent.swift` (caption из медиа) | **COPY** | Закрывает B-04 на уровне модели |
| `TDLibClientProtocol.swift` (4 новых метода) | **COPY** | Контракт корректный |

### 4.3 Sources/TDLibAdapter/

| Файл | Решение | Почему |
|---|---|---|
| `TDLibClient+HighLevelAPI.swift` (38 lines: getChats, sendMessage, searchPublicChat, viewMessages) | **COPY** | Тонкие wrappers, прямое отображение TDLib API |

### 4.4 Sources/DigestCore/

| Файл | Решение | Почему |
|---|---|---|
| `ChannelMessageSource.fetchUnreadChats()` (новый метод) | **COPY + tests** | Уже работает в spike. Добавить unit тесты на фильтрацию архивов / secret чатов / unreadCount > 0 |
| `ChannelMessageSource.fetchMessages(for:)` (iterative pagination) | **INSPIRE** | Pagination логика правильная, но смешана с filtering. Вынести `IterativePaginator` как отдельный testable component |

### 4.5 Sources/App/

| Файл | Решение | Почему |
|---|---|---|
| `main.swift` service mode (oneshot vs service switch) | **INSPIRE** | Идея режимов верная. В long-poll архитектуре lifecycle проще — обычный `Task` с `withTaskCancellationHandler` + signal handling без swift-service-lifecycle (см. раздел 5.6) |

### 4.6 .claude/

| Файл | Решение | Почему |
|---|---|---|
| `vk-bot-bridge-rfc.md` (старый) | **KEEP as-is** | Архив. В новом RFC ссылаемся как на исторический документ |
| `v0.4.0-beta-release-materials.md` | **KEEP** | Релизные заметки spike-фазы, исторический артефакт |

### 4.7 Новые компоненты (которых не было в spike)

Long-poll архитектура добавляет файлы которых в spike не существовало:

| Файл | Назначение |
|---|---|
| `Sources/VKBridge/LongPoll/VKLongPollClient.swift` | актор-цикл long-poll (см. 5.4) |
| `Sources/VKBridge/LongPoll/VKLongPollSession.swift` | DTO `{server, key, ts}` |
| `Sources/VKBridge/LongPoll/VKLongPollResponse.swift` | response decoder + `failed:1/2/3` discriminator |
| `Sources/VKBridge/LongPoll/BackoffPolicy.swift` | exponential backoff с DI clock (для тестов) |
| `Sources/VKBridge/Events/VKEventDispatcher.swift` | замена `VKWebhookHandler`, актор-диспатчер |
| `Sources/VKBridge/Events/WhitelistFilter.swift` | вынесенный фильтр с unit тестами |
| `Sources/VKBridge/Events/AuditLogger.swift` | вынесенный logger с unit тестами (закрывает B-09) |
| `Sources/DigestCore/Notifiers/VKBotNotifier.swift` | реализация `BotNotifierProtocol` поверх `VKAPIClient` |

### 4.8 Сводный итог

| Категория | Файлов из spike | Доля |
|---|---|---|
| COPY (с тестами поверх) | 13 | ~59% |
| PARTIAL COPY (модели без webhook-частей) | 1 | ~5% |
| INSPIRE (переписать с TDD) | 5 | ~23% |
| DISCARD (Hummingbird-специфичные) | 3 (`VKWebhookHandler`, `BotBridgeServer`, `VK_CALLBACK_SECRET`/`CONFIRMATION_TOKEN` логика) | ~13% |

**Вывод:** ~64% spike-кода переиспользуется (COPY + PARTIAL). Discard'нутые ~13% относятся **именно** к webhook-механике, которая в long-poll архитектуре исчезает. Это не «переписать всё с нуля», это **легитимизация** spike тестами + замена webhook-инфры на более простой long-poll loop.

---

## 📋 Раздел 5 — Архитектура v2 (VK Long Poll)

> **Архитектурное решение** (изменено относительно spike): отказ от webhook + Hummingbird в пользу **VK Long Poll** через URLSession. Обоснование — раздел 1.4. Эффект: −5 транзитивных deps, −20–40 сек к build time на Linux, упрощение деплоя для open-source пользователей (не нужны nginx + HTTPS-домен + cert).

### 5.1 Целевая структура таргетов

```
Package.swift
├── App (executable)
│   ├── DigestCore (existing — добавляется VKBotNotifier в Notifiers/)
│   ├── TDLibAdapter (existing, без изменений)
│   ├── BotBridge (existing в main для v0.5.0 TelegramBotNotifier)
│   └── VKBridge (НОВЫЙ — long-poll loop + commands)
│         ↓ deps
│         DigestCore (для ChannelMessageSource, BotNotifierProtocol, HTTPClientProtocol)
│         TDLibAdapter (для TDLibClient passthrough)
│         Logging (уже в проекте)
│         (НЕТ Hummingbird, НЕТ swift-nio, НЕТ swift-service-lifecycle)
└── Tests
    ├── TgClientUnitTests/VKBridge/         (НОВЫЙ суб-каталог)
    ├── TgClientComponentTests/VKBridge/    (НОВЫЙ)
    └── TgClientE2ETests/VKBridgeE2ETests/  (НОВЫЙ)
```

**Принцип разделения:**

- `DigestCore.Notifiers/` — **выходные адаптеры** (Telegram bot уже, VK добавляется как `VKBotNotifier`)
- `VKBridge` — **входной адаптер** (long-poll цикл + парсинг events + dispatch к командам). Бизнес-логика лежит в DigestCore

### 5.2 Расположение `VKBotNotifier`

**Решение:** `VKBotNotifier` живёт в `Sources/DigestCore/Notifiers/VKBotNotifier.swift`, рядом с `TelegramBotNotifier`, реализует **тот же** `BotNotifierProtocol`.

**Почему:**
- Согласованность с уже существующим pattern в main
- Зависимости: `URLSessionHTTPClient` + `HTTPClientProtocol`, без Hummingbird
- Тесты: `Tests/TgClientComponentTests/DigestCore/VKBotNotifierTests.swift` (по аналогии с `TelegramBotNotifierTests.swift`)

```swift
// Sources/DigestCore/Notifiers/VKBotNotifier.swift
public actor VKBotNotifier: BotNotifierProtocol {
    private let httpClient: HTTPClientProtocol
    private let config: VKNotifierConfig

    public func notify(_ digest: Digest, to recipient: Recipient) async throws { ... }
    public func sendRaw(_ text: String, to peerID: Int64) async throws { ... }
}
```

### 5.3 Поток данных (целевой)

```
[App startup, service mode]
                            │
                            ▼
    VKLongPollClient.run()  (актор, бесконечный цикл)
                            │
   ┌────────────────────────┴──────────────────────────┐
   │                                                   │
   ▼ (1) GET groups.getLongPollServer                  │
   VK API ──► {server, key, ts}                        │
   │                                                   │
   ▼ (2) GET <server>?key=<k>&ts=<ts>&wait=25          │
   VK API (висит до 25 сек) ──► one of:                │
        a) {ts: new_ts, updates: [...]}                │
        b) {failed: 1, ts: new_ts}    ← retry с new_ts │
        c) {failed: 2}                ← обновить key   │
        d) {failed: 3}                ← обновить ts    │
        e) network error              ← backoff retry  │
   │                                                   │
   ▼ (3) для каждого update в response:                │
        Task { await dispatcher.dispatch(update) }     │
   │      │                                            │
   │      ▼                                            │
   │   VKEventDispatcher (актор)                       │
   │      ├─ filter: type == "message_new"             │
   │      ├─ WhitelistFilter (by from_id)              │
   │      ├─ AuditLogger (one INFO log)                │
   │      └─ CommandRouter.process(text, ownerID)      │
   │                                                   │
   ▼ (4) обновить ts, перейти к шагу (2)               │
   ─────────────────────────────────────────────────────
                            ▼
         CommandRouter (actor)
                            │
              ├─ ConcurrentGuard (один cmd в обработке)
              ├─ CommandParser (Unit-tested, pure)
              └─ dispatch к Handler[command]
                            │
                            ▼
              Handler<Command>
              │   читает: ChannelMessageSource (DigestCore)
              │   форматирует: RawDigestFormatter
              │   пишет: VKBotNotifier (DigestCore.Notifiers)
              │   markaread: TDLibClient (TDLibAdapter)
                            ▼
              VKBotNotifier
              │   chunks ≤ 4096
              │   POST api.vk.com/method/messages.send
                            ▼
              VK API → user
```

### 5.4 Ключевые компоненты (новые)

#### `VKLongPollClient` — основной актор-цикл

```swift
// Sources/VKBridge/LongPoll/VKLongPollClient.swift

public actor VKLongPollClient {
    private let httpClient: any HTTPClientProtocol
    private let config: VKLongPollConfig
    private let dispatcher: VKEventDispatcher
    private let logger: Logger
    private let backoff: BackoffPolicy  // injectable для тестов

    private var session: VKLongPollSession?  // {server, key, ts}
    private var isRunning = false

    /// Запускает бесконечный цикл long-poll. Не возвращается до cancellation.
    public func run() async throws {
        isRunning = true
        while isRunning && !Task.isCancelled {
            do {
                if session == nil {
                    session = try await fetchSession()
                }
                let response = try await pollOnce(session: session!)
                try await handle(response: response)
            } catch let error as VKLongPollFailure {
                try await handleFailure(error)  // failed=1/2/3
            } catch {
                logger.warning("long-poll network error", metadata: ["error": .string("\(error)")])
                try await backoff.delay()  // exponential 1→2→4→8s, max 60s
            }
        }
    }

    public func stop() { isRunning = false }

    private func fetchSession() async throws -> VKLongPollSession { ... }
    private func pollOnce(session: VKLongPollSession) async throws -> VKLongPollResponse { ... }
    private func handle(response: VKLongPollResponse) async throws { ... }
    private func handleFailure(_ failure: VKLongPollFailure) async throws { ... }
}
```

#### `VKEventDispatcher` — диспатч событий из long-poll

```swift
// Sources/VKBridge/Events/VKEventDispatcher.swift

public protocol VKEventDispatcher: Sendable {
    func dispatch(_ update: VKUpdate) async
}

public actor DefaultVKEventDispatcher: VKEventDispatcher {
    private let whitelist: WhitelistFilter
    private let audit: AuditLogger
    private let router: CommandRouter

    public func dispatch(_ update: VKUpdate) async {
        guard update.type == "message_new" else { return }
        guard let message = update.object?.message else { return }
        guard whitelist.allows(fromID: message.fromID) else {
            audit.log(.deniedByWhitelist(fromID: message.fromID))
            return
        }
        audit.log(.commandReceived(fromID: message.fromID, text: message.text))
        await router.process(rawText: message.text, fromOwner: message.fromID)
    }
}
```

#### `CommandRouter` и далее — без изменений относительно прошлой версии раздела

```swift
public actor CommandRouter {
    func process(rawText: String, fromOwner ownerID: VKUserID) async
}

public actor OwnerSessionStore {  // session state per-owner
    func setMenu(_ menu: ChatMenu, for ownerID: VKUserID)
    func menu(for ownerID: VKUserID) -> ChatMenu?
}

public protocol CommandHandler {
    associatedtype Cmd: Command
    func handle(_ cmd: Cmd, ctx: CommandContext) async throws -> CommandResult
}
```

```swift
// DigestCore (уже есть в main)
public protocol BotNotifierProtocol {
    func notify(_ digest: Digest, to recipient: Recipient) async throws
    func sendRaw(_ text: String, to recipient: Recipient) async throws
}

public actor VKBotNotifier: BotNotifierProtocol { ... }  // NEW
public actor TelegramBotNotifier: BotNotifierProtocol { ... }  // already in main
```

### 5.5 Логика рестарта long-poll цикла

VK Bot Long Poll API возвращает специальные коды `failed`, которые **обязательно** обрабатывать:

| Код | Что значит | Действие |
|---|---|---|
| **`{updates: [...], ts: new}`** | OK, есть события | сохранить `ts`, обработать события, продолжить с шага 2 |
| **`{failed: 1, ts: new}`** | События пришли, но `ts` устарел | сохранить `new ts`, продолжить с шага 2 |
| **`{failed: 2}`** | Ключ долго (24ч) не обновлялся | заново вызвать `groups.getLongPollServer` (шаг 1), затем шаг 2 |
| **`{failed: 3}`** | Информация устарела (`key` + `ts` оба) | то же что failed:2 |
| Network error / timeout | Сетевая ошибка | `BackoffPolicy.delay()` (exponential 1→2→4→8 сек, max 60 сек), потом шаг 2 |
| HTTP 5xx от VK | Серверная ошибка VK | exponential backoff |
| `Task.isCancelled` | Сервис останавливается | break loop, return |

**Принцип fail-safe:**
- Long-poll цикл **никогда не завершается сам** (только по `stop()` или cancellation)
- Любая ошибка → лог + retry. Не падаем
- Если 10+ подряд network errors — лог `error`, продолжаем (но уже есть signal что что-то не так)
- При SIGTERM (systemd stop) — graceful: `client.stop()` + ждём текущий poll до 30 сек, потом cancel

### 5.6 Lifecycle интеграция в main.swift

```swift
@main
struct TGClient {
    static func main() async {
        // 1. env, logger, TDLib auth (как сейчас)
        // 2. mode = oneshot | service
        switch mode {
        case .oneshot:
            // существующий путь
        case .service:
            let httpClient = URLSessionHTTPClient()
            let notifier = VKBotNotifier(httpClient: httpClient, config: ...)
            let dispatcher = DefaultVKEventDispatcher(...)
            let longPoll = VKLongPollClient(
                httpClient: httpClient,
                dispatcher: dispatcher,
                config: config.longPoll,
                logger: logger,
                backoff: .exponential(min: 1, max: 60)
            )

            // graceful shutdown через signal handling
            await withTaskCancellationHandler {
                try? await longPoll.run()
            } onCancel: {
                Task { await longPoll.stop() }
            }
        }
    }
}
```

**Что заменилось:**
- ❌ Hummingbird `Application` + router + middleware
- ❌ swift-service-lifecycle `ServiceGroup`
- ✅ Простой `Task` с `withTaskCancellationHandler`

### 5.7 Изменения в Package.swift

```swift
// БЕЗ новых внешних dependencies (URLSession уже есть через Foundation)

.target(
    name: "VKBridge",
    dependencies: [
        "DigestCore",
        "TDLibAdapter",
        .product(name: "Logging", package: "swift-log"),  // уже в проекте
    ]
),

// в App.dependencies добавить "VKBridge"

// тесты:
.testTarget(name: "TgClientUnitTests", dependencies: [..., "VKBridge"]),
.testTarget(name: "TgClientComponentTests", dependencies: [..., "VKBridge"]),
.testTarget(name: "TgClientE2ETests", dependencies: [..., "VKBridge"]),
```

### 5.8 Что НЕ трогаем

- `DigestCore.MessageSource` — без изменений
- `TDLibAdapter` — добавляем только to-be-tested wrappers (getChats etc.) если их ещё нет в main
- `BotBridge` target из v0.5.0 — **не путать** со spike `BotBridge`, это разные имена. Целевое имя нашего нового — **VKBridge** (без коллизии)
- `URLSessionHTTPClient` + `HTTPClientProtocol` — уже подходит, ничего расширять не нужно

### 5.9 nginx и сетевая конфигурация

**Cерверная сторона:**
- ❌ Раньше: `location /vkWebHook` в nginx, входящий port 8082, HTTPS cert
- ✅ Теперь: **ничего**. Long-poll = только исходящие соединения к `api.vk.com`. nginx остаётся как есть для cashflow.

**Что это даёт open-source пользователям:** запуск без домена/cert. Просто `swift run tg-client service` + env vars + работающий VK community.

---

## 📋 Раздел 6 — TDD Pipeline

> Outside-In, **per-story цикл**. Не двигаемся к US-(N+1), пока US-N не GREEN + REFACTORed. Это значит: 7 stories = 7 циклов = 7 PR (или 7 пачек коммитов в ветке).

### 6.1 Шаблон одной итерации (per-story)

```
┌──────────────────────────────────────────────────────────────┐
│  US-N итерация (1–3 дня каждая)                              │
├──────────────────────────────────────────────────────────────┤
│  STEP 0  Architecture-First spike                            │
│         → если новые зависимости, новые протоколы — открыть   │
│            мини-RFC внутри этого документа (раздел 5.4)       │
│  STEP 1  E2E test (RED)                                      │
│         → описывает observable behavior из AC story          │
│         → mock'и: TDLib + VK API                             │
│  STEP 2  Component drop-in                                   │
│         → выявляются collaborators (Formatter, Parser, ...)   │
│         → tests пишем ДО реализации                          │
│  STEP 3  Unit tests                                          │
│         → парсеры, форматтеры, модели — pure functions       │
│         → быстрые (<10ms each)                               │
│  STEP 4  GREEN — minimal code                                │
│         → fakeit'ить если нужно, но всё что fake → TODO       │
│  STEP 5  REFACTOR                                            │
│         → extract, rename, kill duplication                  │
│         → tests stay green throughout                        │
│  STEP 6  Smoke run на dev VPS (для P0 stories)               │
│         → VK команда → реальный TG → проверка глазами         │
│  STEP 7  Commit batch + PR (or merge to feature/vk-bridge-tdd)│
└──────────────────────────────────────────────────────────────┘
```

### 6.2 Roles per step (из ROLES.md main)

| STEP | Роль | Что делает |
|---|---|---|
| 0 | **Architect** | Согласовать структуру модулей и протоколов с пользователем ДО кода |
| 1 | **Testing Architect** | E2E дизайн + mock стратегия для story |
| 2 | **Testing Architect** | Component тесты — выявление collaborators |
| 3 | **Testing Architect** | Unit тесты — pure logic (parsers, formatters) |
| 4 | **Swift Developer** | Минимальный код для GREEN |
| 5 | **Swift Developer** | Refactor с защитой тестами |
| 6 | **AI-Assisted Developer** | Smoke + контекст-проверка на сервере |
| 7 | **Swift Developer** | Атомарные коммиты, PR description |

### 6.3 Research-First чек (для US-1)

US-1 — первая story → новый код для нового таргета. **Обязателен Research-First** (см. TESTING.md):

- ✅ Spike на VK API + sendMessage + URLSession в Swift 6 — уже **сделан** (`feature/vk-bot-bridge`)
- ✅ VKModels (message_new event) — Codable модели проверены spike
- ⚠️ **Не done:** **VK Bot Long Poll API** — нужен мини-spike (~1ч) на проверку:
  - формат ответа `groups.getLongPollServer`
  - формат long-poll request (GET vs POST, где key, ts, wait, mode)
  - точная схема `failed: 1/2/3` (есть ли case с `failed=4`?)
  - таймаут долго висящего соединения (URLSession default 60 сек, наш `wait=25` — должно хватить)
  - что возвращает VK при revoke токена (для алертов)
- ⚠️ **Не done:** `withTaskCancellationHandler` + `URLSession` cancellation в Linux Foundation — проверить что cancel прерывает долгий long-poll корректно (~30 мин)

### 6.4 Anti-pattern: чего избегаем (из TESTING.md retro)

| Соблазн | Почему вредно | Что делаем |
|---|---|---|
| Mock >100 строк (например, fake VK API server in-process) | Mock сам становится bug source, эффект "тестирую mock" | `MockHTTPClient` → table-driven scripted responses, ≤30 строк. Ассертим request snapshot |
| Параллельные тесты на shared TDLib state | Race conditions в самих тестах | Каждый тест → свой `MockTDLibClient` instance |
| Закомментированный тест | Легко забыть, накапливается tech debt | `#if false` запрещён. Только `XCTSkip` с reason |
| Тесты на implementation detail | Пере-mocking, fragile | E2E через protocol boundary, не через приватные функции |
| "Пишу код, потом обмажусь тестами" | Мы это уже сделали в spike — больше не делаем | RED→GREEN строго |

### 6.5 Definition of Done (per story)

Story закрыта если:
- [x] AC из раздела 2 — все Given/When/Then проходят
- [x] Все 3 уровня тестов на месте (E2E + Component + Unit где применимо)
- [x] Coverage на новый код ≥ 80% (line + branch)
- [x] `swift test 2>&1` GREEN на Linux (build-clean.sh)
- [x] `swift test --sanitize=thread 2>&1` GREEN (TSan)
- [x] Smoke на dev VPS пройден для P0 stories (US-1, US-2, US-3)
- [x] Audit log на команду виден в syslog (B-09)
- [x] Atomic commits (Sources / Tests / Docs отдельно)

### 6.6 Параллелизация (если нужна скорость)

Stories внутри **одной фазы** обычно зависят (US-2 строит меню, US-3 его читает). Между фазами — сильно меньше связности. **Возможность параллели:** US-5/US-6/US-7 в Phase 3 — независимые, можно делать в любом порядке или параллельно (ветки feature/vkbridge-us5 etc, потом merge). Но для одного разработчика — последовательно проще.

---

## 📋 Раздел 7 — Phasing (3 фазы реализации)

> Каждая фаза = own merge-commit в `feature/vk-bridge-tdd` (потом одним PR в main, либо incremental). Smoke + регресс между фазами обязательны.

### Phase 1 — Infrastructure + US-1 /ping

**Цель:** заложить фундамент. К концу — работающий `/ping` в проде, без бизнес-логики, без Hummingbird.

**Содержимое:**
- ✅ Создать `Sources/VKBridge/` target в Package.swift (БЕЗ Hummingbird, БЕЗ swift-nio)
- ✅ Mini-spike: VK Bot Long Poll API contract verification (~1ч)
- ✅ Mini-spike: URLSession cancellation на Linux в long-poll context (~30 мин)
- ✅ `VKBridgeConfig` (COPY из spike + rename + fail-fast валидация на старте, B-02)
- ✅ `VKModels.swift` (PARTIAL COPY: message_new + новые long-poll DTO)
- ✅ `VKLongPollClient` (НОВЫЙ актор-цикл с retry/backoff)
- ✅ `VKLongPollSession`, `VKLongPollResponse`, `VKLongPollFailure` (НОВЫЕ DTO)
- ✅ `BackoffPolicy` (НОВЫЙ — exponential, DI clock)
- ✅ `VKEventDispatcher` (НОВЫЙ — заменяет VKWebhookHandler)
- ✅ `WhitelistFilter` + `AuditLogger`
- ✅ `CommandRouter` (минимум — только обработка `/ping`)
- ✅ `VKBotNotifier` в `DigestCore.Notifiers/` (минимум — `sendRaw`)
- ✅ `App/main.swift` service mode переписан через `withTaskCancellationHandler` + signal handling
- ✅ E2E US-1 (3 теста: happy / deny / TDLib disconnected)
- ✅ Component тесты: VKLongPollClient (failed=1/2/3, network error, backoff, graceful shutdown), WhitelistFilter, AuditLogger, VKEventDispatcher, VKBotNotifier
- ✅ Unit тесты: VKModels, VKBridgeConfig, BackoffPolicy

**Smoke acceptance:**
- `/ping` от owner → "pong (...)" в VK в течение 3 сек (long-poll latency = poll wait + roundtrip)
- `/ping` от не-owner → silent ignore + лог `decision=deny` (никаких VK API вызовов)
- `/digest` → ответ "Команда не реализована в этой фазе" (placeholder)
- Pull network cable на 10 сек на VPS → лог про backoff → восстановление работы автоматическое

**DoD:** US-1 закрыта (раздел 6.5).

**Estimate:** ~3-4 дня (slightly faster than webhook variant — нет Hummingbird-плотры).

---

### Phase 2 — Digest Core (US-2 menu + US-3 get N)

**Цель:** ключевая ценность сервиса — пользователь может прочитать непрочитанные через VK.

**Содержимое:**
- ✅ `MenuFormatter` — группировка по типу чата
- ✅ `ChatGrouper` — fetchUnreadChats() из DigestCore
- ✅ `ChunkSplitter` — 4096 char без потери нумерации между чанками
- ✅ `OwnerSessionStore` — session state с TTL
- ✅ `RawDigestFormatter` — entities + media markers (B-03 + B-04)
- ✅ `EntityConverter` — bold/italic/links → unicode/plain
- ✅ `MediaMarker` — `[🖼️]`, `[🎵]`, `[🎥]`, `[📎]`, `[📍]`
- ✅ `GraphemeSafeTruncate` — Cyrillic/emoji-safe (B-05)
- ✅ `Handler[/digest]`, `Handler[/get N]`, `Handler[/get all|channels|groups|dm]`
- ✅ `Handler[/get N read]` — опциональная отметка (закрывает B-06 частично)
- ✅ E2E US-2: 3 сценария (нормальное меню / пусто / >50 чатов)
- ✅ E2E US-3: 4 сценария (happy / меню не было / N out of range / `read` суффикс)
- ✅ Component: ChunkSplitter, RawDigestFormatter, EntityConverter
- ✅ Unit: 30+ entity/media сценариев + GraphemeSafeTruncate

**Smoke acceptance:**
- `/digest` → меню в VK с реальными непрочитанными
- `/get 1` → читаемые сообщения с правильными bold/italic/links
- `/get 1 read` → чат помечен прочитанным
- Большие списки (50+ чатов) — без обрыва нумерации

**DoD:** US-2 + US-3 закрыты.

**Estimate:** ~5-7 дней (самая контентная фаза).

---

### Phase 3 — Advanced Commands (US-4..US-7)

**Цель:** догнать spike по функциональности. Все 7 stories покрыты тестами.

**Содержимое:**
- ✅ US-4 `/last N count` — `IterativePaginator` с TDLib batch=100
- ✅ US-5 `/reply N text` — sendMessage handler
- ✅ US-6 `/to <username> text` — UsernameNormalizer + searchPublicChat
- ✅ US-7 `/read N` — ReadMarker через viewMessages

**Тесты:** см. раздел 3 (Test Matrix per story).

**Smoke acceptance:**
- Все команды spike доступны в v2 binary
- 1:1 функциональный паритет со spike-binary
- Никаких regressions относительно Phase 2

**DoD:** US-4..US-7 закрыты, готовность к v0.6.0 release.

**Estimate:** ~5-7 дней (4 stories по 1-2 дня, US-4 с pagination — длиннее).

---

### Phasing summary

| Phase | Stories | Duration | Smoke | Может уйти в прод? |
|---|---|---|---|---|
| 1 | US-1 | 3-5 days | `/ping` works | ❌ нет (spike-binary остаётся) |
| 2 | US-2, US-3 | 5-7 days | `/digest`+`/get` works | ⚠️ optional (паритет ещё не достигнут) |
| 3 | US-4..US-7 | 5-7 days | full parity со spike | ✅ да — cutover (см. раздел 8) |
| **Total** | 7 | **13-19 days** | — | — |

**Realistic timeline:** 3-4 недели с учётом отладки, ревью, реальной нагрузки.

### 7.1 Не-функциональные задачи (между фазами)

- После Phase 1: обновить `ARCHITECTURE.md` (новый таргет VKBridge)
- После Phase 2: обновить `DEPLOY.md` (новые env vars если появились) + `BACKLOG.md` (отложенные feature flags)
- После Phase 3: обновить `MVP.md` (vk bridge — out of MVP scope, но закроем как nice-to-have)
- Перед merge в main: обновить `README.md` + создать release notes

---

## 📋 Раздел 8 — Versioning + Release plan

> Цель: предсказуемый переход со spike на v0.6.0 без downtime, с возможностью отката.

### 8.1 Версионная семантика

| Тег | Что внутри | Когда | Как помечается |
|---|---|---|---|
| `v0.6.0-alpha.1` | Phase 1 готова (US-1) | конец Phase 1 | `feature/vk-bridge-tdd` локально, не пушим в main |
| `v0.6.0-beta.1` | Phase 2 готова (US-2 + US-3) | конец Phase 2 | merge `feature/vk-bridge-tdd` в main + git tag, **деплой на VPS параллельно spike** (другой порт) |
| `v0.6.0-rc.1` | Phase 3 готова, паритет со spike | конец Phase 3 | merge в main + git tag, **cutover на VPS** (см. 8.3) |
| `v0.6.0` | После недели стабильности на проде | +7 дней после rc.1 без инцидентов | git tag + GitHub release |

### 8.2 Что считается "паритет со spike"

| Команда | Spike | v0.6.0-rc.1 |
|---|---|---|
| `/test` (alias /ping) | ✅ | ✅ |
| `/digest` | ✅ | ✅ |
| `/get N`, `/get all`, `/get channels|groups|dm` | ✅ | ✅ |
| `/last N [count]` | ✅ | ✅ |
| `/reply N <text>` | ✅ | ✅ |
| `/to <username> <text>` | ✅ | ✅ |
| ~~`/to_alena`~~ | ✅ shortcut | ❌ **удалён** (хардкод имени, open-source — убираем) |
| `/read N` | ✅ | ✅ |
| **Entities (bold/italic/links)** | ❌ | ✅ (новое — закрывает B-03) |
| **Media markers** | ❌ | ✅ (новое — закрывает B-04) |
| **Audit log** | partial | ✅ (новое — закрывает B-09) |
| **Long-poll auto-restart** | ❌ (был webhook) | ✅ (новое — failed=1/2/3 + network backoff) |

### 8.3 Cutover plan на VPS (long-poll специфика)

**Ключевое отличие от webhook:** long-poll цикл — это **исходящие** соединения. Если **два** binary одновременно делают `groups.getLongPollServer` для одной и той же group + token, оба получают валидные ключи и оба полят одни и те же события. VK дублирует events на оба соединения. Это означает:
- ❌ **Нельзя** держать spike + v0.6.0 одновременно на одном VK group — оба отреагируют на каждую команду, юзер получит дублированные ответы
- ✅ Cutover требует **brief downtime** (~30 сек): остановить spike → запустить v0.6.0

**Шаг 1: параллельный билд v0.6.0-beta** (после Phase 2):
- Бинарь `/opt/tg-client-v06/tg-client` (отдельный путь, рядом со spike)
- systemd unit `tg-client-v06.service` **в `disabled` состоянии** (не стартует автоматически)
- Использует **отдельный** `TDLIB_STATE_DIR=/opt/tg-client-v06/.tdlib` (свежий)
- Использует **тот же** VK group, **тот же** `VK_BOT_TOKEN`
- ⚠️ **Не запускаем одновременно со spike**

**Шаг 2: smoke window 1 (~5 мин)** на v0.6.0-beta:
```bash
# окно низкой нагрузки (рано утром, например)
systemctl stop tg-client.service          # spike off → VK long-poll сессия закрывается
systemctl start tg-client-v06.service     # v0.6.0 on
# ждём ~10 сек pока v06 авторизует TDLib и запустит long-poll
# отправляем /ping, /digest вручную в VK
# проверяем journalctl -u tg-client-v06 -f
# если что-то не так:
systemctl stop tg-client-v06.service
systemctl start tg-client.service         # обратно на spike
```

**Шаг 3: cutover на v0.6.0-rc.1** (после Phase 3):
```bash
# на сервере (root, без sudo нужен)
systemctl stop tg-client.service           # spike off, downtime начинается

# подменяем бинарь
cp /opt/tg-client/tg-client /opt/tg-client/tg-client.spike   # backup
cp /opt/tg-client-v06/tg-client /opt/tg-client/tg-client     # v06 → main

# обновляем env (новые vars)
cp /etc/tg-client.env /etc/tg-client.env.backup
# ... merge новые VK_LONGPOLL_* vars ...

systemctl start tg-client.service          # теперь v0.6.0 binary
systemctl status tg-client.service         # check
journalctl -u tg-client -n 50 -f           # smoke

# downtime end (~20-30 сек)

# отключаем параллельный unit (если был)
systemctl disable tg-client-v06.service
rm /etc/systemd/system/tg-client-v06.service
systemctl daemon-reload
```

**Шаг 4: rollback** (если что-то пошло не так):
```bash
systemctl stop tg-client.service
cp /opt/tg-client/tg-client.spike /opt/tg-client/tg-client   # back to spike
cp /etc/tg-client.env.backup /etc/tg-client.env
systemctl start tg-client.service
# spike возвращается, бинарь v0.6.0 остаётся на /opt/tg-client-v06/ для дебага
```

**Downtime окно:** ~20-30 сек. При personal use не критично.

### 8.4 ENV миграция

**v0.6.0 ENV vars** (упрощены относительно spike — нет webhook, нет порта):

| Var | Spike | v0.6.0 | Примечание |
|---|---|---|---|
| `TELEGRAM_API_ID` | ✅ | ✅ | без изменений |
| `TELEGRAM_API_HASH` | ✅ | ✅ | без изменений |
| `TDLIB_STATE_DIR` | ✅ | ✅ | без изменений |
| `VK_BOT_TOKEN` | ✅ | ✅ **required** | community access token со scope `messages` |
| `VK_BOT_GROUP_ID` | ✅ | ✅ **required** | числовой ID сообщества |
| `VK_BOT_OWNER_IDS` | ✅ | ✅ **required** | comma-separated VK user IDs |
| ~~`VK_CALLBACK_SECRET`~~ | ✅ | ❌ **удалён** | webhook secret, в long-poll не нужен |
| ~~`VK_CONFIRMATION_TOKEN`~~ | ✅ | ❌ **удалён** | webhook confirmation handshake, в long-poll не нужен |
| `VK_API_VERSION` | optional | optional default `5.199` | без изменений |
| ~~`BOT_BRIDGE_PORT`~~ | ✅ | ❌ **удалён** | внутренний HTTP сервер не нужен |
| `OPENAI_API_KEY` | optional | optional | для будущих AI команд |
| **`VK_LONGPOLL_WAIT`** | — | optional, default `25` | seconds — long-poll wait timeout |
| **`VK_LONGPOLL_BACKOFF_MAX`** | — | optional, default `60` | seconds — max exponential backoff |
| **`VK_AUDIT_LOG_LEVEL`** | — | optional, default `info` | управление audit log verbosity |

**Принцип:** v0.6.0 ENV — **подмножество** spike-env (минус webhook-специфичные). Spike `.env` будет работать, лишние vars просто игнорируются. Новые `VK_LONGPOLL_*` — opt-in с разумными дефолтами.

**Документация для open-source пользователей:** в `DEPLOY.md` добавить раздел «Получение VK creds» с скриншотами и шагами:
1. Создать VK community → Управление → Работа с API → создать access_token со scope `messages, manage`
2. В разделе Long Poll API → включить, выбрать события `message_new`
3. Указать `VK_BOT_GROUP_ID` (виден в URL community) и `VK_BOT_OWNER_IDS` (свой VK user ID)

### 8.5 Release notes draft (для v0.6.0)

```markdown
# v0.6.0 — VK Bridge (Long Poll, TDD-driven)

## What's new
- ✨ VK Bot Bridge (production-ready) — альтернативный канал команд через VK Bot Long Poll API
- ✨ Семь команд: /ping, /digest, /get, /last, /reply, /to, /read
- ✨ Telegram entities (bold/italic/links) сохраняются в VK ответе
- ✨ Media markers ([🖼️], [🎵], [🎥], [📎], [📍]) для медиа без caption
- ✨ Простой деплой — нет nginx, нет HTTPS-домена, нет inbound портов

## Improvements
- 🔒 Fail-fast валидация конфига при старте сервиса
- 📋 Audit log на каждую команду (cmd, owner, decision, duration)
- 🛡️ Grapheme-safe truncation для Cyrillic + emoji
- 🔁 Auto-restart long-poll цикл с exponential backoff (failed=1/2/3 + network errors)
- 🚪 Graceful shutdown через TaskCancellation (без swift-service-lifecycle)

## Migration from spike (feature/vk-bot-bridge)
- **Архитектурное изменение:** webhook + Hummingbird → Long Poll + URLSession
- ENV: удалены `VK_CALLBACK_SECRET`, `VK_CONFIRMATION_TOKEN`, `BOT_BRIDGE_PORT`. Новые: `VK_LONGPOLL_WAIT`, `VK_LONGPOLL_BACKOFF_MAX`
- systemd: упрощён (см. DEPLOY.md)
- nginx: `location /vkWebHook` больше не нужен — можно удалить
- Новый таргет: `VKBridge` + `DigestCore.Notifiers.VKBotNotifier`
- 100+ новых тестов (Unit + Component + E2E)
- Удалена команда `/to_alena` (хардкод имени)

## Removed dependencies
- `hummingbird-project/hummingbird` (5+ транзитивных deps)
- `swift-service-lifecycle`
- `swift-http-types`
```

---

## 📋 Раздел 9 — Acceptance criteria для merge в main

> Критерии приёмки финального PR. Без них — merge не делаем, даже если "вроде работает".

### 9.1 Code & Tests

- [ ] **Coverage** на новый код (`Sources/VKBridge/` + `DigestCore/Notifiers/VKBotNotifier.swift`) ≥ **80% line + 75% branch**
- [ ] Все 7 stories — DoD выполнен (раздел 6.5)
- [ ] `swift test 2>&1` GREEN на Linux (через build-clean.sh)
- [ ] `swift test --sanitize=thread 2>&1` GREEN (TSan, нет race conditions)
- [ ] `swift test --filter VKBridgeE2ETests 2>&1` GREEN — все 10+ E2E
- [ ] **Нет** закомментированных тестов (`#if false`, `XCTSkipIfNo*` без обоснования)
- [ ] **Нет** Mock объектов > 100 строк (раздел 6.4)

### 9.2 Регрессии

- [ ] **v0.4.0 mark-as-read** E2E — проходят без изменений
- [ ] **v0.5.0 TelegramBotNotifier** Component тесты — проходят без изменений
- [ ] DigestCore unit/component tests — green
- [ ] Smoke OpenAI summary generation — работает (если ключ есть)

### 9.3 Smoke на реальном VPS

- [ ] **v0.6.0-rc.1 на dev VPS** ≥ 3 дней без crash/restart
- [ ] **v0.6.0-rc.1 на prod VPS** ≥ 7 дней без regression
- [ ] Все 7 команд протестированы вручную с реальным VK + TG
- [ ] Audit log виден в `journalctl -u tg-client`
- [ ] systemd `Restart=always` срабатывает корректно если сервис падает

### 9.4 Документация

- [ ] `ARCHITECTURE.md` обновлён — новый таргет `VKBridge`, диаграмма pipeline v2
- [ ] `DEPLOY.md` обновлён — новые env vars, systemd unit для v0.6.0
- [ ] `README.md` обновлён — фичи v0.6.0, badge версии
- [ ] `BACKLOG.md` — отложенные фичи (AI режим, multi-tenant, Long Poll fallback)
- [ ] `CHANGELOG.md` — release entry для v0.6.0
- [ ] **Этот RFC** перемещён в `.claude/archived/v0.6.0-vk-bridge-tdd-rfc.md` (исторический артефакт)

### 9.5 Backward compatibility

- [ ] Spike `.env` файл работает с v0.6.0-binary без правок (лишние webhook vars игнорируются)
- [ ] systemd unit от spike работает с v0.6.0 после замены бинаря (ExecStart остаётся `tg-client service`)
- [ ] **nginx**: `location /vkWebHook` можно удалить (но не обязательно — он просто не используется)
- [ ] TDLib state dir переиспользуется (не cold start для пользователя)
- [ ] Long-poll цикл стартует автоматически после auth — никаких ручных шагов не нужно

### 9.6 Definition of Ready (для следующей сессии)

Этот RFC можно "закрыть как готов к старту" если:
- [ ] Разделы 1-10 заполнены (1-7 ✅, 8 ✅, 9 ✅, 10 ⏳)
- [ ] Пользователь подтвердил: branch strategy, phasing, versioning
- [ ] Создан Phase 1 backlog (раздел 7) с конкретными задачами в `.claude/TASKS.md`
- [ ] Создана новая ветка `feature/vk-bridge-tdd` от `origin/main`
- [ ] `git fetch && git pull` выполнен (актуализация main)

---

## 📋 Раздел 10 — Risks + Mitigations

> Что может пойти не так и что мы заранее продумали.

### 10.1 Технические риски

| # | Риск | Вероятность | Impact | Mitigation |
|---|---|---|---|---|
| **R-01** | TDLib SEGV (B-01, continuation leak) | 🟡 средняя | 🔴 высокий (сервис падает) | Использовать `ResponseWaiters` паттерн уже принятый в main; systemd `Restart=always` с throttle; добавить healthcheck endpoint для алерта |
| **R-02** | SwiftPM hang issue #9441 на Linux | 🟡 средняя | 🟡 средний (билды вешаются) | `./scripts/build-clean.sh` уже работает; **меньше deps в long-poll архитектуре** (без Hummingbird) — снижает риск; мониторим issue #9441 |
| **R-03** | URLSession `wait=25` long-poll не отменяется при `Task.cancel()` на Linux | 🟡 средняя | 🟡 средний (graceful shutdown зависает до 25 сек) | Mini-spike в Phase 1 проверяет это; fallback — `URLSession.invalidateAndCancel()` или wrap через `withTimeout` |
| **R-04** | VK rate limit на `groups.getLongPollServer` (часто вызывается при failed=2) | 🟢 низкая | 🟡 средний (long-poll стопорится) | `failed=2` редкое событие (раз в 24ч), не должно триггерить rate limit; backoff на любые HTTP ошибки |
| **R-05** | TDLib `viewMessages` race с одновременным `/get` от пользователя | 🟢 низкая | 🟢 низкий (auto-mark убран в v0.6.0) | `/read` делается явно, race не возникает |
| **R-06** | VK Long Poll API меняет схему JSON | 🟢 низкая | 🔴 высокий | `VKModels` через optional fields; алерт если 5+ unknown event types за час; смотрим dev.vk.com на breaking |
| **R-12** | URLSession.shared не поддерживает `wait=25` (Linux Foundation timeout < 25 сек) | 🟢 низкая | 🟡 средний | Mini-spike Phase 1; fallback — кастомный `URLSessionConfiguration.timeoutIntervalForRequest = 30` |

### 10.2 Операционные риски

| # | Риск | Вероятность | Impact | Mitigation |
|---|---|---|---|---|
| **R-07** | Disk pressure на VPS (86% used) | 🔴 высокая | 🔴 высокий (билд упадёт) | Перед каждой Phase: `du -sh .build` + cleanup; рассмотреть migration на больший VPS до Phase 2; не держать debug + release одновременно |
| **R-08** | Cutover downtime (~30 сек, обязательно — два long-poll цикла на одну group дублируют события) | 🟡 средняя | 🟢 низкий (юзер пропустит ≤30 сек обмена) | Cutover в окно низкой нагрузки; rollback скрипт готов (раздел 8.3); spike-binary backup на месте |
| **R-09** | Spike в проде падает между Phase 2 и Phase 3 | 🟡 средняя | 🔴 высокий (сервис недоступен) | План B: ускоренный cutover на v0.6.0-beta (без Phase 3 команд — `/last`, `/reply`, `/to`, `/read` временно отказывают) |
| **R-10** | TDLib auth ломается при cutover (новый stateDir не подхватился) | 🟢 низкая | 🔴 высокий (нужна re-auth с phone+code) | Использовать **тот же** `TDLIB_STATE_DIR` что в spike; backup state dir перед cutover |
| **R-11** | Обе binary (spike + v0.6.0-beta) случайно запущены одновременно — VK дублирует events | 🟡 средняя | 🟡 средний (юзер видит дублирующиеся ответы, не критично) | systemd unit `tg-client-v06.service` стартует только manually (`disabled` по умолчанию); явная инструкция в runbook |

### 10.3 Процессные риски

| # | Риск | Вероятность | Impact | Mitigation |
|---|---|---|---|---|
| **R-12** | TDD цикл занимает существенно дольше estimate | 🟡 средняя | 🟡 средний | Per-phase **time-box**: если Phase 1 > 7 дней — пересматриваем стратегию (может быть упрощаем DI, или сокращаем тесты), не переносим slip на Phase 2/3 |
| **R-13** | Соблазн "просто закоммитить spike-код, потом дотестировать" | 🔴 высокая | 🔴 высокий (вернёмся в spike-режим) | Этот RFC = контракт. RED→GREEN строго. Ревью per-PR обязательно (даже самостоятельный) |
| **R-14** | Контекст (этот разговор) истечёт между фазами | 🟡 средняя | 🟢 низкий | RFC + TASKS.md + CHANGELOG.md содержат всё нужное для рестарта; новый чат имеет full context |
| **R-15** | Изменения в `origin/main` ломают наш план (например, BotNotifierProtocol изменится в v0.5.1) | 🟢 низкая | 🟡 средний | Работаем в `feature/vk-bridge-tdd`, периодически rebase на main; conflicts ловим рано |

### 10.4 Что мы **готовы** не закрыть в этом RFC

- ✅ Не покрываем кейс multi-tenant (несколько разных VK сообществ) — out of scope
- ✅ Не покрываем VK Long Poll fallback — webhook надёжен
- ✅ Не покрываем metrics/Prometheus — есть audit log
- ✅ Не покрываем AI-режим (`/digest --ai`) — отдельная story в BACKLOG для будущего
- ✅ Не покрываем web-UI для управления — out of scope MVP

### 10.5 Эскалация

Если столкнулись с риском **🔴 высокого impact** в процессе реализации:

1. **STOP** — не продолжать без обсуждения
2. **Document** — append к этому RFC секцию `10.6 Incidents log` с описанием
3. **Decide** — обсудить с пользователем (мини-RFC внутри)
4. **Adjust** — обновить phasing если нужно

---

## 📚 Открытые вопросы — РЕШЕНЫ (2026-05-09 → 2026-05-10)

### ✅ Q1 — Branch strategy
**Решение:** Новая ветка `feature/vk-bridge-tdd` от свежего `origin/main`. Spike-ветка `feature/vk-bot-bridge` остаётся как production reference.

### ✅ Q2 — VK community для тестов
**Решение:** **Тот же** prod VK community, отдельное не нужно. В `DEPLOY.md` добавляется раздел «Получение VK creds» для open-source пользователей.

**Caveat (актуально для long-poll):** одновременно спайк + v0.6.0-beta polling одну group **запрещено** (VK дублирует events). Cutover требует ~30 сек downtime. См. раздел 8.3.

### ✅ Q3 — VPS upgrade
**Решение:** Не блокер. При нехватке диска — cleanup `.build` руками. При нехватке CPU — собирать на десктопе и копировать бинарь.

### ✅ Q4 — Time budget на spike в Phase 1
**Решение:** ОК. Бюджет: ~1ч на VK Long Poll API contract + ~30 мин на URLSession cancellation в Linux. Влезает в общий эстимейт Phase 1 (3-4 дня).

### ✅ Q5 — Auto-mark-as-read
**Решение:** **Не нужен**. Отдельная команда `/read N` (US-7) достаточна. Из RFC удалены: `VK_AUTO_MARK_READ` env var, `/get N read` суффикс, B-06 закрывается через US-7.

### ✅ Q6 — `/to_alena` shortcut
**Решение:** **Удалён**. Хардкод имени в open-source проекте — некорректно. `/to <username>` остаётся как универсальная команда.

### ✅ Q7 — Кто делает деплой
**Решение:** Claude — root на этом VPS (проверено: `whoami=root`, `sudo -l: ALL`), может все деплои сам. Cutover согласовываем текстом перед действием.

---

## 🚀 Новые открытые вопросы (specific to long-poll)

> Эти могут открыться в Phase 1 mini-spike. Перечислены чтобы было что искать.

1. **VK Long Poll `mode` параметр**: какие биты включать? По доке `mode=2|8|32|64|128` опционально. Минимум для бота `mode=2` (вложенные attachments). Уточнить в spike.
2. **`groups.getLongPollServer.access_token`** — community token достаточно или нужен `service` token? Проверить в spike.
3. **Should we use `lp_version`?** В доке упоминается версия long-poll API (текущая 12). Default стабильный, но фиксировать ли явно?
4. **`URLSession.shared` vs custom URLSession для long-poll?** Shared имеет default timeout 60 сек, наш `wait=25` влезает. Но если будут странности — выделить отдельный `URLSession` с увеличенным timeout.

---

## 📚 Ссылки

- Spike branch: `feature/vk-bot-bridge` @ `spike/vk-bridge-2026-05-09`
- Старый RFC: [.claude/vk-bot-bridge-rfc.md](vk-bot-bridge-rfc.md)
- BotNotifierProtocol в main: `Sources/DigestCore/Notifiers/BotNotifierProtocol.swift`
- TelegramBotNotifier reference: `Sources/DigestCore/Notifiers/TelegramBotNotifier.swift`
- Тесты-референс: `Tests/TgClientComponentTests/DigestCore/TelegramBotNotifierTests.swift`
- TESTING.md (TDD): `.claude/TESTING.md`
- ROLES.md: `.claude/ROLES.md`
- VK Callback API: https://dev.vk.com/ru/api/callback/getting-started
- Hummingbird: https://github.com/hummingbird-project/hummingbird
