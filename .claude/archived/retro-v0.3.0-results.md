# Результаты Ретроспективы v0.3.0

**Дата:** 2025-12-10
**Период:** 2025-12-06 → 2025-12-08 (3 дня)
**Релиз:** [v0.3.0](https://github.com/flyer2001/tg-client/releases/tag/v0.3.0)

---

## 🔄 Проверка гипотез предыдущего ретро (2024-11 + 2025-12-05)

### Гипотеза 1.2: "Mock только boundaries (FFI)" ✅ ПОДТВЕРЖДЕНА

**Из ретро 2024-11:**
> Если мокировать только FFI, Mock не ломается при рефакторинге Real.

**Метрика:** Mock стабилен 3+ рефакторинга подряд

**Проверка в v0.3.0:**
- ✅ MockTDLibFFI: 0 изменений (стабилен с 2024-11)
- ✅ MockHTTPClient: 40 строк, правило соблюдено
- ✅ TDLibClient рефакторился 3+ раза → 0 изменений в Mock
- **Целевой показатель достигнут**

**НО: Процессная проблема**
- ❌ Claude предложил MockSummaryGenerator (v0.3.0) → пользователь остановил
- Правило в TESTING.md строка 37, но **НЕ в "Критичных правилах"** (строки 7-15)

**Вывод:** Гипотеза работает, Mock'и стабильны. Проблема в процессе: правило не читается автоматически.

**Решение (принято 2025-12-10):**

Комбинация усилений в 3 местах:

1. **TESTING.md** — добавить в "Критичные правила" (строки 10-12):
   ```markdown
   6. **Mock только boundaries** — FFI/network/filesystem, НЕ internal logic
   ```

2. **ROLES.md** — чеклист для Testing Architect:
   ```markdown
   **Перед созданием Mock*.swift:**
   - [ ] СТОП: это boundary (FFI/network/filesystem)?
   - [ ] Если ДА → создаём Mock
   - [ ] Если НЕТ → используем Real в тестах
   ```

3. **/.claude/commands/endsession.md** — усилить генерацию промпта (секция 5):
   - При генерации промпта: читать ROLES.md, определить роль, включить критичные чеклисты
   - Промпт показывать ТЕКСТОМ в терминале (не файлом)
   - Включать ВЕСЬ контекст: роль + статус + следующий шаг + критичное

**Как следим (метрика v0.4.0):**
- % задач с тестированием соблюдают правило → Цель: 100%
- Trigger: создание Mock*.swift → СТОП → проверка boundary

**Статус:** Решение принято, реализация TODO (см. ниже)

---

## 📊 Метрики v0.3.0

### Код:
- **Тесты:** 146 → 205 (+59 тестов, +40%)
  - OpenAISummaryGenerator: 7 Unit tests
  - DigestOrchestrator: 6 Component tests
  - ChannelMessageSource bugfix: +2 regression tests
  - ChatList/ChatPosition models: 11 + 13 + 6 = 30 новых тестов
- **Новые модули:** OpenAISummaryGenerator, DigestOrchestrator
- **Новые файлы:** 11 (Sources + Tests)
- **Изменённые файлы:** ~25
- **Коммиты:** 16 (11 features + 4 docs + 1 bugfix)

### Баги:
- **Найдено до релиза:** 2
  1. FoundationNetworking import на macOS (быстро исправлен)
  2. 🔴 **Критический:** Архивные каналы попадают в дайджест
- **Найдено после релиза:** 0
- **Регрессионные тесты добавлено:** 2

### Процесс:
- **Research-First применён:** 2/3 (66%)
  - ✅ OpenAI API (spike перед реализацией)
  - ✅ getChatHistory offset (live эксперименты)
  - ❌ TDLib updateChatPosition (пропущен → **критический баг**)
- **TDD циклы:** 6 (OpenAISummaryGenerator, DigestOrchestrator, 3 bugfix, ChatPosition models)
- **Outside-In правильно:** 6/6 (100%)

### Качество кода:
- **TSan:** 205 тестов GREEN, 0 data races ✅
- **Cross-platform:** macOS + Linux сборки успешны ✅
- **E2E manual test:** 6 непрочитанных → 952 символа за ~10 сек ✅

### Time tracking (примерно):
- **Разработка v0.3.0:** ~12 часов (OpenAI + DigestOrchestrator + тесты)
- **Багфикс (архивные каналы):** ~8 часов (research + fix + regression tests)
- **Доля багфиксов:** 40% времени 🔴
- **SwiftPM debugging:** ~6 часов (strace + lldb + community response)

---

## 🔍 Анализ инцидентов

### 1. Дублирование функционала вместо мока (ПОВТОРНЫЙ инцидент)

**Status:** ❌ Нарушение процесса

**Root Cause:**
- Правило "Mock только boundaries" было в TESTING.md (после ретро 2024-11)
- Claude **НЕ применил правило** при проектировании DigestOrchestrator тестов
- Предложил создать MockSummaryGenerator (дублирование функционала)
- Пользователь остановил ДО реализации

**Анализ гипотез:**

✅ **Гипотеза 1:** Не перечитал TESTING.md перед задачей
→ **Подтверждена:** ROLES.md указывает читать TESTING.md для "Написание тестов", но это не было сделано автоматически

✅ **Гипотеза 2:** Нужен явный чеклист в ROLES.md
→ **Подтверждена:** Чеклист есть (раздел "Критичные правила"), но не был применён

⚠️ **Гипотеза 3:** Стоит добавить в промпт "Проверь: не дублируешь ли функционал"
→ **Частично:** Правило есть, но нужно повышать приоритет (в начало TESTING.md)

**Метрики:**
- Нарушения "Mock только boundaries": 1/1 задач с тестированием (100% 🔴)
- Пользователь поймал: 1/1 (100% ✅) — процесс review работает!

**Действие:**
- [x] Правило добавлено в TESTING.md (2025-12-05)
- [x] Ссылка добавлена в ROLES.md (Testing Architect)
- [ ] TODO: Переместить "Mock только boundaries" в **первые 30 строк** TESTING.md

---

### 2. Баг с архивными каналами (КРИТИЧЕСКИЙ)

**Status:** 🔴 Критический баг, исправлен до релиза

**Root Cause:**
- **Research-First НЕ применён** для TDLib `updateChatPosition`
- Полагались на параметр `chatList: .main` без проверки поведения
- TDLib присылает `updateNewChat` **БЕЗ** `positions` (пустое поле)
- Реальные позиции приходят отдельно в `updateChatPosition`
- Mock **НЕ эмулировал** реальное поведение (синтетические данные)

**Анализ гипотез:**

✅ **Гипотеза 1: Research-First нарушен**
→ **Подтверждена:** НЕ читали WebFetch документацию updateChatPosition до багфикса

✅ **Гипотеза 2: Component тесты с "умным" Mock**
→ **Подтверждена:** MockTDLibFFI возвращает синтетические данные, НЕ отражает TDLib протокол

✅ **Гипотеза 3: Нет manual test checklist**
→ **Подтверждена:** Manual E2E тест был сделан, но **только перед релизом** (слишком поздно)

✅ **Гипотеза 4: Не хватило краевых сценариев**
→ **Подтверждена:** Не было теста "archived channel → NOT included"

**Метрики Research-First:**
- External API методов использовано: 4 (loadChats, getChats, getChatHistory, updateChatPosition)
- Прошли Research-First: 1/4 (25% 🔴)
  - ❌ loadChats — полагались на параметр, не проверили behavior
  - ❌ updateChatPosition — не декодировали до момента бага
  - ✅ getChatHistory — live эксперименты спасли от повторного бага
  - ❌ OpenAI API — spike был, но не через WebFetch (shell script)

**Время обнаружения:**
- **Manual E2E:** сразу обнаружил (в 1-м запуске)
- **Time to fix:** ~8 часов (research + models + tests + fix)

**Действие:**
- [x] Regression tests добавлены (archived channel test)
- [x] Research-First документирован в retro-questions.md
- [ ] TODO: Добавить **обязательный чеклист** для External API в TESTING.md:
  ```
  External API → Research-First:
  1. WebFetch документация (параметры + behaviour)
  2. Failing test с краевыми сценариями
  3. Live эксперимент (если семантика неочевидна)
  4. Только потом реализация
  ```

---

### 3. Создание дубликата модели (ChatList)

**Status:** ⚠️ Обнаружено компилятором

**Root Cause:**
- Использовал `Glob` только для `**/TgClientModels/Models/*.swift` (директория не существовала!)
- НЕ сделал `Grep` по всему модулю TgClientModels
- Создал дубликат `enum ChatList` (уже существовал в `LoadChatsRequest.swift`)
- Конфликт имён обнаружен компилятором

**Анализ гипотез:**

✅ **Гипотеза 1: Нет явного правила "Grep перед Write"**
→ **Подтверждена:** В CLAUDE.md нет секции "Перед созданием типа → Grep"

✅ **Гипотеза 2: Glob по директории недостаточен**
→ **Подтверждена:** Если директория не существует, Glob возвращает 0 результатов (не показывает проблему)

✅ **Гипотеза 3: Нужен чеклист в ROLES.md**
→ **Подтверждена:** Swift Developer роль НЕ имеет чеклиста "Проверь существующие типы"

**Метрики:**
- Дубликаты созданы: 1/6 новых типов (16%)
- Обнаружено компилятором: 1/1 (100% — компилятор спас)
- Time to fix: ~15 минут (удаление дубликата + рефакторинг)

**Действие:**
- [ ] TODO: Добавить правило в CLAUDE.md:
  ```
  Перед созданием нового типа (struct/enum/class):
  1. Grep "struct TypeName" + "enum TypeName" в модуле
  2. Grep "import.*TypeName" (может быть в другом модуле)
  3. Только если NOT FOUND → создавать новый файл
  ```
- [ ] TODO: Чеклист для Swift Developer в ROLES.md

---

### 4. Manual E2E обнаружил декодирование order как String

**Status:** ✅ Процесс сработал правильно

**Root Cause:**
- TDLib присылает `order` как **String** (числа > 2^53), не как JSON number
- Модель `ChatPosition` ожидала `Int64` напрямую → decode failed
- **Manual E2E перед Unit тестами** обнаружил проблему

**Что сработало:**
- ✅ План bugfix: "Fix → E2E → Unit → Regression" (правильная последовательность)
- ✅ Manual E2E **ДО** Unit тестов → избежали "написать тесты для неправильной модели"
- ✅ WebFetch документации показал типы, но **НЕ формат JSON**

**Анализ гипотез:**

⚠️ **Гипотеза 1: Unit тест модели СРАЗУ после создания**
→ **Не нужно:** Manual E2E достаточен, Unit test пришёл позже (Шаг 4)

✅ **Гипотеза 2: Research-First должен включать JSON примеры**
→ **Подтверждена:** WebFetch показывает типы, но **НЕ формат JSON** (String vs Number)

⚠️ **Гипотеза 3: Библиотека реальных JSON примеров из debug логов**
→ **Полезно, но не критично:** Manual E2E покрыл gap

**Вывод:**
- **Текущий процесс работает:** Manual E2E → обнаружил проблему → исправили → Unit tests
- **Улучшение:** Research-First для моделей = WebFetch docs + **debug лог с реальным JSON** (если доступно)

**Time to fix:** ~20 минут (добавление decodeInt64 helper + тесты)

**Действие:**
- [x] Unit тесты для ChatPosition с реальным JSON (13 тестов)
- [ ] TODO: Уточнить в TESTING.md:
  ```
  External API модели:
  1. WebFetch docs (типы полей)
  2. Manual E2E для получения реального JSON (debug лог)
  3. Unit test модели с реальным JSON (ПОСЛЕ E2E, не ДО)
  ```

---

### 5. Преждевременное завершение дебага

**Status:** ⚠️ Процессная ошибка (поймана пользователем)

**Root Cause:**
- Claude добавил DEBUG лог для проверки `positions`
- После первого запуска (не показал архивных) сразу начал убирать DEBUG
- Фраза: "Давай уберу DEBUG и проверим что BUGFIX завершён"
- **Проблема:** "removed 0 archived" в каждом запуске должно было насторожить

**Lesson:**
- ❌ НЕ завершать дебаг пока баг **не воспроизведён и не исправлен**
- ❌ "Тесты GREEN" ≠ "Баг исправлен" (если баг в production data)

**Триггеры для остановки преждевременного завершения:**
1. "removed 0 archived" при заявленной проблеме → **продолжить дебаг**
2. Отсутствие воспроизведения бага в логах → **расширить DEBUG**
3. Пользователь явно указывает проблему → **не предполагать fix без proof**

**Метрики:**
- Попытки преждевременного завершения: 1
- Остановлено пользователем: 1/1 (100% ✅)

**Действие:**
- [ ] TODO: Добавить правило в CLAUDE.md (секция "Баг-фикс workflow"):
  ```
  Дебаг НЕ завершён пока:
  - Баг НЕ воспроизведён в логах/тестах
  - Фикс НЕ подтверждён на реальных данных
  - Пользователь НЕ подтвердил что проблема решена
  ```

---

### 6. Research-First спас от пропущенной логики getChatHistory ✅

**Status:** ✅ Успешный пример Research-First

**Контекст:**
- **v0.1.0:** Реализовали `getChatHistory` с упрощённой логикой `offset=0, fromMessageId=0`
- **Проблема:** Получали ПОСЛЕДНИЕ сообщения, не обязательно непрочитанные
- **v0.3.0:** Обнаружили баг при manual E2E test

**Решение v0.3.0 (Research-First) ✅:**
1. ✅ Создали тестовый канал @aidigestcreator
2. ✅ Live эксперименты с offset на реальном TDLib
3. ✅ WebFetch документации для уточнения семантики
4. ✅ Зафиксировали выводы: `offset=-N` → N сообщений ПОСЛЕ fromMessageId
5. ✅ Корректная логика с `lastReadInboxMessageId`:
   - `lastReadInboxMessageId=0` → `(fromMessageId=0, offset=0)`
   - `lastReadInboxMessageId>0` → `(fromMessageId=lastRead, offset=-N)`

**Lesson:**
- ✅ **External API с неочевидной семантикой → Research-First обязателен**
- ✅ **Моковые тесты НЕ ловят логические ошибки в понимании API**
- ✅ **Live эксперименты > документация + моки**
- ⚠️ **Обсуждение логики ≠ Реализация** (нужен failing test как якорь)

**Метрики:**
- External APIs с неочевидной семантикой: 2 (getChatHistory, updateChatPosition)
- Research-First применён: 1/2 (50%)
  - ✅ getChatHistory (v0.3.0 bugfix)
  - ❌ updateChatPosition (пропущен → критический баг)

**Гипотеза для проверки:**
> "Research-First для External APIs снижает риск логических ошибок с 80% до 10%"

**Проверка гипотезы:**
- До Research-First: 2 бага (getChatHistory v0.1.0, updateChatPosition v0.3.0) из 2 External APIs = **100% ошибок** 🔴
- После Research-First (getChatHistory v0.3.0): **0 ошибок** ✅
- **Гипотеза подтверждена:** Research-First снижает риск **до 0%** (при корректном применении)

**Действие:**
- [x] Правило добавлено в retro-questions.md (Вопрос 4)
- [ ] TODO: Формализовать в TESTING.md:
  ```
  External API с параметрами навигации (offset, pagination, cursor):
  1. Research-First обязателен (WebFetch + live эксперименты)
  2. Failing test с краевыми сценариями ДО реализации
  3. Test = якорь для возврата к задаче (снижает риск пропуска с 50% до 5%)
  ```

---

## ✅ Что работает

1. **Research-First (когда применяется):**
   - OpenAI API spike → избежали проблем с декодированием
   - getChatHistory live эксперименты → исправили логику v0.1.0
   - **Снижает риск ошибок с 100% до 0%**

2. **Outside-In TDD:**
   - 6/6 циклов правильно (100%)
   - Component test → RED → Unit test → GREEN → Refactor

3. **Manual E2E перед Unit тестами:**
   - Обнаружил декодирование order как String
   - Обнаружил архивные каналы в дайджесте
   - **Спасает от неправильных тестов**

4. **Пользователь как reviewer:**
   - Остановил MockSummaryGenerator до реализации ✅
   - Остановил преждевременное завершение дебага ✅
   - **Culture работает!**

5. **Cross-platform сборки + TSan:**
   - 205 тестов GREEN на macOS + Linux
   - 0 data races в TSan
   - **Стабильность высокая**

---

## ❌ Что НЕ работает

1. **Research-First не применяется автоматически:**
   - 1/4 External APIs прошли Research-First (25% 🔴)
   - Правило есть в TESTING.md, но **НЕ читается** перед задачей
   - **Root cause:** TESTING.md слишком длинный? Правило не в начале файла?

2. **"Mock только boundaries" нарушается:**
   - 1/1 задач с тестированием (100% 🔴)
   - Правило есть, но **НЕ применяется**
   - **Root cause:** Чеклист в ROLES.md есть, но не используется автоматически

3. **Нет чеклиста "Проверь существующие типы":**
   - 1/6 новых типов были дубликатами (16%)
   - Glob по несуществующей директории **НЕ показывает проблему**
   - **Root cause:** Нет явного правила в CLAUDE.md

4. **Manual E2E делается слишком поздно:**
   - Архивные каналы обнаружены только перед релизом
   - Manual E2E должен быть **во время разработки**, не в конце
   - **Root cause:** Нет workflow "TDD → Manual E2E → Regression tests"

5. **Преждевременное завершение дебага:**
   - 1 попытка завершить без воспроизведения бага
   - "Тесты GREEN" ≠ "Баг исправлен"
   - **Root cause:** Нет явного правила "НЕ завершать пока баг НЕ воспроизведён"

---

## 🔄 Изменения в процессе

### 1. Research-First для External APIs (КРИТИЧНО)

**Проблема:** 75% External APIs пропустили Research-First → критический баг

**Решение:**
- [x] Правило уже есть в TESTING.md (секция "Research-First для External APIs")
- [ ] **TODO:** Переместить в **первые 30 строк** TESTING.md
- [ ] **TODO:** Добавить в CLAUDE.md (секция "Workflow при получении задачи"):
  ```markdown
  ### Перед реализацией External API:
  1. WebFetch документация (параметры + behaviour + edge cases)
  2. Failing test с краевыми сценариями
  3. Live эксперимент (если семантика неочевидна: offset, pagination, async updates)
  4. Только потом реализация
  ```

**Метрика успеха:** 100% External APIs проходят Research-First (следующая задача)

---

### 2. Чеклист "Проверь существующие типы" перед Write

**Проблема:** 16% новых типов были дубликатами

**Решение:**
- [ ] **TODO:** Добавить в CLAUDE.md (секция "Workflow при получении задачи"):
  ```markdown
  ### Перед созданием нового типа (struct/enum/class):
  1. Grep по всему модулю: `pattern: "struct TypeName"`
  2. Grep по всему модулю: `pattern: "enum TypeName"`
  3. Grep импорты: `pattern: "import.*TypeName"`
  4. Только если NOT FOUND → создавать новый файл
  ```
- [ ] **TODO:** Добавить чеклист в ROLES.md (Swift Developer):
  ```markdown
  **Перед Write нового файла:**
  - [ ] Grep существующие типы в модуле
  - [ ] Grep импорты в других модулях
  - [ ] Проверка: тип НЕ существует
  ```

**Метрика успеха:** 0% дубликатов (следующая задача)

---

### 3. "Mock только boundaries" в начало TESTING.md

**Проблема:** 100% задач с тестированием нарушили правило

**Решение:**
- [x] Правило добавлено в TESTING.md (2025-12-05)
- [ ] **TODO:** Переместить секцию "Правила мокирования" в **первые 30 строк** TESTING.md
- [ ] **TODO:** Дублировать в CLAUDE.md (секция "TDD Workflow"):
  ```markdown
  ### Правила мокирования (КРИТИЧНО):
  - ✅ Mock ТОЛЬКО boundaries (FFI, network, filesystem)
  - ❌ НЕ создавай Mock для internal implementation
  - Детали: [TESTING.md#правила-мокирования]
  ```

**Метрика успеха:** 0% нарушений (следующая задача)

---

### 4. Workflow дебага: НЕ завершать без proof

**Проблема:** 1 попытка завершить без воспроизведения

**Решение:**
- [ ] **TODO:** Добавить в CLAUDE.md (секция "Баг-фикс workflow"):
  ```markdown
  ### Дебаг НЕ завершён пока:
  1. Баг НЕ воспроизведён в логах/тестах
  2. Фикс НЕ подтверждён на реальных данных
  3. Пользователь НЕ подтвердил решение проблемы

  Триггеры для продолжения дебага:
  - "removed 0 X" при заявленной проблеме с X
  - Отсутствие воспроизведения в логах
  - Пользователь указывает что проблема остаётся
  ```

**Метрика успеха:** 0 преждевременных завершений (следующая задача)

---

### 5. Manual E2E во время разработки (не в конце)

**Проблема:** Архивные каналы обнаружены только перед релизом

**Решение:**
- [ ] **TODO:** Обновить TESTING.md (секция "Outside-In TDD"):
  ```markdown
  ### Workflow:
  1. Component Test (RED)
  2. Unit Tests (RED)
  3. Implementation (GREEN)
  4. **Manual E2E test** (реальные данные, debug логи) ← НОВОЕ
  5. Regression tests (если баг обнаружен)
  6. Refactor
  ```

**Метрика успеха:** Manual E2E делается сразу после GREEN (следующая задача)

---

## 📋 Обновить документы

- [ ] **TESTING.md:**
  - Переместить "Research-First для External APIs" в первые 30 строк
  - Переместить "Правила мокирования" в первые 30 строк
  - Добавить секцию "Manual E2E во время разработки"
  - Добавить чеклист "External API → Research-First"

- [ ] **CLAUDE.md:**
  - Добавить секцию "Перед External API → Research-First"
  - Добавить секцию "Перед созданием типа → Grep существующие"
  - Добавить секцию "Баг-фикс workflow → НЕ завершать без proof"
  - Дублировать "Mock только boundaries" в TDD Workflow

- [ ] **ROLES.md:**
  - Добавить чеклист для Swift Developer: "Перед Write → Grep типы"
  - Обновить Testing Architect: ссылка на Research-First чеклист

---

## 📈 Выводы и действия

### Главные уроки:

1. **Research-First критичен для External APIs:**
   - Без Research-First: 100% багов
   - С Research-First: 0% багов
   - **Гипотеза подтверждена полностью**

2. **Manual E2E спасает от логических ошибок:**
   - Обнаружил 2 критических бага (декодирование, архивные каналы)
   - Должен быть **во время разработки**, не в конце

3. **Правила работают когда их читают:**
   - "Mock только boundaries" в TESTING.md → нарушен (не прочитан)
   - Нужно: **первые 30 строк** + дублирование в CLAUDE.md

4. **Пользователь как reviewer работает отлично:**
   - 2/2 инцидентов остановлены ДО реализации
   - Culture + explicit rules = успех

### Приоритет действий:

**🔴 Высокий приоритет (сделать в течение 1 дня):**
1. Переместить "Research-First для External APIs" в первые 30 строк TESTING.md
2. Переместить "Mock только boundaries" в первые 30 строк TESTING.md
3. Добавить в CLAUDE.md секцию "Перед External API → Research-First"

**🟡 Средний приоритет (сделать в течение 3 дней):**
4. Добавить чеклист "Grep типы перед Write" в CLAUDE.md + ROLES.md
5. Добавить workflow "Баг-фикс → НЕ завершать без proof" в CLAUDE.md
6. Обновить TESTING.md: Manual E2E во время разработки

**🟢 Низкий приоритет (backlog):**
7. Создать библиотеку реальных JSON примеров для TDLib
8. Автоматизация: pre-commit hook для проверки новых Mock* файлов

---

**Следующая проверка:** 2025-12-13 (через 3 дня после v0.4.0 start)
