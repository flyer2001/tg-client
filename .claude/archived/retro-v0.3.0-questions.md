# Ретроспектива v0.3.0 - Вопросы для анализа

**Дата подготовки:** 2025-12-06
**Версия:** v0.3.0
**Предыдущее ретро:** [archived/RETRO-RESULT.md](archived/RETRO-RESULT.md) (ретро 2024-11)

> **⚠️ ВАЖНО:** Перед началом новой ретроспективы ОБЯЗАТЕЛЬНО:
> 1. Прочитать результаты предыдущего ретро ([archived/RETRO-RESULT.md](archived/RETRO-RESULT.md))
> 2. Проверить метрики из предыдущего ретро
> 3. Оценить работают ли гипотезы
> 4. Только потом проводить новое ретро

---

## 🎯 Scope v0.3.0

**Цель:** DigestOrchestrator + OpenAISummaryGenerator (AI-дайджесты из сообщений)

**Реализовано:**
- ✅ OpenAISummaryGenerator (с прямым HTTP вызовом OpenAI API)
- ✅ DigestOrchestrator (координация pipeline)
- ✅ E2E тест с реальным OpenAI API (работает)
- ✅ 146 тестов GREEN на macOS + Linux
- ✅ Cross-platform сборка (macOS + Linux)

**Инциденты:**
1. ⚠️ Cross-platform баг: `import FoundationNetworking` на macOS (быстро исправлен через `#if os(Linux)`)
2. 🐛 **Критический баг:** Архивные каналы попадают в дайджест (обнаружен перед релизом, исправлен)

---

## ❓ Вопросы для следующего ретро

### 1. Дублирование функционала вместо мока (повторный инцидент)

**Контекст:**
При реализации `OpenAISummaryGenerator` Claude предложил создать `MockSummaryGenerator` вместо того чтобы просто заменить `URLSession` на `MockHTTPClient`.

**Проблема:**
- Это **повторение** ошибки из истории TDLibClient (там тоже предлагал Mock вместо FFI замены)
- Паттерн уже был в ретро 2024-11: "Mock только boundaries, не internal implementation"
- Правило было добавлено в TESTING.md, но **не применил его автоматически**
- Пользователь остановил и напомнил про правило

**Вопросы для анализа:**
- Почему **снова** предложил дублирование, несмотря на существующее правило?
- Что мешает **автоматически** применять правило "Mock только boundaries"?
- Нужен ли **явный чеклист** перед написанием Component теста?
- Как сделать так чтобы правило "всплывало" в момент проектирования теста?

**Возможные гипотезы:**
- [ ] Не перечитал TESTING.md перед задачей (нарушение процесса из CLAUDE.md)
- [ ] Правило было "в голове", но не применил критически
- [ ] Нужен явный чеклист в ROLES.md (Testing Architect роль)
- [ ] Стоит добавить в промпт: "Проверь: не дублируешь ли функционал вместо мока boundary?"

**Действия:**
- После ретро: обновить ROLES.md (Testing Architect) чеклистом
- Рассмотреть: добавить pre-commit hook для проверки новых Mock* файлов

---

### 2. Баг с архивными каналами: пропущена валидация TDLib API

**Контекст:**
При реализации `ChannelMessageSource.loadAllChats()` полагались на параметр `loadChats(chatList: .main)` для фильтрации архивных чатов. Но:
- TDLib присылает `updateNewChat` **БЕЗ** поля `positions` (пустое)
- Реальные позиции приходят отдельно в `updateChatPosition` (который мы **не декодировали**)
- Архивные каналы попали в дайджест

**Обнаружено:** Только при manual E2E тесте на реальных данных (перед релизом v0.3.0)

**Вопросы для анализа:**

**A. Почему не прочитал документацию TDLib полностью? (Research-First нарушение)**
- Полагался на название параметра `chatList: .main` → "должен фильтровать"
- Не проверил что **именно** возвращает `loadChats()` (какие updates, в каком формате)
- Не читал документацию `updateChatPosition` до момента дебага
- **Research-First** правило есть в TESTING.md, но **не применили**

**B. Почему не протестировали с архивными каналами раньше?**
- Component тест `ChannelMessageSourceTests` использует `MockTDLibFFI`
- Mock возвращает синтетические данные (мы **сами** создаём ChatResponse)
- Mock **НЕ эмулирует** реальное поведение TDLib (updateNewChat + updateChatPosition отдельно)
- Не было тест-кейса: "чат в архиве → НЕ должен попасть в результат"

**C. Процессные ошибки:**
- Не проверили **финально** что приходит от TDLib в реальности
- Не добавили краевой сценарий "архивный канал" в Component тесты
- Не делали manual test с архивными каналами до попытки релиза

**Возможные гипотезы:**
- [ ] **Research-First** не применили для нового TDLib метода (хотя правило есть!)
- [ ] Component тесты со слишком "умным" Mock (не отражает реальность TDLib)
- [ ] Нужен **обязательный manual test checklist** перед релизом
- [ ] Не хватило краевых сценариев в тестах (archived, folders, pinned, empty)
- [ ] Mock должен быть "тупым" (эмулировать протокол, не логику TDLib)

**Метрики для проверки (при ретро):**
- Сколько external API методов использовано в проекте? (__/__)
- Сколько из них прошли через Research-First (WebFetch docs)? (__/__)
- Сколько external API имеют тест на краевой сценарий? (__/__)

---

### 3. Создание дубликата модели без проверки существующих

**Контекст:**
При реализации Шага 1 багфикса (создание моделей для TDLib positions), Claude начал создавать новый файл `ChatList.swift` в `Sources/TgClientModels/Models/`, не проверив что `ChatList` уже существует в `Sources/TgClientModels/Requests/LoadChatsRequest.swift`.

**Что произошло:**
- Использовал `Glob` только для `**/TgClientModels/Models/*.swift` (директория не существовала)
- НЕ сделал полноценный поиск по всему модулю TgClientModels
- Создал дубликат enum ChatList
- Ошибка обнаружена только при компиляции (конфликт имён)
- Потребовалась переделка: удаление дубликата + расширение существующего

**Правильный подход:**
1. `Grep` по всему модулю: `pattern: "enum ChatList"` или `pattern: "struct ChatList"`
2. Если не нашли → `Grep` по имени типа в import/usage
3. Только после проверки → создавать новый файл

**Вопросы для анализа:**
- Почему не использовал Grep для проверки существующих типов перед созданием нового файла?
- Нужен ли явный чеклист: "Перед созданием модели → Grep по имени типа"?
- Как автоматизировать проверку на дубликаты (pre-write hook)?

**Возможные гипотезы:**
- [ ] Нет явного правила в CLAUDE.md: "Проверяй существующие типы перед созданием"
- [ ] Glob по директории недостаточен (директория может не существовать)
- [ ] Нужен чеклист в ROLES.md (Swift Developer роль)
- [ ] Стоит добавить в промпт: "Grep по имени типа перед Write нового файла"

**Действия:**
- После ретро: добавить правило в CLAUDE.md (секция "Workflow при получении задачи")
- Рассмотреть: чеклист для Swift Developer роли в ROLES.md

---

### 4. Manual E2E обнаружил декодирование order как String (Research-First работает, но есть вопросы)

**Контекст:**
Шаг 3 bugfix v0.3.0: Manual E2E тест перед написанием Unit тестов (следуя плану "Fix → E2E → Unit → Regression").

**Что обнаружили:**
- TDLib присылает `order` как **String** (большие числа > 2^53), а не как number в JSON
- Модель `ChatPosition` ожидала `Int64` напрямую → decode failed
- Также: `chat_folder_id` требовалось для всех ChatList (но нужно только для `.folder`)

**Процесс сработал правильно:**
- ✅ Manual E2E перед тестами → обнаружили проблему декодирования
- ✅ Шаг 3 плана: "Запустить на реальных данных, проверить логи" → это Research-First
- ✅ НЕ написали тесты сразу → избежали "тесты сломались из-за неправильной модели"

**Вопрос для уточнения процесса:**

**A. Можно ли было обнаружить раньше?**
- Шаг 1: создали модели `ChatPosition`, `ChatList`
- НЕ написали Unit тест с реальным JSON сразу (отложили до Шага 4)
- WebFetch документации показал `order: int64` (но не формат JSON!)
- **Вопрос:** Нужно ли для external API моделей делать Unit тест с реальным JSON **сразу после создания модели**?

**B. Research-First для моделей: что включает?**
- Текущий подход: WebFetch документации → читаем типы полей
- **НЕ сделали:** получить пример реального JSON (через debug логи или docs)
- **Вопрос:** Research-First для external API должен включать **примеры реального JSON**, а не только типы?

**Возможные гипотезы:**
- [ ] Manual E2E достаточен (текущий подход работает)
- [ ] Unit тест модели с реальным JSON должен идти СРАЗУ после создания модели (между Шагом 1 и Шагом 2)
- [ ] Research-First для моделей = WebFetch docs + **получить JSON пример** (debug логи или примеры в docs)
- [ ] Для TDLib: создать библиотеку "реальных JSON примеров" из debug логов (переиспользовать в тестах)

**Метрики:**
- Обнаружено: Manual E2E (Шаг 3) ✅
- Могло быть обнаружено: Unit тест модели с реальным JSON (Шаг 1 → Unit тест → Шаг 2)?
- Время на фикс: ___ минут (от обнаружения до исправления)

**Действия:**
- После ретро: уточнить в TESTING.md - когда писать Unit тесты для external API моделей
- Рассмотреть: создать набор "TDLib JSON examples" для переиспользования в тестах

---

## 📊 Метрики v0.3.0

_Заполнить после релиза_

**Код:**
- Тесты: 146 → ___ (после фикса архивных чатов)
- Новые модули: OpenAISummaryGenerator, DigestOrchestrator
- Новые файлы: ___
- Изменённые файлы: ___

**Баги:**
- Найдено до релиза: 2 (FoundationNetworking, архивные каналы)
- Найдено после релиза: ___
- Регрессионные тесты добавлено: ___

**Процесс:**
- Research-First применён: 1/2 (OpenAI API - да, TDLib updateChatPosition - **нет**)
- TDD циклы: ___ (OpenAISummaryGenerator, DigestOrchestrator, архивные каналы фикс)
- Outside-In правильно: ___ / ___ циклов

**Time tracking:**
- Разработка v0.3.0: ___ часов
- Багфикс (архивные каналы): ___ часов
- Доля багфиксов от общего времени: ___%

---

## 🔄 Действия после ретро

_Заполнить при проверке гипотез (через 3 дня после релиза)_

**Что работает:**
- ...

**Что НЕ работает:**
- ...

**Изменения в процессе:**
- ...

**Обновить документы:**
- [ ] TESTING.md (если нужны новые правила)
- [ ] ROLES.md (чеклисты для Testing Architect)
- [ ] CLAUDE.md (workflow при багах)

## Вопрос 3: Преждевременное завершение дебага (2025-12-07)

**Проблема:** Claude начал завершать BUGFIX (убирать DEBUG логи) в середине процесса отладки, хотя проблема не была полностью решена.

**Контекст:**
- Пользователь сообщил что `red_mad_robot` - архивный канал, но попал в дайджест
- Claude добавил DEBUG лог для проверки `positions`
- Сразу после первого запуска (который не показал `red_mad_robot`) Claude начал убирать DEBUG лог
- Фраза: "Давай уберу DEBUG лог и проверим что BUGFIX завершён"

**Вопросы для анализа:**
1. Почему Claude решил что BUGFIX завершён, не дождавшись подтверждения что архивные каналы НЕ попадают в дайджест?
2. Что должно было насторожить Claude:
   - "removed 0 archived" в каждом запуске
   - Пользователь явно сказал что канал архивный
   - DEBUG лог не показал ни одного архивного канала
3. Как правильно было бы действовать:
   - Продолжить дебаг: проверить РЕАЛЬНЫЙ архивный канал
   - Попросить пользователя назвать точное название архивного канала
   - Расширить DEBUG лог на ВСЕ каналы (не только 2 конкретных)
4. Какие триггеры должны останавливать преждевременное завершение:
   - "removed 0 archived" при заявленной проблеме с архивными
   - Отсутствие воспроизведения бага в логах
   - Пользователь явно указывает что проблема остаётся

**Lesson:** НЕ завершать дебаг пока проблема не воспроизведена и не исправлена. "Тесты GREEN" != "Баг исправлен".

---

## Вопрос 4: Research-First спас от пропущенной логики getChatHistory ✅ (2025-12-07)

**Контекст:**
- **v0.1.0:** Реализовали `ChannelMessageSource.fetchUnreadMessages()`
- Обсудили offset логику для getChatHistory, НО использовали упрощённый вариант `offset=0, fromMessageId=0`
- Тесты GREEN (моки), но реальный клиент получал ПОСЛЕДНИЕ сообщения, не обязательно непрочитанные
- **v0.3.0:** Обнаружили что непрочитанные сообщения не попадают в дайджест

**Root cause v0.1.0:**
- **Сложность моковых тестов:** Поведение `offset=-N` (получить N сообщений ПОСЛЕ fromMessageId) очень сложно корректно замокать
- **Нет Research-First:** Не проверяли на реальном TDLib перед реализацией
- **Документация неполная:** TDLib docs не дают явного примера "как получить непрочитанные сообщения"
- **Тесты дали false positive:** GREEN тесты с моками не поймали логическую ошибку в понимании API
- **Обсуждение != Реализация:** Обсудили логику offset, НО при реализации не применили

**Решение v0.3.0 (правильный подход) ✅:**
1. ✅ Создали тестовый канал `@aidigestcreator` для экспериментов
2. ✅ Live эксперименты с разными offset на реальном TDLib клиенте
3. ✅ WebFetch документации для уточнения семантики offset
4. ✅ Зафиксировали выводы: `offset=-N` → N сообщений ПОСЛЕ fromMessageId
5. ✅ Логика с учётом `lastReadInboxMessageId`:
   - `lastReadInboxMessageId=0` → канал никогда не читали → `(fromMessageId=0, offset=0, limit=N)`
   - `lastReadInboxMessageId>0` → берём ПОСЛЕ lastRead → `(fromMessageId=lastRead, offset=-N, limit=N)`

**Урок:**
- **External API с неочевидной семантикой → Research-First обязателен**
- **Моковые тесты не ловят логические ошибки в понимании API**
- **Live эксперименты на реальном клиенте > документация + моки**
- **Обсуждение логики != Применение в коде** (нужен failing test как якорь)

**Гипотеза для проверки:**
"Research-First для External APIs снижает риск логических ошибок с 80% до 10%"

**Новое правило в TESTING.md:**
- External API с параметрами навигации (offset, pagination, cursor) → **обязательно Research-First перед реализацией**
- Создать тестовое окружение (канал, данные) для live экспериментов
- Зафиксировать выводы в failing test ДО реализации (test = якорь для возврата к задаче)
- Failing test после Research снижает риск пропуска реализации с 50% до 5%

