# Ретроспектива v0.4.0 — Отслеживание метрик

**Период разработки v0.4.0:** TBD
**Дата релиза:** TBD

---

## 🔄 ПРОМПТ ДЛЯ РЕГУЛЯРНОГО ОТСЛЕЖИВАНИЯ (раз в 1-3 дня)

**Скопируй и запусти этот промпт в новой сессии:**

```
Дата: ____

Отслеживание метрик v0.4.0 (раз в 1-3 дня).

**Роль:** AI-Assisted Developer

**Читать:**
1. .claude/archived/retro-v0.4.0-questions.md (этот файл)
2. .claude/archived/retro-v0.3.0-results.md (контекст целевых метрик)

**Задача:**
Проверить соблюдение правил и метрик за последние 1-3 дня:

1. **Research-First для External APIs:**
   - Были ли новые External APIs?
   - Research-First применён? (WebFetch + live эксперименты + failing test)

2. **Mock только boundaries:**
   - Были ли новые Mock*.swift?
   - Правило соблюдено? (boundary: FFI/network/filesystem)

3. **Code Review вчерашних коммитов:**
   - Проводился ли review? (первая сессия дня + коммиты за вчера)
   - Проблемы найдены? (критичные/желательные/опционально)

4. **Дубликаты типов:**
   - Были ли новые типы (struct/enum/class)?
   - Grep выполнен перед созданием?

5. **Преждевременное завершение дебага:**
   - Были ли баг-фиксы?
   - Попытки завершить без воспроизведения?

6. **Concurrency компоненты:**
   - Были ли новые concurrency компоненты?
   - Architecture-First чеклист заполнен?
   - TSan запущен?

**Формат вывода:**
Append лог в секцию "Логи метрик" этого файла (retro-v0.4.0-questions.md).

Формат:
---
## Лог метрик — [Дата]

**Research-First:** X/Y External APIs (100% = целевой показатель)
**Mock только boundaries:** соблюдено/нарушено
**Code Review:** проведён/пропущен
**Дубликаты типов:** 0 дубликатов (целевой показатель)
**Преждевременное завершение:** 0 попыток (целевой показатель)
**Concurrency:** Architecture-First применён (ДА/НЕТ), TSan GREEN/warnings

**Инциденты:**
- [описание если были]

**Нарушения правил:**
- [описание если были + root cause]

**Выводы:**
- Работает: [что работает]
- Не работает: [что не работает]
---

Давай проверим метрики!
```

---

## 📚 Контекст: Целевые метрики v0.4.0

**Из ретро v0.3.0 ([retro-v0.3.0-results.md](retro-v0.3.0-results.md)):**

### Проблемы v0.3.0:
- **Research-First:** 25% (1/4 External APIs) 🔴
- **Mock только boundaries:** 0% соблюдений (1/1 нарушение) 🔴
- **Дубликаты типов:** 16% (1/6 новых типов) 🔴
- **Доля багфиксов:** 40% времени 🔴

### Целевые метрики v0.4.0:
1. **Research-First:** 100% External APIs (было 25%)
2. **Mock только boundaries:** 100% соблюдений (было 0%)
3. **Дубликаты типов:** 0% (было 16%)
4. **Преждевременное завершение дебага:** 0 попыток (было 1)
5. **Code Review:** 100% дней с коммитами (новая метрика)
6. **TSan учения:** 3/3 ошибок обнаружены (новая метрика)
7. **Concurrency компоненты:** 0 рефакторингов из-за concurrency ошибок (новая метрика)

---

## 🎯 Гипотезы для проверки в v0.4.0

### Гипотеза 1.3: "Overnight Pause" (Code Review)
**Цель:** Обнаружить критичную проблему ДО production через свежий взгляд

**Метрика:** % дней с коммитами → code review в первой сессии следующего дня

**Проверена если:**
- Code review обнаружил критичную проблему ДО production
- ИЛИ 5+ reviews прошли без критичных находок (процесс работает)

---

### Гипотеза "Concurrency-Aware Architecture" (1.1 + 1.4 + 1.5)
**Цель:** Обнаружить concurrency проблемы на этапе design, не после 50+ коммитов

**Метрика:**
- Architecture-First применён ДО реализации (чеклист 7 блоков)
- TSan обязателен для concurrency компонентов
- 0 рефакторингов из-за concurrency ошибок

---

## 📝 Логи метрик (append сюда после каждой проверки)

<!-- Логи добавляются через `echo "---" >> файл` или Edit append -->


---

## 🔴 Инцидент — 2025-12-11 (начало v0.4.0)

**Тип:** Нарушение TDD процесса (пропущен User Story документ)

**Что произошло:**
1. Начали реализацию v0.4.0 (MarkAsReadService) сразу с DocC документации в коде
2. Пропущен шаг: создание E2E User Story документа (как FetchUnreadMessages.md)
3. Обнаружено также: отсутствует User Story для v0.3.0 (GenerateSummary)

**Последствия:**
- Несоответствие процессу TDD (User Story → E2E → Component → Unit)
- Устаревшая документация на сайте (v0.2.0 вместо v0.3.0)
- Риск: начать реализацию без чёткого понимания user value

**Root Cause:**
- Нет чеклиста "перед началом новой версии"
- TDD workflow в TESTING.md фокусируется на тестах, но НЕ упоминает User Story документ

**Корректирующие действия:**
1. ✅ Восстановить пропущенные User Story:
   - GenerateSummary.md (v0.3.0)
   - MarkAsRead.md (v0.4.0)
2. ✅ Убрать упоминание версии из документации (TgClient.md)
3. ⏳ Добавить в TESTING.md секцию "Workflow новой версии" (чеклист)
4. ⏳ Сгенерировать и опубликовать DocC документацию

**Предотвращение в будущем:**
- Добавить в TESTING.md чеклист "перед началом новой версии":
  ```
  ☐ Research-First (если новый External API)
  ☐ Architecture-First (если concurrency/performance критичен)
  ☐ RFC в MVP.md
  ☐ User Story документ (E2E-Scenarios/*.md) ← НОВОЕ
  ☐ ТОЛЬКО ПОТОМ → Component Test (RED)
  ```

**Метрика для отслеживания:**
- "User Story документ создан ДО начала реализации" (100% соблюдение)

**Статус:** ✅ Исправлено (документы созданы, задача в TASKS.md)


---

## 🔴 Инцидент — 2025-12-11 (spike viewMessages)

**Тип:** Недокументированное требование (TDLIB_DATABASE_ENCRYPTION_KEY)

**Что произошло:**
1. Запуск E2E spike теста для viewMessages
2. TDLib упал с ошибкой "Wrong padding length" при авторизации
3. Root Cause: encryption key длиной 23 байта (не кратно 16 для AES)

**Последствия:**
- E2E тест зависает на этапе авторизации
- Пользователь не может запустить приложение/тесты без валидного ключа
- Требование не документировано в .env.example, README, DEPLOY.md

**Root Cause:**
- Нет документации требований к TDLIB_DATABASE_ENCRYPTION_KEY
- Нет валидации длины ключа при старте приложения
- .env.example не содержит пример валидного ключа

**Корректирующие действия:**
1. ✅ Сгенерирован валидный ключ (32 байта hex, 64 символа)
   - Формат: hex (0-9a-f) вместо base64 (проблемы с `/`, `+`, `=` в .env)
   - Команда: `openssl rand -hex 32`
2. ✅ Задача добавлена в BACKLOG.md
3. ⏳ Документировать требование в .env.example
4. ⏳ Добавить валидацию в TDConfig.forTesting()
5. ⏳ Документировать в DEPLOY.md

**Предотвращение в будущем:**
- Добавить валидацию обязательных env переменных при старте
- Валидировать формат/длину критичных параметров (encryption key, API credentials)

**Метрика для отслеживания:**
- "Env переменные валидируются при старте" (новое требование)

**Задача:** См. BACKLOG.md "Валидация и документация TDLIB_DATABASE_ENCRYPTION_KEY"

**Статус:** ✅ Исправлено

**Дополнительная находка (2025-12-12):**
- Баг в `main.swift` — не передавал `databaseEncryptionKey` в TDConfig
- Исправлено: main.swift:28,43 — читает из `env["TDLIB_DATABASE_ENCRYPTION_KEY"]`
- Spike test успешен после исправления

