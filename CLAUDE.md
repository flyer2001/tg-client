# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

## 🎯 Цель проекта

CLI-клиент Telegram: логин → чтение непрочитанных → саммари через внешний AI → отправка результата через бота

---

## 📋 СТАТИЧНЫЕ ПРАВИЛА (кэшируемая секция)

### Неснимаемые правила

- **Пишем код по TDD** (детали в [TESTING.md](.claude/TESTING.md)). Сборка на Linux обязательна.
- Swift 6; зависимости ≤ 6 мес.; минимум внешних библиотек.
- Логи на каждом внешнем вызове (Telegram, AI, Bot). Секреты только из env.
- Изменения > 2 файлов или смена архитектуры — сначала план/RFC.

### Правила для ассистента

- Нужны деплой/SSH/env? Иди в документы ниже. Секреты в код не добавлять.
- Публичные контракты не менять без тестов. Если правок много — сначала план.
- По умолчанию отвечай кратко (6–10 строк); разворачивай детали только по запросу.
- **Роли**: используй роли из [PROMPTS.md](.claude/PROMPTS.md) в зависимости от типа задачи (Architect для проектирования, Testing Architect для тестов, Swift Developer для кода).
- **Git коммиты**: строго следуй [DEVELOPMENT.md](.claude/DEVELOPMENT.md) (на русском, БЕЗ "Co-Authored-By: Claude").
- **Перед коммитом**: раздели коммиты (Sources/, Tests/) отдельно, документация отдельно; актуализируй TASKS.md и предложи добавить запись в CHANGELOG.md (prepend через bash). Если критичный контекст — обнови DEPLOY/SETUP/ARCHITECTURE/TROUBLESHOOTING.
- **⚠️ КРИТИЧНО: Безопасность работы с git:**
  - **ЗАПРЕЩЕНО** использовать `git reset --hard`, `git clean -fd` или другие деструктивные команды БЕЗ явного предупреждения пользователю
  - Перед деструктивной операцией ОБЯЗАТЕЛЬНО: показать `git status`, перечислить ЧТО ИМЕННО будет потеряно, спросить подтверждение
  - Если есть unstaged/uncommitted изменения → сначала спросить пользователя что с ними делать (stash, commit, discard)
  - Для отката изменений использовать `git stash` вместо `git reset --hard` (stash можно восстановить)
- **⚠️ КРИТИЧНО: Проверка окружения перед сборкой:**
  - В начале сессии ВСЕГДА проверяй `Platform` из `<env>` тега
  - **Linux** → ОБЯЗАТЕЛЬНО используй `./scripts/build-clean.sh` (НЕ `swift build`)
  - **macOS** → можно `swift build` напрямую
  - **Причина**: на Linux SwiftPM зависает без kill процессов и очистки .build
- **⚠️ КРИТИЧНО: НИКОГДА не используй pipe с swift test:**
  - **ЗАПРЕЩЕНО:** `swift test | head`, `swift test | tail`, `swift test | grep`
  - **Причина:** SIGPIPE убивает swift test и вызывает зависание
  - **Правильно:** `swift test 2>&1` (без pipe) или сохранить в файл: `swift test 2>&1 | tee /tmp/test.log`

#### Workflow при получении задачи (TDD)

Когда пользователь даёт задачу типа "Добавь метод X" или "Реализуй фичу Y":

**Шаг 1: Уточнить контекст (НЕ кодить сразу!)**
- Спроси: "На какой существующий модуль/сценарий ориентироваться?"
- (Или если очевидно: "Буду следовать паттерну из [модуль X], ок?")

**Шаг 2: Найти документацию (если TDLib метод)**
- WebFetch на `core.telegram.org/tdlib/docs` для нужного метода
- Покажи что нашёл (параметры, response структура)

**Шаг 3: Определить природу задачи (КРИТИЧНО - СПРОСИ ЕСЛИ СОМНЕВАЕШЬСЯ!)**

Задай себе вопрос: "С точки зрения **пользователя** (не разработчика), это:"
- **A) Новая цель/действие?** → Новый E2E сценарий
- **B) Деталь существующего действия?** → Component/Unit Test (часть существующего E2E)
- **C) Техническая оптимизация?** → Component/Unit Test (БЕЗ E2E)

**Примеры проверки:**
- ✅ "Получить непрочитанные" → Новый E2E (пользователь хочет ЭТО)
- ❌ "Подписаться на updates" → Component Test (техническая деталь HOW, часть E2E "Получить непрочитанные")
- ❌ "AsyncStream для TDLib" → Component Test (чисто техническое, пользователю без разницы)

**ЕСЛИ СОМНЕВАЕШЬСЯ:**
- 🚨 **ОБЯЗАТЕЛЬНО спроси пользователя:** "Это новая user story (E2E) или часть существующего сценария [название]?"
- НЕ начинай писать код до получения ответа
- НЕ создавай E2E документ "на всякий случай"

**Шаг 4: Определить TDD подход (Outside-In vs Inside-Out)**

Если это Component/Unit Test (не новый E2E), определи подход:

**Outside-In** когда:
- Есть **явный consumer** (вызывающий код) - E2E сценарий или другой компонент
- Не знаешь точный интерфейс до начала проектирования
- Примеры: UpdatesHandler (consumer = ChannelMessageSource), MessageFetcher

**Inside-Out** когда:
- **НЕТ явного consumer** - просто модели данных
- Контракт очевиден из бизнес-логики
- Примеры: SourceMessage, DigestSummary, ChannelInfo

**Правило:** В проекте почти всё Outside-In (от требований consumer) на разных уровнях!

**Шаг 5: ОБЯЗАТЕЛЬНО прочитать документацию перед тестами**

⚠️ **КРИТИЧНО: НЕ пиши тесты без чтения этих файлов!**

1. **Read .claude/TESTING.md**
   - Правила TDD (Outside-In, декомпозиция ТОЛЬКО после попытки)
   - Антипаттерны (придумывание Mock API до реализации)
   - Примеры правильных/неправильных тестов

2. **Read .claude/PROMPTS.md**
   - Найти роль **Testing Architect** для Component/Unit тестов
   - Применить роль при написании теста

**Шаг 6: Senior Architect проверка (если новый компонент)**

⚠️ **КРИТИЧНО:** При проектировании нового функционала/фичи/метода TDLibClient/внутренней абстракции:

1. **Read [PROMPTS.md](.claude/PROMPTS.md)** → роль **Senior Swift Architect**
2. **Read [ARCHITECTURE.md](.claude/ARCHITECTURE.md)** → раздел **"Architecture Decision Records (ADR)"**
3. **Проговори архитектурные решения ВСЛУХ** (4 блока: производительность, память, отказоустойчивость, логирование)
4. **Сохрани анализ:**
   - Сложный компонент (>100 строк) → новый ADR в ARCHITECTURE.md
   - Простой helper → DocC комментарий в коде

**Золотое правило:** Проектируй для будущего, реализуй для MVP

**НЕ НАЧИНАЙ реализацию без явного архитектурного анализа!**

**Шаг 7: Предложить E2E сценарий (только если это новая user story)**
- "Начнём с пользовательского сценария. Предлагаю такой E2E: [описание]"
- Жди подтверждения/правок

**Шаг 8: Идти по Outside-In TDD (см. TESTING.md)**
- E2E сценарий → Component Test → Unit Tests → Models → Implementation → Mock → GREEN → Refactor
- **На каждом этапе делай паузу и спрашивай: "Продолжаем дальше?"**
- НЕ делай всё за раз без пауз!

### Архитектура MVP

5 core модулей: MessageSource, SummaryGenerator, BotNotifier, StateManager, DigestOrchestrator.

Детали: [MVP.md](.claude/MVP.md), [ARCHITECTURE.md](.claude/ARCHITECTURE.md)

### Быстрые команды (для активной разработки)

```bash
# ⚠️ ВАЖНО: Сборка на Linux (Swift 6.2)

# ЕДИНСТВЕННЫЙ РАБОЧИЙ ВАРИАНТ (~50-60 сек):
./scripts/build-clean.sh
# - Убивает зависшие процессы Swift
# - Очищает кеш SwiftPM (purge-cache + reset)
# - Полная пересборка каждый раз
# - ПРИЧИНА: SwiftPM 6.2 зависает при инкрементальных сборках на Linux

# macOS: можно swift build напрямую (инкрементальная сборка работает ~5-10 сек)

# Тесты
# ⚠️ НЕ используй pipe (| head/tail) с swift test - вызывает SIGPIPE зависание!
swift test --verbose 2>&1                    # Правильно
swift test --filter MyTest --verbose 2>&1    # С фильтром

# Запуск приложения
swift run tg-client
```

Подробнее см. [SETUP.md](.claude/SETUP.md)

---

## 🔄 ДИНАМИЧЕСКИЕ ИНСТРУКЦИИ (в начале сессии)

**🚀 НАЧАЛО НОВОЙ СЕССИИ (Development Flow):**

1. `git fetch && git status` - проверить актуальность кода (разработка идёт на desktop + server)
2. Если origin/main впереди → выполни `git pull`
3. **Анализ состояния:**
   - **Если staged changes есть** → это WIP (work-in-progress):
     - Прочитай TASKS.md для контекста текущей задачи
     - Предложи: "Вижу staged changes по задаче X. Продолжаем работу?"
     - Коммиты будут сделаны позже (не предлагай автоматически)
   - **Если staged changes нет** → прочитай TASKS.md и предложи новую задачу

**⚠️ ФОКУС НА MVP:**
Все новые задачи направлены на достижение MVP. Детали scope — по требованию в [MVP.md](.claude/MVP.md).

---

## 📚 СТРАТЕГИЯ ЧТЕНИЯ ДОКУМЕНТАЦИИ

**Читать в начале сессии (кэшируются на 5+ минут):**
- **[ARCHITECTURE.md](.claude/ARCHITECTURE.md)** - архитектура, модули, паттерны (стабильно)
- **[DEVELOPMENT.md](.claude/DEVELOPMENT.md)** - правила разработки (стабильно)
- **[TESTING.md](.claude/TESTING.md)** - стратегия тестирования (стабильно)
- **[PROMPTS.md](.claude/PROMPTS.md)** - роли для разных типов задач (стабильно)

**Читать только по требованию:**
- **[TASKS.md](.claude/TASKS.md)** - текущие задачи (часто меняется)
- **[CHANGELOG.md](.claude/CHANGELOG.md)** - история сессий (⚠️ только prepend через bash)
- **[MVP.md](.claude/MVP.md)** - цели и scope MVP (по требованию)
- **[IDEAS.md](.claude/IDEAS.md)** - бэклог для версий после MVP (по требованию)
- **[SETUP.md](.claude/SETUP.md)** / **[CREDENTIALS.md](.claude/CREDENTIALS.md)** / **[DEPLOY.md](.claude/DEPLOY.md)** - инфраструктура (по требованию)
- **[TROUBLESHOOTING.md](.claude/TROUBLESHOOTING.md)** - частые проблемы (по требованию)

---

## 🎛️ УПРАВЛЕНИЕ КОНТЕКСТОМ И ТОКЕНАМИ

### Отслеживание бюджета токенов

- **Мониторинг**: отслеживай token usage в system warnings на каждом шаге
- **Алерт на 80%**: при достижении 150k токенов (80% из 190k) — предупреди пользователя и предложи:
  - Завершить текущую задачу
  - Суммаризовать прогресс перед новой сессией
  - Зафиксировать изменения в git
- **Критичный порог 90%**: при 170k токенов (90%) — ОБЯЗАТЕЛЬНО предупреди и завершай работу

### Оптимизация bash-логов

**Проблема:** логи от `swift build`, `swift test`, `git log` могут занимать 2-5k токенов.

**Правила обработки логов:**
- **Успешные команды:** показывай только итоговую строку (например, "Build complete!")
- **Команды с большим выводом:** используй `| head -50` или `| tail -50` для ограничения
- **Ошибки компиляции:**
  - Показывай первые 20 строк ошибок
  - Последние 10 строк (часто там root cause)
  - НЕ вставляй весь лог в контекст
- **Длинные логи (>100 строк):**
  - Сохраняй в временный файл: `/tmp/build.log`, `/tmp/test.log`
  - Анализируй файл отдельно
  - В контексте держи только краткую сводку (5-10 строк) + путь к файлу

**Примеры команд:**
```bash
swift build 2>&1 | tee /tmp/build.log | tail -20  # Сохранить полный лог, показать концовку
swift test 2>&1 | head -50                          # Ограничить вывод теста
git log --oneline -10                               # Короткая история вместо полной
```

### Работа с файлами кода

**Редактирование файлов:**
- **Точечные изменения (<10 строк):** используй `str_replace` вместо показа всего файла
- **Средние правки (10-50 строк):** используй `str_replace` с минимальным контекстом
- **Крупные рефакторинги (>50% файла):** покажи полный файл, но предупреди о расходе токенов

**Просмотр файлов:**
- **Используй view_range:** вместо чтения всего файла читай нужные строки: `view(path, view_range=[10, 50])`
- **Не дублируй код:** если уже показывал файл в сессии — ссылайся на него, а не показывай снова
- **Для больших файлов (>200 строк):** сначала покажи структуру (классы/функции), потом детали по запросу

### Стратегия сжатия контекста

Когда контекст растёт, применяй:

1. **Суммаризация завершённых задач:**
   - После завершения подзадачи — кратко опиши что сделано (2-3 строки)
   - НЕ храни всю переписку о решённой проблеме

2. **Ссылки вместо копирования:**
   - Вместо "вот код из файла X" → "см. файл X, строки Y-Z"
   - Вместо "вот результат теста" → "все тесты прошли (см. /tmp/test.log)"

3. **Удаление устаревшей информации:**
   - Если bash-лог устарел (команда выполнена заново) — не ссылайся на старый вывод
   - Если план изменился — не держи старые варианты в истории

### Когда создавать новую сессию

Рекомендуется начать новую сессию:
- ✅ После завершения крупной задачи (новый модуль/фича готова)
- ✅ При достижении 170k токенов (90% бюджета)
- ✅ При смене фокуса работы (переход к другому архитектурному слою)
- ✅ После крупного рефакторинга (чтобы начать с чистого контекста)

Перед началом новой сессии:
- Зафиксируй все изменения в git
- Обнови TASKS.md с текущим прогрессом
- Добавь запись в CHANGELOG.md (prepend)

---

## 🔍 ОТЛАДКА И ЛОГИРОВАНИЕ

### Уровни детализации ответов

- **По умолчанию:** краткий ответ (6-10 строк), затем спроси "нужны детали?"
- **При ошибках:** покажи суть проблемы, предложи 2-3 решения, детали — по запросу
- **При успехе:** подтверди результат (1-2 строки), не расписывай что уже очевидно

### Формат технических деталей

Когда пользователь просит детали или технический разбор:
- Структурируй ответ по шагам
- Используй минимум markdown (только заголовки, код, списки где уместно)
- Избегай излишнего форматирования (bold, курсив только для акцентов)

---

## 📦 ПОЛНЫЙ СПИСОК ДОКУМЕНТАЦИИ

Вся документация находится в папке `.claude/`:

**Читать в начале сессии:**
- [ARCHITECTURE.md](.claude/ARCHITECTURE.md)
- [DEVELOPMENT.md](.claude/DEVELOPMENT.md)
- [TESTING.md](.claude/TESTING.md)

**По требованию:**
- [TASKS.md](.claude/TASKS.md) - текущие задачи
- [MVP.md](.claude/MVP.md) - scope MVP
- [IDEAS.md](.claude/IDEAS.md) - бэклог после MVP
- [CHANGELOG.md](.claude/CHANGELOG.md) - история (⚠️ НЕ читать без запроса)
- [SETUP.md](.claude/SETUP.md) - настройка окружения
- [CREDENTIALS.md](.claude/CREDENTIALS.md) - управление секретами
- [DEPLOY.md](.claude/DEPLOY.md) - деплой инструкции
- [TROUBLESHOOTING.md](.claude/TROUBLESHOOTING.md) - частые проблемы
