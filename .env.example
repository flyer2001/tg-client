# Telegram API credentials
  # Получите их на https://my.telegram.org/apps
  # 1. Авторизуйтесь с помощью номера телефона
  # 2. Создайте новое приложение
  # 3. Скопируйте "api_id" и "api_hash"
  
TELEGRAM_API_ID=your_api_id_here
TELEGRAM_API_HASH=your_api_hash_here
TELEGRAM_PHONE=+1234567890

# ========================================
# OPENAI API CONFIGURATION
# ========================================
# Получите API ключ: https://platform.openai.com/api-keys
# Необходим для генерации AI саммари непрочитанных сообщений

OPENAI_API_KEY=your_openai_api_key_here

  # Директория для хранения состояния TDLib (опционально, по умолчанию $HOME/.tdlib)
  # ВАЖНО: НЕ коммитьте эту директорию - там хранится сессия авторизации!

TDLIB_STATE_DIR=$HOME/.tdlib

  # Ключ шифрования для базы данных TDLib (AES-256)
  # Пустая строка - БД НЕ шифруется (небезопасно для production!)
  # Генерация случайного ключа: openssl rand -base64 32

TDLIB_DATABASE_ENCRYPTION_KEY=dataBasePasswordExample

  # Примечание: Скопируйте этот файл в .env и заполните реальными значениями
  # НЕ коммитьте файл .env в git (он уже в .gitignore)
  # НЕ используйте ключевое слово 'export' - только формат KEY=value